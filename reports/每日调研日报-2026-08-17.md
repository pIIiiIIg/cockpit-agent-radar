# 每日调研日报｜2026-08-17

证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、最近 H20 A/B 2026-08-03，本轮无新提交与新基准）。**Radar 连续第五轮未刷新**：`data/items.json` 的 `generated` 仍是 2026-08-12 09:30，新增 0 条。**上一轮预告的索引回补已经发生**：08-15 报告记录当时 arXiv 全站最新投稿是 2026-08-13T17:59:58Z 的 2608.13561，本轮同一查询返回 2026-08-14T17:59:59Z 的 2608.14549，即整个 08-14 批次已入库，而该批次**从未被任何一份已发布报告覆盖**。因此本轮窗口按投稿时间回拨为 **2026-08-13 18:00 UTC 至 2026-08-17 02:30 UTC**（与 08-15 报告的实际覆盖终点严格衔接、不重叠），去重后 83 篇。**注意 2026-08-15 02:30Z 之后 arXiv 全站仍是零投稿零修订**，本轮的新论文全部来自 08-14 那个积压批次而非最近两天。另：**08-16 那一轮没有产出报告**（`reports/` 与全部远端分支都没有 2026-08-16 的文件，其分支只有一次恢复提交），故对比基线为 2026-08-15，本报告覆盖两个自然日；main 上此前只有到 2026-08-10 的报告，08-11 至 08-15 五轮已在本轮先恢复到主线。制品复核时刻为 2026-08-17，晚于 02:30 的论文截止。

方向：流式 TTS — VoiceChat-TTS 是首个制品可逐文件核实的低时延流式 TTS（当前首音频 P50 2,918ms（A）/3,491ms（B）而首文本最快只要 131ms，瓶颈明确在 Edge TTS，此前唯一的 TTS 候选依据 Luna-TTS 项目页至今无代码无权重无许可证且页内自相矛盾；2608.13831（NVIDIA，2026-08-13T23:37:07Z 投稿）给出**并发 1**——与项目 batch=1 同口径——在 RTX A6000 上的实测：声学 ITL 7.16ms + codec 2.46ms = **9.62ms**，同协议重测的 Qwen3-TTS-12Hz-1.7B 为 15.44/4.90/20.34ms，每帧对应 80ms 音频，模型 977M（778M Gemma 3 流式 TTS + 199M codec）；制品本轮逐文件核实为真——代码在 `NVIDIA-NeMo/Speech`（**Apache-2.0**、18,144 star、2,882 条目未截断）内命中 31 个 `voicechat|duplex` 路径，权重在 `nvidia/NVIDIA-NemotronLabs-VoiceChat-11B`（**OpenMDW-1.1**、非门控、1,536 下载/390 likes）；可以尝试的**只有只读代码与制品审阅**，回答两个可判定问题——977M 的 TTS 子模块能否脱离 11B 主干单独实例化推理、22,050Hz 输出如何对接项目 16kHz 链路；三条边界写死：**全英文无中文指标**；TTS 子模块**没有独立权重文件**，被封在 44,382,749,892 字节的单一 `model.safetensors` 内且 HF 上不存在独立 TTS 模型页；**9.62ms 是 ITL 不是首包**，论文引言谈 TTFA 但结果部分从未报自己的 TTFA，首包时延记为未知）

方向：打断收尾 — 确定性静音帧是可脱离模型单独借用的机制（当前 `playback-boundary-ledger-offline-v1` 与 `tool-hold-message-first-audio-v1` 都只有协议层设计，缺一个明确的“收尾动作”定义；VoiceChat-TTS 把收尾实现成在中断对齐位置**确定性写入一个预先算好的静音帧**而不是等声学流自然衰减，且写入后**不重置 KV cache**，打开 `Force Silence` 后 IOR@320ms 由 96.8% 升到 100.0%、平均停止时延由 228.3ms 降到 89.9ms、1 秒内泄漏由 169.1ms 降到 55.8ms、后续轮 AI-CER 由 0.255% 降到 0.095%；该机制本轮**逐行核对与论文完全一致**——`duplex_ear_tts.py` 第 1193 行 `inference_force_speech_silence_on_eos`，`get_codec_silence_frame()` 编码 `10 * target_sample_rate` 的零波形、用 `Counter` 对每帧 31 个 token 的组合计数、取 `most_common(1)`，正是论文所述“编码一段较长纯静音并取出现频次最高的 31-token codec 帧”；可以尝试在项目自己的播报链路上实现同构的确定性收尾写入，但**那四个数字不可借用为项目预期值**——它们是英文 FDB 音频上用该模型自己的 codec 帧测的，任何借用都必须在项目自己的取消残留指标上单独验收）

方向：TTS 质量与速度的分离 — “更快”不等于“更好”（当前项目若做 TTS 替换容易只看时延一侧；同一张表里 Qwen3-TTS-12Hz-1.7B 的 CER 0.34%、WER 1.01%、SECS 0.827、Squim-MOS 4.45 **四项全面优于** VoiceChat-TTS 的 1.00%/2.00%/0.757/4.380，论文自己也写“常规 TTS 系统取得了最好的单轮可懂度、说话人相似度与预测整体语音质量”；作者另主动写出一条退化——未见说话人的 SECS 随轮次由 1 轮 0.757 单调降到 4 轮 0.685；可以尝试的是把“时延 + 质量”固定成替换实验的双侧验收口径，并记住 Squim-MOS 是**非侵入式预测**质量估计而非主观 MOS，论文明确说它“作为非侵入式质量估计而不是自然度的直接度量”使用）

方向：MoE 解码 — DeaMoE 把“小批量”的下界钉在 batch 4，是一条筛子而不是一个方案（当前解码 110ms/token，根因已实测定位到 HF MoE 每 token 约 816 次 GPU→CPU 同步，速度不随 KV 长度变化、与按 config 实算的带宽下限差约 36 倍；2608.14385 标题与摘要都主打 small-batch decoding，摘要给出每步加载权重最多减少 50.9%、7B 端到端 TPOT 最高 1.33×、DeepSeek-V3 微基准 A40/H100 峰值 2.00×/1.97×，但图 3 横轴**从 batch 4 起步**，逐点为 4→1.02×、8→1.14×、16→1.26×、24→1.32×、32→**1.33×**、40→1.30×、48→1.26×、64→1.20×、96→1.13×、128→1.03×，论文原话是“加速比随 batch 从 4 增长到 32 而上升”；项目是 batch=1，位于曲线起点之外更低的一侧，按其自身趋势外推收益低于 1.02×；可以尝试的是把家族判据从“需要大 batch”**扩写为“最小可评测 batch 是否为 1；若不是且收益随 batch 减小而单调下降则直接否”**，一次性挡掉整类以 small-batch 为卖点却从 batch 4 起步的工作；另两条独立否决理由：它是**预训练期架构改动**需从零预训练 7.3B/110B token，对既有 Qwen3-Omni 权重不可用；它攻击的是**访存带宽**瓶颈——论文原话“缓解 A40 GPU 上的带宽瓶颈”——而项目实测瓶颈**不是带宽**，同一个词“解码慢”两边指的是完全不同的东西；开放状态：**无代码无权重**，全文唯一 GitHub URL 是参考文献里的 Meituan LongCat，61 条 URI 注解逐条查看全为参考文献，正文只承诺“训练用代码将在之后开源”）

方向：图捕获 — CUDA Graph 拿到第三条独立旁证且形式最强（当前 `decode-loop-cudagraph-static-kv-v1` 的依据是 08-14 的 FlashDrive 与 vToken 两条线索；DeaMoE 在实现小节写“我们也为 DeaMoE 与所有基线启用了 vLLM 的 CUDA Graph 执行。由于解码本身对时延敏感且通常运行在小批量区间，采用 CUDA Graph 是业界降低 kernel 启动开销的通行做法”——它不是把图捕获当优化来宣传，而是当作**基线也必须具备的标配**否则对照不公平；可以尝试把这条记为先验提高，但**执行顺序不变**：仍是先落地无同步 `index_select`+`bmm` 快路径，再把整环图捕获作为第二个独立变量单独测量，两步不得合并成一次结论；另有一条**仅供定性参考不可相减**的量级观察——DeaMoE 的基线在 A40 上于 20–30ms TPOT 预算内可支撑 batch 24–64，而项目在更强的 H20 上 batch=1 却是 110ms/token，但模型/框架/硬件/batch 四项全不同，只能支持“110ms 更像实现问题而非硬件问题”这一方向性判断）

方向：全双工评测 — DuplexWorld 发的是场景规格，不是评测能力（当前项目评测仍是自建 452 条加 414 冻结 canary，无第三方全双工场景集；`CentificAIResearch/Duplex-World` 于 2026-08-15T16:50:32Z 创建、**CC-BY-4.0**、非门控、标注 `arxiv:2608.10716`，六个领域加 `all.jsonl` 共 10 个文件；本轮实测两项自洽性检查均通过——六个领域文件字节和恰为 103,426 与 `all.jsonl` **完全相等**，行数和 3+2+2+1+9+1 = **18** 也相等；单条不是短文本而是约 10KB 的完整场景规格，17 个字段含 `decision_tree`（2,566 字节）与 `evaluation_criteria`（2,459 字节）；可以尝试的只有格式借鉴——把“系统应该怎么走”和“怎么算通过”在数据结构上分成两个独立字段，正是判定器不该读到某些字段的前提，可用来支撑既有的 `grader-treatment-invariance-audit`；边界写死：**没有音频、没有 harness 代码**，08-11 记录的最有价值结论“三个 harness 旋钮对 Pass@1 的影响与换系统相当”**并未因本次发布变得可复现**；**18 条不足以支撑任何准确率结论**，README 的 `size_categories: n<1K` 技术上成立但会严重高估规模，不得并入项目 452 条评测集）

方向：检索基础设施 — 零结果与索引回补是同一条时间线的先后状态（当前 Radar 连续第五轮未刷新，直接检索是唯一发现通道；本轮先按常规窗口 `submittedDate:[202608150230 TO 202608170230]` 跑 14 组查询全部返回 0 条，随后三级证伪——去日期约束的对照查询正常返回（cs.SD 全站 **21,696**，08-15 记录为 21,686）、不限类别按投稿时间倒序的最新一条已由 2608.13561 前进到 **2608.14549（2026-08-14T17:59:59Z）**、全站窗口 `[202608150230 TO 202608170230]` totalResults 仍为 **0** 而 `[202608131800 TO 202608170230]` 为 249；逐类别最新条目全部落在 08-14 12:34Z–17:59Z，`lastUpdatedDate` 落在最近两天的修订三组查询均为 0；可以尝试记住两条判据——两次的全站最新投稿时刻都是 17:59:5x 即 arXiv 每日 14:00 ET 截稿边界，说明**索引按整批推进而非逐篇滴入**，所以“窗口零结果”与“批次尚未入库”是同一现象的两种描述，08-15 那份零新增报告在当时是正确的、本轮 83 篇也不是它漏掉的；下一轮窗口起点为 2026-08-17 02:30Z，且必须再查全站最新投稿时刻——若仍停在 08-14T18:00Z 说明 08-15/08-16 两批仍未入库、还会有更大一次回补，**一律按投稿时间归属**）

方向：回声消除 — 连续第五轮无重大新增，且排除了一个假阳性（当前 AEC 与扬声器泄漏仍完全没有一手依据；本窗口 `speech enhancement OR noise suppression OR echo cancellation OR beamforming` 一组返回 5 篇，逐条查看后**全部是无线通信波束成形**（mmWave 信道估计、感知通信计算一体化、6G 光子基站、SIM-UAV 波束优化）与一篇冷原子物理，**无一为声学 AEC**；HF 窗口内唯一命中 `echo` 关键词的一手发布 `X-Zhang/EchoMask`（2026-08-16T06:33:52Z）经核实指向 arXiv:2504.09209《EchoMask: Speech-Queried Attention-based Mask Modeling for Holistic Co-Speech Motion Generation》（ACM MM 2025，cs.GR/cs.CV/cs.SD），是**语音驱动的协同手势与整体动作生成**，产物为 SMPL-X 动作与 BEAT2 相关的 npz/权重压缩包，与回声消除毫无关系，已从该方向排除；附带一条支持既有复核纪律的事实——该论文 2025-04-12 投稿、2026-08-16 才发官方 checkpoint，**制品滞后约 16 个月**；继续跟踪，不写任何 AEC 结论或预期）

方向：制品核对 — `pushed_at` 也会在没有可达新提交时前进（当前既有用法是“`updated_at` 会因 star 变动而动，要用 `pushed_at` 对照提交列表”；本轮 `Yaxin9Luo/AutoDesign` 的 `pushed_at` 为 **2026-08-16T12:19:13Z**，但 `git/refs` 里只有 `refs/heads/main` = `cd5b2bd997`（**2026-08-15T08:24:24Z**）与两个 PR head，**默认分支 HEAD 并未前进**，递归树仍 **724 条目**、42 个 json/jsonl/csv 集合未变、releases 0 / tags 0；可以尝试把复核动作再往下加一层——判断代码是否真的变化必须查到 `git/refs` 与提交列表，`updated_at` 与 `pushed_at` **都不足够**；附带结论：论文附录 A.5 承诺的逐案例分数存档与人评决策记录**连续第三次复核仍不存在**，数据类文件仍只有 `eval/benchmark_manifest.jsonl` 与 `eval/small_subset_ids.json`，引用 r=0.34 时仍须附一句“其原始人评数据不可被第三方复核”）

方向：去重 — HF 窗口 148 条新条目，一手发布 3 条，且项目基座开始出现第三方衍生（当前 arXiv 最近两天为零时容易把 HF 新增当补充来源；本轮以 14 个关键词按创建时间筛出窗口内 **148 条**，一手发布仅 3 条——`CentificAIResearch/Duplex-World`（CC-BY-4.0）、`X-Zhang/EchoMask` 与其推理数据集、`laion/moss-voice-identity-loras` 与 `moss-voice-identity-repairs`（Apache-2.0，仅跟踪）；可以尝试新增一条噪声过滤规则——**把 `base_model:*Qwen3-Omni*` 明确列入过滤**，因为本轮出现 `VikramPal/Qwen3-Omni-30B-A3B-*` 共 8 条（S2S 与 SLURP 两组各含 QLoRA/bf16/DynQuant-3bit/DynQuant-4bit），全部带 `base_model:Qwen/Qwen3-Omni-30B-A3B-Instruct` 系标签、下载与点赞均为 0，是**项目所用基座的第三方衍生而非官方发布**，极易被误读为基座更新；另记批量单作者上传仍在抬高计数——`Devsyril/mms-tts-*-sherpa-onnx` 10 条、`uam-wmi-asr-eval-labs/*` 8 条、`anonymous4486/audio-flamingo-3-hf_*` 6 条、`ghananlpcommunity/*` 与 `lingamvamshikrishnareddy/*` 各 4 条，合计 32 条只相当于个位数的实际发布，**按发布计而不是按行计**）

方向：持续跟踪不重启的项目（判定器处理不变性检查仍是优先级最高的一件事，**高于本轮的 TTS 只读审阅**，本轮唯一相关新增素材是 DuplexWorld 把 `decision_tree` 与 `evaluation_criteria` 分成两个独立字段这一写法，可作为“判定器不得读取哪些字段”排除清单的参照；快照“下一步”第 2 条的 clean/20dB/音色分桶归因连续第三轮列为首位，仍是 `streaming-se-frontend-rtsemamba-kd1-v1` 的硬前提与其 `expected_fixes` 上界的唯一来源；`RoyChao19477/RT-SEMamba` 本轮复核 `pushed_at` 仍 2026-08-14T05:16:18Z、size 仍 14368、仍 1 star，连续第三轮字节级不变；`CodeForZzc/GCM-Bench` 最后推送仍 2026-08-11T06:29:47Z 连续第六天不变、PACE 中间件本体仍未公开；`ASLP-lab/SemBridge` 仍占位（`pushed_at` 2026-08-10T05:51:29Z，`updated_at` 2026-08-15 为元数据活动，再次印证 `updated_at` 不可用于判断代码变更）；`ASLP-lab/Smart-Glass-Challenge` 仍 26KB 无 LICENSE；`z-lab/flashdrive` 仍 MIT、最后推送仍 2026-07-06T00:54:50Z；`aalto-speech/CASA` 仍 Apache-2.0、`pushed_at` 仍 2026-08-13T09:59:25Z、无权重文件；**20 个跟踪 arXiv ID 经逐一复核全部仍为 v1**，`updated` 均等于 `published`，无一有 v2；candidate-ID/CMTF、conditional top-2 verifier、投机预取、工具清单压缩、主模型自判停的负结果不重跑；参数化本体、云监督与 SoulX 不重复启动）

今日新增 handoff 候选：**0 条**。本轮唯一够格进入执行序列的新素材是 VoiceChat-TTS，但它有两个未决的可判定问题——977M TTS 子模块能否脱离 44.4GB 的 11B 整包单独实例化、22,050Hz 与项目 16kHz 链路如何对接——在这两个问题有答案之前既没有清晰单变量也没有可测的预计修复数，按既定规则**只留在报告并登记为只读前置任务，不进入候选**。DuplexWorld 数据集只有 18 条且无音频无 harness，只能做格式借鉴，同样没有单变量。DeaMoE 是明确的反面判据，本身就不该成为候选。既有五条候选 `omnivoice-local-tts-16step-v1`、`tool-hold-message-first-audio-v1`、`playback-boundary-ledger-offline-v1`、`decode-loop-cudagraph-static-kv-v1`、`streaming-se-frontend-rtsemamba-kd1-v1` 全部保持仅离线、`h20_request_allowed=false`，本轮无一条状态变化；其中 `decode-loop-cudagraph-static-kv-v1` 的外部先验因 DeaMoE 的旁证而提高，但顺序与门槛不变。

去重与来源关系：本轮四处必须写明的来源关系。其一，**VoiceChat-TTS（2608.13831）不是独立新工作**，而是 08-12 报告已记录的 NVIDIA NemotronLabs VoiceChat 11B 全双工模型的**TTS 组件级论文**，论文自述其 checkpoint “作为 NVIDIA-NemotronLabs-VoiceChat-11B 的一部分”提供，因此 08-12 记录的整机数字（FDB-v3 工具选择 82.5%、参数准确率 44.2%、Pass@1 33%、口语 BFCL-v3 平均 56.1%）与本轮的 TTS 数字**不可混用**；另注意该权重页 `lastModified` 为 2026-08-11T09:46:06Z，**早于本论文 08-13 的投稿时刻**，即论文发表并未伴随任何新上传。其二，**DuplexWorld 数据集发布不是新论文**，而是 2608.10716（2026-08-11 投稿、08-11 已展开）的制品发布，与 RT-SEMamba 于 08-14 发布制品同类，不计入新增 arXiv ID。其三，**`X-Zhang/EchoMask` 与回声消除无关**，是 arXiv:2504.09209 的官方 checkpoint（ACM MM 2025，协同语音动作生成），仅因关键词 `echo` 被扫描命中。其四，`VikramPal/Qwen3-Omni-30B-A3B-*` 等 8 条是项目基座的第三方量化与微调衍生，不是官方发布。另记两处源内不一致与状态：VoiceChat-TTS 的对照模型名为 `Qwen3-TTS-12Hz`（12Hz）而论文正文两处均写“同样工作在 **12.5 Hz**”，两值并列不代为取舍；DeaMoE 的论文 PDF `/License` 为 CC-BY-NC-ND-4.0，**这是论文本身的许可证与任何代码或权重无关**，其代码与权重均未公开。本轮所有新论文均为 v1，且逐个 arXiv ID grep 确认此前从未出现在任何报告或 `data/items.json` 中。
