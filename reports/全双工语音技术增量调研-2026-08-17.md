# 全双工语音技术增量调研

更新日期：2026-08-17

证据日期：

- 项目状态快照仍是 `project_status/StreamingModelHarness.md`，生成于 2026-08-10 10:30（UTC+8）；其中最新提交实际日期为 2026-08-04，最近 H20 A/B 基准实际日期为 2026-08-03。本轮没有新的项目提交，也没有新基准。
- `data/items.json` 的 `generated` 仍是 2026-08-12T09:30:35+08:00：**Radar 已连续第五轮未刷新，新增条目为 0 条**。发现通道仍然只能靠直接检索 arXiv 与 Hugging Face。
- 本轮的论文检索窗口为 **2026-08-13 18:00 UTC 至 2026-08-17 02:30 UTC**。这个窗口比平常宽，原因见下条，不是口径放松。
- **上一轮（08-15）预告的“索引恢复后积压一次性出现”已经发生。** 08-15 报告记录当时 arXiv 全站最新投稿是 `2608.13561`、投稿时间 2026-08-13T17:59:58Z，并写明“下一轮需留意 2026-08-13T18:00Z 之后的积压投稿一次性出现，按投稿时间而不是发现时间归属”。本轮复核：全站最新投稿已变为 `2608.14549`，投稿时间 **2026-08-14T17:59:59Z**，即整个 08-14 批次已进入索引。**该批次从未被任何一份已发布报告覆盖过**，因此本轮按投稿时间把窗口起点回拨到 2026-08-13T18:00Z，与 08-15 报告的实际覆盖终点严格衔接、不重叠。
- **08-16 那一轮没有产出报告。** 事实：`reports/` 中不存在 `2026-08-16` 的任何文件，全部远端分支上也没有；08-16 的 agent 分支（`cursor/bc-9902f1ad-...`）只有一次 `reports: recover unpublished 2026-08-11..2026-08-15` 提交，没有当日报告。因此本报告的对比基线是 08-15，覆盖两个自然日，这一点在下文所有“较前一日”的措辞中已按两日口径处理。
- **窗口内 2026-08-15 02:30Z 至 2026-08-17 02:30Z 这一段，arXiv 全站新投稿与修订仍然都是 0 条**（三级证伪见第 4 节）。也就是说本轮的全部新论文都来自 08-14 那个积压批次，而不是最近两天。
- 制品与开放状态的复核时刻为 **2026-08-17**，晚于 02:30 UTC 的论文截止；涉及分钟级或小时级时间差处已标注确切时间戳。
- 所有开放状态结论只来自 GitHub / Hugging Face 的元数据 API、`LICENSE` 与 README 文本、仓库内配置文件与源码文本，以及作者项目页 HTML。**本轮没有运行任何论文仓库代码**，也没有调用 Kimi / Moonshot 或 `scripts/enhance_kimi.py`。

对比基线：

- `全双工语音技术增量调研-2026-08-15.md`
- 说明：main 上此前最新的已提交报告仍是 2026-08-10，08-11 至 08-15 五轮报告都只存在于各自未合并的 agent 分支上。本轮已先把五天的两份报告与 `data/handoff/candidates/*.json` 原样恢复到主线（单独一次 `reports: recover unpublished ...` 提交），因此本报告可以按正常口径写作。

面向项目：

- StreamingModelHarness
- Cockpit Agent Radar
- 视觉、音频、文本与车控融合的智能座舱助手

## StreamingModelHarness 当前进展与问题

### 项目实测事实

本轮没有任何新的项目证据。以下数字全部沿用已记载的同一批结果，不是今天产生的：

- 2026-08-03 H20 公平对照使用 `audio_index.jsonl` 中完全相同的前 100 条音频（82 条车控 + 18 条日常对话）。A/B 严格工具召回/执行成功率 80.49% / 79.27%，100 条样本下置信区间高度重叠；日常对话误车控均为 0%；RAG Recall@24 为 98.78%；ASR micro CER 6.39%。
- A/B 时延 P50/P90：ASR 完成 183/248ms（仅 A）；RAG 完成 227/315ms（仅 A）；首文本 758/1,006ms 与 131/141ms；首动作 1,094/1,440ms 与 700/850ms；首音频 2,918/3,453ms 与 3,491/4,171ms；整轮结束 2,945/4,224ms 与 3,576/4,246ms。项目结论是 Edge TTS 而非 Qwen 首 token 构成约 3 秒首音频的主要瓶颈。
- 单 token 解码约 110ms，速度不随 KV 长度变化；根因定位到 HF MoE 实现在单 token 解码时每层多次 GPU→CPU 同步（`.nonzero()`、循环内 GPU 标量比较、`torch.where`），48 层 × 17 次 ≈ 816 次/token。按 config 实算的带宽下限为 5090 约 3.0ms、H200 约 1.1ms，实测是下限的约 36 倍（5090 口径）。
- `DynamicCache.update()` 的 `torch.cat` 在 KV=2500 时约 0.2ms，项目判定当前规模下 `StaticCache` 不值得；08-14 已为它补了一个不同性质的理由（图捕获前置条件），该结论本轮不变。
- 闲聊仍注入工具（滤净需 T=5，代价误伤 6.25% 真指令）；上下文引力与聚合意图展开不全未闭环；`probe_turn_end` 主模型自判停为明确负面结果。
- 既有负结果（防重复）：baseline 414/452、召回 443/452；conditional top-2 verifier 离线仅触发 5 次、净修 4，已拒绝 H20；candidate-ID/CMTF 强制覆盖已否决；投机预取与工具清单压缩均为实测负优化。
- 快照“下一步”清单仍未完成，其中第 2 条“对 clean、20dB、音色分别分桶，定位 ASR 与模型理解错误”仍是 08-15 前端候选的硬前提，本轮未变。
- 在办候选：`omnivoice-local-tts-16step-v1`（08-10）、`tool-hold-message-first-audio-v1` 与 `playback-boundary-ledger-offline-v1`（08-12）、`decode-loop-cudagraph-static-kv-v1`（08-14）、`streaming-se-frontend-rtsemamba-kd1-v1`（08-15），全部仅离线、`h20_request_allowed=false`。

### 问题表

| 当前问题 | 项目证据 | 今日相关技术 | 可执行实验 | 成功门槛 |
|---|---|---|---|---|
| 首音频约 3 秒，瓶颈在 TTS 而不是主模型；本地流式 TTS 候选至今没有一个可复核的一手制品 | 首音频 P50/P90 = 2,918/3,453ms（A）与 3,491/4,171ms（B）；首文本仅 131ms（B）。项目自身尚未测过任何本地 TTS 首包 | **VoiceChat-TTS（2608.13831，NVIDIA）**：代码在 `NVIDIA-NeMo/Speech`（Apache-2.0）内已可读到，权重随 `nvidia/NVIDIA-NemotronLabs-VoiceChat-11B`（OpenMDW-1.1）发布。并发 1 下 A6000 实测声学 ITL 7.16ms + 编解码 2.46ms = 9.62ms/帧，每帧对应 80ms 音频 | 不改主链路。先做**只读代码审阅 + 制品盘点**，确认 977M TTS 子模块能否从 44.4GB 单文件 checkpoint 中单独取出；能取出再谈单变量替换 Edge TTS | 首包/整句/取消残留三段分开验收；必须自测中文，论文全部为英文；414 canary 零退化；不得用论文的 ITL 冒充首包时延 |
| 解码 110ms/token，根因是 HF MoE 的每 token 约 816 次主机同步 | 速度不随 KV 长度变化，与带宽下限差约 36 倍 | **DeaMoE（2608.14385）**：标题即“小批量解码”，但其“小批量”下界是 **batch 4**，加速比在 batch 4 时仅 1.02×、batch 32 峰值 1.33×、batch 128 回落到 1.03×；且它是**预训练期架构改动**，需从零预训练 | 不做。作为筛子记录：本条同时被“非 batch=1”和“需重新预训练”两条否掉 | 该论文只作为反面判据与 CUDA Graph 旁证使用，不进入候选，不重复评估同族工作 |
| `decode-loop-cudagraph-static-kv-v1` 缺少项目外的独立佐证 | 08-14 由 FlashDrive、vToken 两条独立线索支持图捕获兼容性作为硬筛子 | DeaMoE 明确写“我们为 DeaMoE 与全部基线都启用了 vLLM 的 CUDA Graph 执行……在小批量场景采用 CUDA Graph 是业界降低 kernel 启动开销的通行做法” | 不新增实验。把它记为第三条独立旁证，候选顺序不变 | 仍是先落地无同步 `index_select`+`bmm` 快路径，再把整环图捕获作为**第二个独立变量**；两步不得合并成一次结论 |
| 打断后的播报收尾与 KV 状态处理没有可参照的开源实现 | `playback-boundary-ledger-offline-v1` 与 `tool-hold-message-first-audio-v1` 均只有协议层设计，无参考实现 | VoiceChat-TTS 的中断走控制 token，并且**中断后不重置 KV cache**；`Force Silence` 打开后 IOR@320ms 由 96.8% 升到 100.0%，平均停止时延由 228.3ms 降到 89.9ms，1 秒泄漏由 169.1ms 降到 55.8ms | 只读研读 `duplex_ear_tts.py` 的中断与强制静音实现，抽取“确定性静音帧”这一机制，落到项目自己的播报收尾协议 | 只借机制不搬代码；任何借用都必须在项目自己的取消残留指标上单独验收 |
| 全双工评测缺可直接消费的场景数据 | 项目评测仍是自建 452 条 + 414 冻结 canary，无第三方全双工场景集 | **DuplexWorld 正式发布数据集** `CentificAIResearch/Duplex-World`（CC-BY-4.0，2026-08-15T16:50:32Z 创建），六个领域共 **18 条场景**，每条约 10KB 的完整规格 | 只做格式借鉴：抽取其 `evaluation_criteria` 与 `decision_tree` 字段结构，对齐项目自己的判定协议 | 明确写清它**不含音频、不含 harness 代码**，18 条规模不足以做统计判定，不得用于任何准确率结论 |
| 回声消除仍无一手依据 | 只有 clean/20dB 两档合成音频 | 无。窗口内没有任何 AEC 论文；HF 窗口里唯一命中 `echo` 关键词的 `X-Zhang/EchoMask` 经核实是**协同语音手势生成**（arXiv:2504.09209，ACM MM 2025），与回声消除无关 | 不做。连续第五轮记为无重大新增 | 没有 AEC 一手证据前不写任何回声消除结论或预期 |

## 一、先说结论

**今日有新论文，但它们全部来自 2026-08-14 的积压批次，而不是最近两天。** 最近两天（08-15 02:30Z 之后）arXiv 全站依然是零投稿零修订。本轮最有决策价值的三件事：一个直接对着项目首音频瓶颈的 TTS 一手制品、一个把“小批量解码”这个词的含义钉死的反面判据、以及一次证明“索引冻结不等于没有论文”的流程验证。

### P0：直接命中当前工程短板

1. **VoiceChat-TTS（2608.13831）是迄今第一个同时具备“流式 TTS + 显式打断 + 不重置 KV + 代码和权重都真实存在”的一手制品，直接对着项目首音频约 3 秒这个最大瓶颈。** 论文事实：并发 1（即 batch=1，与项目口径一致）下在 RTX A6000 上，声学 token ITL 7.16ms、编解码 2.46ms、合计 9.62ms，而每帧对应 80ms 音频；同口径重测的 Qwen3-TTS-12Hz-1.7B 为 15.44 / 4.90 / 20.34ms。制品事实（本轮现场逐文件核实）：代码确实在 `NVIDIA-NeMo/Speech`（Apache-2.0、18,144 star）内，`nemo/collections/speechlm2/models/duplex_ear_tts.py` 等 31 个路径命中；权重在 `nvidia/NVIDIA-NemotronLabs-VoiceChat-11B`（OpenMDW-1.1、非门控）。编辑判断：这是本周唯一一个能把“本地流式 TTS”从纸面推进到可复核对象的更新，**但它不能直接替换 Edge TTS**，原因是两条硬事实——它只有英文评测，且 TTS 子模块（977M）被封在一个 44,382,749,892 字节的单一 `model.safetensors` 里，没有独立发布。详见第 1 节。
2. **DeaMoE（2608.14385）把“小批量解码”这个词的实际含义钉死了，这对项目是一条比任何加速方案都更有用的筛子。** 论文事实：标题与摘要都主打 small-batch decoding，但图 3 的横轴从 **batch 4** 起步，加速比 batch 4 时 1.02×、batch 32 峰值 1.33×、batch 128 回落 1.03×，呈非单调；论文自述“加速比随 batch 由 4 增长到 32 而上升”。编辑判断：项目是 batch=1，位于该曲线起点之外且更低的一侧，按其自身趋势外推收益低于 1.02×，实质为零。更关键的是它是**预训练期架构改动**（自行预训练 7.3B、110B token），对既有的 Qwen3-Omni 权重不可用。这使它成为“KV/服务反模式家族”的第四个成员，但成因与前三个不同——不是“需要大 batch”，而是“最小可评测 batch 就不是 1”。详见第 2 节。
3. **CoRun（2608.14376）把“固定形状解码”与“输出可复现”绑在一起，这给 `decode-loop-cudagraph-static-kv-v1` 增加了一个此前没有考虑过、且不依赖时延成败的收益维度。** 论文事实：CoRun 用隔离 prefill + 固定形状批量 decode + 固定尺寸 CUDA Graph 取得确定性输出，相对 batch-invariant 方案吞吐提升 15–324%，端到端时延分解为 Standard 19.4ms / Batch-invariant 88.4ms / CoRun 32.6ms。**与项目最相关的一条**：在其对照实验里，Qwen3-30B-A3B（MoE）的 rollback ratio 为 **45.41%**，接近同规模稠密模型 Qwen3-32B（24.25%）的两倍，论文解释为“一个很小的 router 扰动就可能改变被选中的专家，此后两条执行路径执行的是不同的计算，而不只是累积不同的低位比特”。编辑判断：项目基座 Qwen3-Omni-30B-A3B 同属 A3B MoE 族，这条**机制性**结论可迁移；但 45.41% 这个**数值**不可迁移（它是投机-验证框架下的候选拒绝率，不是“单次 batch=1 推理有 45% 概率不可复现”）。真正该做的是一个零成本自测：同一音频同一 seed 连续跑 N 次，逐 token 比对。详见第 5 节。
4. **同一篇 DeaMoE 顺带给了 `decode-loop-cudagraph-static-kv-v1` 第三条独立旁证。** 论文原文：“我们也为 DeaMoE 与所有基线启用了 vLLM 的 CUDA Graph 执行。由于解码本身对时延敏感且通常运行在小批量区间，采用 CUDA Graph 是业界降低 kernel 启动开销的通行做法。” 编辑判断：08-14 由 FlashDrive 提出、vToken 佐证的“图捕获兼容性是硬筛子”，现在有了第三个来源，而且是把它当作**基线也必须启用的标配**而非优化项。这提高了该候选的先验，但**不改变执行顺序**：仍然先做无同步 `index_select`+`bmm` 快路径，再把整环图捕获作为第二个独立变量。

### P1：适合形成下一阶段架构

1. **VoiceChat-TTS 的“确定性静音帧”是一个可以脱离其模型单独借用的机制，直接落在项目两个在办候选上。** 论文事实：打开 `Force Silence` 后 IOR@320ms 由 96.8% 升到 100.0%，平均停止时延由 228.3ms 降到 89.9ms，1 秒内泄漏由 169.1ms 降到 55.8ms，后续轮次的 AI-CER 由 0.255% 降到 0.095%，且**全程不重置 KV cache**。仓库事实（本轮逐行核实）：`duplex_ear_tts.py` 第 1193 行的 `inference_force_speech_silence_on_eos` 与 `get_codec_silence_frame()` 与论文描述**逐字对应**——编码 10 秒纯静音波形、对每帧的 31 个 token 组合计数、取出现频次最高的那一帧。编辑判断：这条机制的可迁移部分不是模型，而是“把收尾定义成一个确定性的静音写入动作，而不是等待自然衰减”，这正是 `playback-boundary-ledger-offline-v1` 缺的那一块。详见第 1 节。
2. **DuplexWorld 的数据集正式发布了，但发布的是场景规格而不是评测能力，这个区分必须写清楚。** 制品事实：`CentificAIResearch/Duplex-World` 于 2026-08-15T16:50:32Z 创建，CC-BY-4.0、非门控，标注 `arxiv:2608.10716`；六个领域文件加 `all.jsonl` 共 10 个文件，**总计 18 条**（banking 3、healthcare 2、insurance 2、logistics 1、pathfinding 9、travel 1），`all.jsonl` 的字节数 103,426 与六个领域文件之和**完全相等**。每条约 10KB，含 `decision_tree`、`evaluation_criteria`、`task_params` 等 17 个字段。编辑判断：**没有音频，没有 harness 代码**——而 08-11 记录的 DuplexWorld 最有价值的结论恰恰是“三个 harness 旋钮对 Pass@1 的影响与换系统相当”，那部分依然不可复现。可借用的只有评测标准的字段结构。详见第 3 节。

3. **“用受约束解码去修工具弃权”这条路有了明确的负面证据，项目应当继续不走它。** 论文事实（2608.13959，已全文核实）：把约束语法的两件事——规定生成在哪里停止、规定哪些 token 可发——分开测之后，相对无约束解码，弃权列按符号在六个单元格中的**五个**为负，其中**四个区间不含零**（0.6B KO 是 −0.5 [−9.5, +8.0]，区间含零），**最差 −29.5 个百分点**，而没有任何一个单元格出现区间不含零的正效果；分解到最小模型的韩语上是停止 token **−20.0**、enum **+19.5**、合计 **−0.5**；被修复的 698 条弃权中 **545 条原本没有可读答案、0 条是评分器拒绝给出的判断**。编辑判断：项目当前走的是候选工具集晚注入加确定性执行器校验（已拦住 45% 的失配大头），不是解码期语法约束；这篇支持维持现状。可复用的是它的实验设计——把“停止位置”与“可发 token 集合”当作两个独立变量分别测。**注意它测的是弃权，作者说在“需要调用工具”的条目上效果全程为正**，不得读成“约束解码总是有害”。详见 6.1。

### P2：长期研究与明确的边界

1. **“arXiv 索引冻结”与“没有论文”是两件事，本轮给出了这一区分的完整证据。** 复核事实：08-15 报告在 08-15T02:30Z 时刻看到的全站最新投稿是 2026-08-13T17:59:58Z；本轮同一查询返回 2026-08-14T17:59:59Z，即 08-14 整批已回补；而 `submittedDate:[202608150230 TO 202608170230]` 全站 `totalResults` 依然是 **0**。编辑判断：08-15 那份“零新增”报告在当时是正确的，本轮的 83 篇也不是它漏掉的——两者是同一条时间线上的先后状态。这条流程结论的价值在于：**零结果必须记录窗口与全站最新投稿时刻，否则下一轮无法区分回补与漏检**。详见第 4 节。
2. **关键词扫描的假阳性这次以最干净的形式出现了一例。** 复核事实：HF 窗口内唯一命中 `echo` 关键词的一手发布是 `X-Zhang/EchoMask`（2026-08-16T06:33:52Z），其 README 与标签指向 arXiv:2504.09209《EchoMask: Speech-Queried Attention-based Mask Modeling for Holistic Co-Speech Motion Generation》（ACM MM 2025，cs.GR/cs.CV/cs.SD），是**协同语音手势生成**，与回声消除毫无关系。编辑判断：AEC 至今连续第五轮零一手证据这一结论不受影响；同时记录一条附带事实——该论文 2025-04 投稿、2026-08 才发权重，制品滞后约 16 个月，再次支持“一次判定无制品不等于永久无制品”的复核纪律。
3. **两条对项目既有做法的旁证，均来自本轮全文核实的六篇。** 其一（2608.14036）：当技能候选池由 5 增长到 100，**实际使用精度由 29.6% 降到 3.3%**，而项目的 RAG 面对的是 **300 个不同工具**——池规模劣化与项目已知的“上下文引力：光场屏偶被选成副驾屏”方向一致；同一篇还写“精确命中 ground-truth 调用既不充分也不必要”，与项目已记录的“严格工具名召回是偏保守口径、语义等价工具会被计为失败”相互印证。其二（2608.14191）：AATC 摘要首句即把 KV cache 定位为**长上下文推理**的内存瓶颈，评测全在 LongBench、RULER 一类长上下文基准——这是 KV/服务反模式家族的**第五个成员**。全文核实后还多出一条决定性的作者自述：“我们的实现没有做系统级优化，特别是**缺少专门的 CUDA 实现**，而这在该方法能用于生产之前是必需的”，且全文零墙钟数字、零硬件声明、`batch` 一词从未出现。两条数字均为**其它领域**所得，只作设计告警，不可搬为项目预期值。
4. **`pushed_at` 陷阱出现了一个新方向，值得写进流程。** 复核事实：`Yaxin9Luo/AutoDesign` 的 `pushed_at` 为 2026-08-16T12:19:13Z，但 `refs` 里只有 `refs/heads/main`（`cd5b2bd997`，2026-08-15T08:24:24Z）与两个 PR head，即**默认分支 HEAD 并未前进**，递归树仍是 724 条目。编辑判断：既有记忆里的用法是“`updated_at` 会因 star 变动而动，要用 `pushed_at` 对照提交列表”；本例说明 **`pushed_at` 自己也会在没有任何可达新提交的情况下前进**，必须再往下走一层查 `git/refs`。附带结论：论文附录 A.5 承诺的逐案例分数存档与人评决策记录**连续第三次复核仍不存在**，因此引用 r=0.34 时仍须附带“其原始人评数据不可被第三方复核”。

## 1. VoiceChat-TTS：面向交互式 Agent 的低时延连续语音合成（NVIDIA）

论文（arXiv v1，2026-08-13T23:37:07Z 提交）：https://arxiv.org/abs/2608.13831

官方代码：https://github.com/NVIDIA-NeMo/Speech

官方权重：https://huggingface.co/nvidia/NVIDIA-NemotronLabs-VoiceChat-11B

### 它是什么

一个**只做语音合成**的流式 TTS 模块，由 LLM 的文本 token 流直接驱动，设计目标是在全双工交互中“一直在线”：没有文本可播时输出静音，收到中断控制 token 时立即转静音，并且跨轮次不重置 KV cache。作者为 NVIDIA，第一作者 Edresson Casanova。

**同源关系必须先说清楚**：这不是一项独立的新工作，而是 08-12 报告已记录的 **NVIDIA NemotronLabs VoiceChat 11B 全双工模型的 TTS 组件**的单独成文。论文自述“VoiceChat-TTS 的代码已在 NVIDIA NeMo Speech 中公开，模型 checkpoint 作为 NVIDIA-NemotronLabs-VoiceChat-11B 的一部分在 Hugging Face 上提供”。按去重纪律，本节记为**同一技术脉络的组件级论文**，不计为新的开源模型发布；08-12 记录的那些整机数字（FDB-v3 工具选择 82.5%、参数准确率 44.2%、Pass@1 33%、口语 BFCL-v3 平均 56.1%）与本节的 TTS 数字不可混用。

### 核心方法

- **直接消费 LLM 文本 token 流**。为解决 LLM 子词词表与 TTS 训练语料不匹配（很多子词在 TTS 语料中罕见或缺失），引入 **Character-Aware Subword Encoder**：每个输入子词先拆成字符序列再编码。使用 NVIDIA Nemotron Nano 2 的子词 tokenizer。
- **连续时间轴上的静音建模**。解码器跨会话时间持续运行，没有 agent 文本时产生静音，而不是停机等待。
- **显式中断控制 token**。中断以控制 token 注入；`Force Silence` 打开时，推理会在与中断 token 对齐的位置**注入一帧固定的静音音频 token**，把生成流确定性地推向静音。该静音帧的取法是：用 codec 编码一段较长的纯静音，取出现频次最高的那个 31-token codec 帧。
- **不重置 KV cache**。中断后模型继续在同一 KV 上生成下一轮，论文用 AI-CER 只统计后续 assistant 轮的方式验证了恢复能力。
- 架构规模：全模型 977M 参数 = 778M 基于 Gemma 3 的流式 TTS 模块 + 199M codec 模型。输出 22kHz 波形，帧率 12.5Hz，31 个 codebook，**每个音频 token 帧对应 80ms 音频**。

> 源内命名不一致，本报告不代为取舍：对照模型的名字是 `Qwen3-TTS-12Hz`（12Hz），而论文正文两处均写“同样工作在 12.5 Hz”。两个值都记录。

训练配置（用于估计迁移代价）：第一阶段仅用单轮 TTS 数据预训练 1.4M 步、**8 张 A100**；第二阶段用完整混合数据微调 200k 步、**32 张 H100**。微调用 Lhotse 动态分桶，每批约 240 秒音频；数据配比为单轮 TTS 40%、词与专名列表 15%、真实对话 10%、合成全双工对话 35%。优化器 AdamW，学习率 4×10⁻⁵。

### 论文结果

**时延（表 4，RTX A6000，vLLM-Omni 框架，同一测量协议下重测对照模型）**：

| 模型 | 并发 | 参数量 | 声学 ITL (ms) | Codec (ms) | 合计 (ms) |
|---|---:|---:|---:|---:|---:|
| Qwen3-TTS-12Hz | 1 | 1.7B | 15.44 | 4.90 | 20.34 |
| **VoiceChat-TTS** | **1** | **977M** | **7.16** | **2.46** | **9.62** |
| Qwen3-TTS-12Hz | 4 | 1.7B | 17.09 | 4.99 | 22.08 |
| VoiceChat-TTS | 4 | 977M | 12.35 | 2.89 | 15.24 |

论文定义：ITL 是收到一次流式文本更新后产出**一帧**声学 token 的墙钟时间；因为工作在 12.5Hz，每帧等于 80ms 音频，所以 ITL 低于 80ms 即快于实时。论文同时说明，Qwen3-TTS 技术报告自己的时延数字是在作者内部 vLLM 引擎、“单个典型计算资源”上测的，**没有给出可直接比较的 GPU 配置**，所以作者在同一台 A6000 上重测以获得可控对照。

**中断（表 3，基于 Full-Duplex-Bench User Interruption 子集构造的 FDB-timed 受控中断基准，Silero VAD 计时，10 次随机运行的均值）**：

| Force Silence | IOR@320ms (%) ↑ | 平均停止时延 (ms) ↓ | Leakage@1s (ms) ↓ | AI-CER (%) ↓ |
|---|---:|---:|---:|---:|
| False | 96.8 | 228.3 | 169.1 | 0.255 |
| True | **100.0** | **89.9** | **55.8** | **0.095** |

论文自评：即使不做确定性强制，模型也已学到较强的中断响应（96.8%）；强制静音带来更严格的中断合规并大幅减少中断后的残留语音。论文明确声明**这不应被解读为官方 FDB 分数**，只借用了 FDB 的用户音频与中断时刻标注。

**质量（表 1，未见说话人用 LibriTTS test-clean 全集）——这里有必须原样保留的不利结果**：

| 模型 | 类型 | 轮次 | CER(%) ↓ | WER(%) ↓ | SECS ↑ | Squim-MOS ↑ |
|---|---|---:|---:|---:|---:|---:|
| Ground Truth | — | — | 0.48 | 1.40 | 0.830 | 4.457 |
| Chatterbox-TTS | 离线 | — | 0.45 | 1.24 | 0.887 | 4.27 |
| Audio Flamingo 3-Chat | 流式 | — | 2.85 | 4.51 | 0.761 | 3.60 |
| **Qwen3-TTS-12Hz-1.7B** | 流式 | — | **0.34** | **1.01** | **0.827** | **4.45** |
| VoiceChat-TTS | 流式 | 1 | 1.00 | 2.00 | 0.757 | 4.380 |
| VoiceChat-TTS | 流式 | 4 | 1.20 | 2.20 | **0.685** | 4.376 |

论文原话：“常规 TTS 系统，例如 Chatterbox-TTS 与 Qwen3-TTS，取得了最好的单轮可懂度、说话人相似度与预测整体语音质量。” 即 **VoiceChat-TTS 在质量上并非最优**，它换来的是连续双工行为。另一条作者主动写出的退化：未见说话人的 SECS 随轮次由 1 轮的 0.757 降到 4 轮的 0.685，作者称长上下文说话人一致性是未来工作。相对最接近的架构基线 Audio Flamingo 3-Chat，单轮 WER 由 4.51% 降到 2.00%，Squim-MOS 由 3.60 升到 4.38。

另有一组模块化证据（表 2）：保持 PersonaPlex 预测的文本 token 流与时序不变、只把语音解码器换成 VoiceChat-TTS，在 FDB 的 Smooth Turn Taking 子集上 CER 由 4.06% 降到 2.05%、WER 由 5.00% 降到 2.42%、Squim-MOS 由 4.094 升到 4.292。论文说明 CER/WER 是在**完整未裁剪**的 assistant 波形上算的，因此本该静音的区间里任何可懂语音都会计入插入错误。

### 对 StreamingModelHarness 的帮助

分三层，且第三层是否定性的。

**第一层，它是项目首音频瓶颈方向上第一个制品齐全的一手对象。** 项目首音频 P50 为 2,918ms（A）／3,491ms（B），而首文本最快只要 131ms，瓶颈明确在 Edge TTS。此前 TTS 方向的候选 `omnivoice-local-tts-16step-v1` 一直卡在 Luna-TTS 项目页无代码、无权重、无许可证且页内自相矛盾（1.28s 块 vs 0.36s 块）的状态上，其 41.6ms 至今不可用于跨系统比较。VoiceChat-TTS 的不同之处在于**代码与权重都能逐文件核到**，并且它的时延是在**并发 1** 下测的，与项目 batch=1 的口径一致，不需要做并发折算。

**第二层，`Force Silence` 是可以脱离模型单独借用的机制。** 项目的 `playback-boundary-ledger-offline-v1` 与 `tool-hold-message-first-audio-v1` 都缺一个明确的“收尾动作”定义。VoiceChat-TTS 给出的形式非常具体：把收尾实现成**在中断对齐位置确定性地写入一个预先算好的静音帧**，而不是让声学流自然衰减；且写入后不重置 KV，模型仍能正常产出下一轮。它给出的收益量级是停止时延 228.3→89.9ms、1 秒泄漏 169.1→55.8ms。编辑判断：项目可以在自己的播报链路上实现同构的“确定性收尾写入”，**但这两个数字不能借用为项目的预期值**——它们是在英文 FDB 音频上、用该模型自己的 codec 帧测的。

**第三层，它现在还不能替换 Edge TTS，有两条硬阻断。**

- **只有英文。** 训练与评测语料是 LibriTTS、Fisher 及内部英文对话，全文没有任何中文数据或中文指标。项目是中文车控，跨语种泛化未知。
- **TTS 子模块没有独立发布。** 本轮逐文件核实：`nvidia/NVIDIA-NemotronLabs-VoiceChat-11B` 仓库里只有**一个** `model.safetensors`，大小 **44,382,749,892 字节**；论文所述的 977M TTS 模块以 `config.json` 中 `model/speech_generation/*` 子树的形式存在（`codec_config.num_quantizers = 31`、`data.frame_length = 0.08`、`target_sample_rate = 22050`，与论文完全对应），但**没有单独的权重文件**。HF 上以 `VoiceChat-TTS`、`voicechat_tts` 检索均返回 0 条，不存在独立的 TTS 模型页。也就是说，要用这个 TTS，当前只能先取回约 44.4GB 的整包。

**可执行动作（本轮不新增 H20 候选，只新增一条只读前置任务）**：对 `NVIDIA-NeMo/Speech` 中 `duplex_ear_tts.py`、`nemotron_voicechat.py` 与 `examples/speechlm2/conf/nemotron_voicechat.yaml` 做只读审阅，回答两个可判定的问题——(a) 977M TTS 子模块能否在不加载 11B 主干的前提下单独实例化与推理；(b) 其 codec 与项目现有音频链路的采样率（22,050Hz vs 项目 16kHz）如何对接。这两个问题有明确答案之前，不进入单变量替换实验。

### 局限与开放状态

论文公开：arXiv:2608.13831 v1，2026-08-13T23:37:07Z 提交。本轮复核无 v2。PDF 的 `/URI` 注解共 10 条，其中 1 条是 arXiv 自身的自链接，其余为参考文献与两条脚注链接（NeMo Speech、VoiceChat-11B），**没有额外的项目页或 demo 页**。

代码公开：**是**。`NVIDIA-NeMo/Speech`，创建于 2019-08-05T20:16:42Z（远早于本论文，是 NeMo 主仓库而非本文专属仓库），最后推送 2026-08-16T23:03:45Z，18,144 star，**Apache-2.0**（API `license` 字段直接给出 Apache-2.0，与常见的 `NOASSERTION` 陷阱不同）。递归树 2,882 条目，未截断。命中 `voicechat|duplex` 的路径共 31 个，含 `nemo/collections/speechlm2/models/duplex_ear_tts.py`、`nemotron_voicechat.py`、`examples/speechlm2/conf/nemotron_voicechat.yaml` 及对应测试。**论文对自身仓库的描述经逐行核对成立**：`duplex_ear_tts.py` 第 1193 行为 `if self.cfg.get('inference_force_speech_silence_on_eos', True):`，`get_codec_silence_frame()` 的实现是编码 `10 * target_sample_rate` 长度的零波形、用 `Counter` 对每帧的 token 组合计数并取 `most_common(1)`，与论文“编码一段较长的纯静音并选取出现频次最高的 31-token codec 帧”逐字对应。

权重公开：**是，但只作为 11B 整包的一部分**。`nvidia/NVIDIA-NemotronLabs-VoiceChat-11B`，创建 2026-07-29T17:46:48Z，`lastModified` **2026-08-11T09:46:06Z**——**早于本论文 08-13 的投稿时刻**，即本论文发表并未伴随任何新的权重上传。非门控，1,536 次下载，390 likes，`base_model` 为 `nvidia/NVIDIA-Nemotron-Nano-9B-v2`。文件清单 18 项，唯一权重文件为 44,382,749,892 字节的 `model.safetensors`。**没有独立的 TTS-only 权重。**

许可证：**代码与权重分属两套，不可混用**。代码 Apache-2.0（NeMo 仓库）；权重 **OpenMDW-1.1**（HF `cardData.license` 与 `license:openmdw-1.1` 标签一致，仓库内另有 `LICENSE` 文件 2,704 字节）。这与既有记忆里“代码许可证不传递给权重”的规律一致。本轮**未逐条阅读 OpenMDW-1.1 全文**，其具体义务记为未知，项目若要实际使用必须先读该许可证正文。

数据公开：**否**。训练用到 Fisher 语料与“小规模内部对话数据集”，后者未公开；合成双工对话由 NeMo Data Designer 生成，未随论文发布。

其余明确边界：

- **全英文**，无中文、无车内实录、无多通道、无 AEC。
- **ITL 不是首包时延。** 论文在引言里讨论 TTFA，但**结果部分报的是 ITL（稳态每帧时延），全文没有报告自己的 TTFA/首包数字**。项目最关心的首音频时延因此**在本论文中记为未知**，9.62ms 不得当作首包时延引用。
- 时延测量硬件为 **RTX A6000**，不是项目的 H20；未给出 H20 或 CPU 数字。
- 质量并非最优：Qwen3-TTS-12Hz-1.7B 在 CER/WER/SECS/Squim-MOS 四项上均优于它（0.34/1.01/0.827/4.45 对 1.00/2.00/0.757/4.380）。**“更快”与“更好”在这里是分离的**，项目若做替换必须同时报两侧。
- 未见说话人的说话人相似度随轮次单调下降（0.757→0.685，1→4 轮），作者已承认。
- Squim-MOS 是**非侵入式预测质量估计**，论文明确说“作为非侵入式质量估计而不是自然度的直接度量”使用，不是主观 MOS。
- **本轮没有运行该仓库任何代码**，全部结论来自 GitHub/HF 元数据 API 与 `raw.githubusercontent.com` 上的源码、配置文本。

## 2. DeaMoE：把“小批量解码”的下界钉在 batch 4（反面判据）

论文（arXiv v1，2026-08-14T15:25:32Z 提交）：https://arxiv.org/abs/2608.14385

### 它是什么

一个 MoE **架构**改动：把专家分组为若干“部门（department）”，同部门专家共享大部分参数、各自只保留少量私有参数，并配一个两阶段路由（先路由到部门、部门内再分配到专家），目的是减少解码时反复加载专家权重的访存量。作者来自中国科学技术大学等。

本节把它写成反面判据，因为它对项目的价值**不在于能不能用，而在于它精确定义了一个此前被含混使用的词**。

### 核心方法

- 专家按“专业领域”分组进部门，同部门共享公共骨干参数，每个专家仅保留少量私有参数以体现差异。
- 两阶段路由：先把所有 token 路由到对应部门，部门一次性统一处理收集到的全部 token；再由部门把 token 分给具体专家做差异化处理。目的是提升单个解码步内跨 token 的权重复用。
- 实现上集成进 vLLM v0.13.0，用 Triton 实现自定义算子，复用 vLLM 原生 fused MoE kernel 所用的 Grouped GEMM 原语，只改 token 索引逻辑。

### 论文结果

- 摘要：相对 vanilla MoE，每步加载权重最多减少 **50.9%**；预训练 7B 模型在 A40 上端到端 TPOT 最高 **1.33×**；DeepSeek-V3 在 A40 与 H100 的微基准峰值分别为 **2.00×** 与 **1.97×**。
- 端到端（图 3，单张 **NVIDIA A40**，vLLM，连续请求流）：加速比 **1.02×–1.33×**，随 batch 非单调变化。逐点标注为 batch 4→1.02×、8→1.14×、16→1.26×、24→1.32×、32→**1.33×**、40→1.30×、48→1.26×、64→1.20×、96→1.13×、128→1.03×。论文原话：“加速比随 batch 从 4 增长到 32 而上升（峰值 1.33×），随后逐渐下降，在 batch 128 时接近 1.03×。”
- 吞吐口径：TPOT 预算 30ms 时基线与 DeaMoE 分别支撑 batch 64 与 96（1.50× 吞吐）；20ms 预算时为 24 与 44（1.83× 吞吐）。
- 模型质量：自行预训练 7.3B 的 DeaMoE 与等预算标准 MoE，各用 **110B token**（RedPajama-v1）。困惑度 PTB 61.91 vs 64.33、WikiText-103 20.31 vs 20.18、WikiText-2 20.33 vs 20.43，论文结论是质量持平。
- 微基准 batch 范围同样为 **4 到 128**。

### 对 StreamingModelHarness 的帮助

**它的帮助是让项目少做一次评估，并给一个既有候选加一条旁证。**

**第一，它给出了一条可直接复用的筛子。** 项目此前的反模式家族判据是“收益需要大 batch 或长上下文”（OasisKV、vToken）。DeaMoE 不属于这一类——它明确面向低时延交互、明确反对大 batch。但它**最小的评测点就是 batch 4**，且加速比在 batch 4 处仅 1.02×、并随 batch 减小而下降。项目是 batch=1，按其自身趋势外推收益低于 1.02×。编辑判断：应把家族判据从“需要大 batch”扩写为**“最小可评测 batch 是否为 1；若不是，且收益随 batch 减小而单调下降，则直接否”**——这条更普适，且能一次性挡掉整类以“small-batch”为卖点却从 batch 4 起步的工作。

**第二，即使 batch 口径吻合，它对项目也是不可用的。** 两条独立原因：(a) 它是**预训练期架构改动**，论文自述采取“pre-training-first”路线、从零预训练；项目用的是既有的 Qwen3-Omni 权重，无法套用。(b) 它攻击的瓶颈是**访存带宽**（专家权重加载），论文原话是“有效降低了每个解码步的访存流量，从而缓解 A40 GPU 上的带宽瓶颈”；而项目已实测自身瓶颈**不是带宽**——解码速度不随 KV 长度变化，实测 110ms 是按 config 实算的带宽下限（5090 口径约 3.0ms）的约 36 倍，根因是每 token 约 816 次 GPU→CPU 同步。**同一个词“解码慢”，两边指的是完全不同的东西。**

**第三，它为 `decode-loop-cudagraph-static-kv-v1` 提供了第三条独立旁证，且是最强的一种形式。** 论文在实现小节写：“我们也为 DeaMoE 与所有基线启用了 vLLM 的 CUDA Graph 执行。由于解码本身对时延敏感且通常运行在小批量区间，采用 CUDA Graph 是业界降低 kernel 启动开销的通行做法。” 编辑判断：这不是把图捕获当成一项优化来宣传，而是把它当作**基线也必须具备的标配**，否则对照不公平。这抬高了该候选的先验。但**顺序不变**：项目仍须先落地无同步 `index_select`+`bmm` 快路径，再把整环图捕获作为第二个独立变量单独测量，两步不得合并成一次结论。

一条**仅供参考、不可直接相减**的量级观察：DeaMoE 的基线（未经其架构改动的 vanilla MoE，vLLM，已开 CUDA Graph）在 A40 上于 20–30ms 的 TPOT 预算内可支撑 batch 24–64。项目在更强的 H20 上、batch=1、却是 110ms/token。四条免责必须同时写：模型不同（7.3B vs Qwen3-Omni 30B-A3B）、框架不同（vLLM vs HuggingFace）、硬件不同（A40 vs H20）、batch 不同（≥4 vs 1）。因此这**不能**作为“110ms 高出多少倍”的定量依据，只能作为一条与项目自身根因分析方向一致的定性佐证——即 110ms 更像实现问题而非硬件问题。

### 局限与开放状态

论文公开：arXiv:2608.14385 v1，2026-08-14T15:25:32Z。PDF 元数据中 `/License` 为 `http://creativecommons.org/licenses/by-nc-nd/4.0/`——**这是论文本身的许可证，与任何代码或权重无关**。

代码公开：**否**。全文唯一的 GitHub URL 是参考文献里的 `https://github.com/meituan-longcat/LongCat-Flash-Omni`（引用 Meituan 的技术报告，**不是本文仓库**）。61 条 `/URI` 注解逐条查看后全部为参考文献的 DOI/arXiv/ACL 链接。论文正文只有一句承诺：“训练用的代码**将在之后开源**，包含前向与反向。”即**当前无代码、无权重、无 demo，且承诺只覆盖训练代码**。

权重公开：**否**。自行预训练的 7.3B 模型未发布。

数据公开：训练语料为公开的 RedPajama-v1，但训练产物未发布。

其余边界：

- 硬件为 **NVIDIA A40** 与 **H100**，均非项目的 H20。
- **batch=1 全文未评测**，最小评测点为 4。
- DeepSeek-V3 与 Qwen3-235B-A22B 的 2.00×/1.74×/1.97× 均来自**逐层微基准**而非端到端，且论文自己写明“对大专家模型更显著”。项目的模型规模与之不同。
- 需要从零预训练，迁移代价与项目无关但必须写明，否则容易被误读为可后接的推理侧优化。

## 3. DuplexWorld 数据集正式发布：发布的是场景规格，不是评测能力

论文（arXiv v1，2026-08-11T09:34:39Z 提交，已在 2026-08-11 报告中展开）：https://arxiv.org/abs/2608.10716

官方数据集：https://huggingface.co/datasets/CentificAIResearch/Duplex-World

项目页：https://duplexworld.github.io

### 它是什么

不是新论文，而是 08-11 已记录的 DuplexWorld（Centific + UMD）**首次发布其基准数据**。按去重纪律，这与 RT-SEMamba 于 08-14 发布制品的情形同类：**制品发布不是新论文**，不计入新增 arXiv ID。

### 核心方法（数据集侧，本轮现场核实）

`CentificAIResearch/Duplex-World`，创建于 **2026-08-15T16:50:32Z**，`lastModified` 2026-08-15T16:50:33Z，即一次性上传后未再改动。非门控，30 次下载，0 likes，标注 `arxiv:2608.10716`、`voice-agents`、`full-duplex`、`speech-to-speech`、`benchmark`、`task-oriented-dialogue`。

文件共 10 项：`README.md`、`assets/hero.webp`、`data/all.jsonl` 以及六个领域文件。行数与字节数如下（本轮实测）：

| 文件 | 字节 | 行数 |
|---|---:|---:|
| `data/banking_payments.jsonl` | 29,923 | 3 |
| `data/healthcare_access.jsonl` | 13,232 | 2 |
| `data/insurance.jsonl` | 12,941 | 2 |
| `data/logistics_delivery.jsonl` | 6,595 | 1 |
| `data/pathfinding.jsonl` | 30,329 | 9 |
| `data/travel.jsonl` | 10,406 | 1 |
| **`data/all.jsonl`** | **103,426** | **18** |

两项可机器校验的自洽性检查均通过：六个领域文件的字节数之和恰为 103,426，与 `all.jsonl` **完全相等**；行数之和 3+2+2+1+9+1 = **18**，与 `all.jsonl` 相等。即 `all.jsonl` 就是六个领域文件的并集，没有额外内容。

单条记录不是一行短文本，而是一份完整场景规格（travel 的唯一一条为 10,406 字节），字段共 17 个：`id`、`domain`、`conversation_type`、`purpose`、`relevant_policies`、`notes`、`persona`、`reason_for_call`、`known_info`、`unknown_info`、`task_instructions`、`decision_tree`、`initial_state`、`evaluation_criteria`、`user_tools`、`required_documents`、`task_params`。其中 `decision_tree` 2,566 字节、`evaluation_criteria` 2,459 字节。

### 论文结果

本节不重复 08-11 已记录的论文数字。需要指出的是**论文里最有价值的那部分结论并没有随本次发布变得可复现**：08-11 记下的核心结论是“三个 harness 旋钮对 Pass@1 的影响与更换被测系统相当”，以及池化凭证 WER 在 G.711 8kHz 下为 17.1%。这些都需要 harness 与音频链路，而**本次发布的 10 个文件里没有任何音频文件，也没有任何评测代码**。

### 对 StreamingModelHarness 的帮助

只有一条，且是格式层面的。

项目的判定协议目前是自建的，缺少可对照的第三方写法。DuplexWorld 每条场景把 `decision_tree`（可达状态与分支）与 `evaluation_criteria`（判定标准）**分成两个独立字段**，且都以结构化文本给出。编辑判断：这与 08-14 记录的判定器构造效度问题（2608.12880：只翻转与行为无关的元数据就改变了 180 条中的 31 条判定）方向一致——把“系统应该怎么走”和“怎么算通过”在数据结构上分开，正是让判定器无法读到不该读的字段的前提。项目可以借鉴这个字段划分，去支撑既有的 `grader-treatment-invariance-audit` 建议项。

**不能做的事，必须写死**：18 条场景**不足以支撑任何准确率结论**，也不能并入项目的 452 条评测集做联合统计。它是英文的、面向客服与出行等场景的，与中文车控域不重合。本轮不因此新增候选。

### 局限与开放状态

论文公开：arXiv:2608.10716 v1，本轮复核仍无 v2。

数据公开：**是**，`CentificAIResearch/Duplex-World`，**CC-BY-4.0**（`cardData.license` 与 `license:cc-by-4.0` 标签一致），非门控。README 声明为 `size_categories: n<1K`、`language: en`——该声明技术上成立，但 18 条这个实际规模需要在任何引用中直接写出，只写 “n<1K” 会严重高估。

代码公开：**否**。数据集仓库内没有评测代码；08-11 至今未见 harness 仓库。

音频公开：**否**。全部为文本场景规格。

其余边界：英文；六个领域为 banking、insurance、travel、healthcare、logistics、Pathfinding，均非车内场景；`user_tools` 与 `required_documents` 两个字段在抽查的 travel 记录中为 `null`，即并非所有场景都填满了全部字段。

## 4. 索引回补的证据链：为什么 08-15 的“零新增”与本轮的 83 篇并不矛盾

### 它是什么

不是一项技术，而是**本轮窗口口径的证据本身**。因为 Radar 已连续第五轮未刷新，直接检索是唯一发现通道；而该通道在 08-15 返回全站零结果、在本轮返回一个完整的历史批次，这个转折必须留痕，否则下一轮无法判断“是回补”还是“上一轮漏检”。

### 核心方法

先按常规窗口 `submittedDate:[202608150230 TO 202608170230]` 跑 14 组查询（既有 13 组，加一组 08-16 记录的 `cs.HC OR cs.OS` 补漏），全部返回 0 条。随后执行既定的三级证伪：

1. **去掉日期约束的对照查询正常返回**，证明 API 与解析链路可用：`cat:cs.SD` 全站 `totalResults` **21,696**（08-15 记录为 21,686）、`cat:eess.AS` 22,178、`cat:cs.CL` 116,387。
2. **不限类别、按投稿时间倒序的全站查询**返回的最新一条是 `2608.14549`，投稿时间 **2026-08-14T17:59:59Z**（08-15 记录为 `2608.13561`、2026-08-13T17:59:58Z）。**这是与上一轮的关键差异：索引前进了整整一个批次。**
3. **全站窗口查询**：`submittedDate:[202608150230 TO 202608170230]` 的 `totalResults` = **0**；而 `submittedDate:[202608131800 TO 202608170230]` = **249**，`[202608140000 TO 202608170230]` = **178**。

逐类别最新条目复核，全部落在 2026-08-14T12:34Z–17:59Z 之间：cs.SD 与 eess.AS 均为 `2608.14516`（17:38:16Z）、cs.CL `2608.14509`（17:24:55Z）、cs.AI 与 cs.LG `2608.14539`（17:51:30Z）、cs.CV `2608.14546`（17:59:01Z）、cs.MM `2608.14260`（12:34:51Z）、cs.HC `2608.14513`（17:32:26Z）、cs.OS `2608.14376`（15:17:14Z）、cs.NI `2608.14386`、cs.DC `2608.14527`。

修订复核：`lastUpdatedDate:[202608150230 TO 202608170230]` 在 `cat:cs.SD`、`cat:eess.AS OR cat:cs.CL`、以及全站 `all:a` 三组上**均为 0 条**。

据此把窗口起点回拨到 2026-08-13T18:00Z，重跑 14 组查询，得到去重后 **83 篇**。

### 论文结果

不适用。83 篇的主题分布中与项目相关的部分已在第 1、2、5 节与第 6 节展开；其余为路径规划、UAV、音乐、深度伪造检测、无线通信等无关方向。

### 对 StreamingModelHarness 的帮助

两条，都是流程性的。

第一，**它证明了 08-15 那份“零新增”报告在当时是正确的，而不是漏检**。判据是全站最新投稿时刻：08-15 时为 2026-08-13T17:59:58Z，本轮为 2026-08-14T17:59:59Z。两者都是 arXiv 每日 14:00 ET 截稿边界（17:59:5x UTC）——即索引是**按整批推进**的，不是逐篇滴入。因此“某个窗口零结果”与“某个批次尚未入库”是同一现象的两种描述。

第二，**它确定了下一轮的窗口起点与一个待观察项**。本轮已覆盖至 2026-08-17T02:30Z，但 arXiv 实际最新投稿只到 2026-08-14T17:59:59Z。下一轮窗口起点应为 **2026-08-17T02:30Z**，同时必须再次检查全站最新投稿时刻：若仍停在 08-14T18:00Z，则 08-15/08-16 两个批次仍未入库，届时还会有一次更大的回补；**回补一律按投稿时间归属，不按发现时间**，否则本报告的窗口口径会与下一份冲突。

### 局限与开放状态

不适用。需说明的是：本节只证明 **arXiv API 在复核时刻返回的索引**中该窗口的内容为上述状态，不能证明作者在该时段没有投稿。`all:a` 作为“全站”代理查询依赖 arXiv 的检索实现，其 249 这个绝对值不宜当作该时段投稿总数的精确统计；本节用它只做**有/无**的判定与相对比较，逐类别最新条目的复核才是主要依据。

## 5. CoRun：固定形状解码同时买到确定性，这对项目的判定链路是一条新论据

论文（arXiv v1，2026-08-14T15:17:14Z 提交）：https://arxiv.org/abs/2608.14376

### 它是什么

一篇 `cs.OS` / `cs.PF` 的推理系统论文（南京大学 + 腾讯 Hunyuan）。它要解决的不是速度，而是**同样的 prompt、同样的采样参数与随机种子，LLM 推理却给出不同 token 序列**这一确定性问题。之所以纳入本报告，是因为它给项目在办候选 `decode-loop-cudagraph-static-kv-v1` 增加了一个此前完全没有考虑过的收益维度，并且顺带给出了一个关于项目所用模型族的具体数字。

按既有记忆的补漏规则，这篇是靠新增的 `cat:cs.HC OR cat:cs.OS` 第 14 组查询捞到的——它不在标准 13 组的覆盖范围内。

### 核心方法

- 论文把非确定性的主因归为**批次相关的 GPU 执行**：动态输入形状会改变 kernel 的 tiling 与浮点归约顺序。
- 既有做法是用 batch-invariant kernel，但论文测得这类 kernel“使时延增加 2 倍以上，并使服务吞吐下降最多 74%”。
- CoRun 的观察是：多数 kernel 虽然不是 batch-invariant，却是 **position-invariant**。因此改为**控制每个请求看到的执行形状**，再保留更快的 position-invariant kernel。
- 具体手段：**隔离 prefill + 固定形状的批量 decode**，decode 走**固定尺寸的 CUDA Graph**；decode 阶段关闭 attention Split-KV；对少数非 position-invariant 的算子使用确定性 collective；采样器与形状对齐。
- 论文把这套保证写成一份**端到端确定性契约**，并明确该契约“不声称跨硬件的可复现性”，只在固定硬件、软件栈、模型与张量并行度下成立。

### 论文结果

- 摘要：在 Qwen 与 DeepSeek 等不同架构上，CoRun 在保证确定性的同时，相对 batch-invariant 方案**吞吐提升 15–324%**，平均**首 token 时延降低 51.8%**、**每输出 token 时延降低 48.6%**。
- 图 1(b) 的端到端时延分解（三档对照）：Standard **19.4ms**、Batch-invariant **88.4ms**、CoRun **32.6ms**。
- 确定性验证用 vLLM 自带的 batch-invariance 测试：每个模型生成 32 条 prompt，先以 batch size 1 执行、再作为 batch 32 的一部分执行，temperature 0.6、seed 42、每条最多 8 个输出 token，比较完整 token 序列与每步采样 token 的 log 概率。
- **与项目直接相关的一条数字**（图 14b，对照方案 LLM-42 在 ShareGPT 上、64 token 验证窗口下的 rollback ratio，定义为被拒绝的候选 token 数除以在非确定性快路径上生成的 token 总数）：Qwen3-8B **17.94%**、Qwen3-32B **24.25%**、而 **Qwen3-30B-A3B（MoE）达到 45.41%，接近同等规模稠密模型 Qwen3-32B 的两倍**。论文给出的解释是：“一个很小的 router 扰动就可能改变被选中的专家，此后两条执行路径执行的是不同的计算，而不只是累积不同的低位比特。”

### 对 StreamingModelHarness 的帮助

**这篇的价值在于它把“图捕获”从一个纯时延议题，扩成一个同时关乎判定可信度的议题。**

**第一，它是 CUDA Graph 的第四条独立旁证，而且是唯一一条把固定形状与确定性绑在一起的。** 08-14 的 FlashDrive 把图捕获写成前置条件、vToken 把兼容性写成硬约束、本轮的 DeaMoE 把它当作基线标配，三者都只谈性能。CoRun 说明：**固定形状的 decode + 固定尺寸 CUDA Graph 同时是可复现输出的实现手段**。

**第二，这条与项目当前优先级第一的事项直接咬合。** 项目最高优先级是判定器处理不变性检查（源于 2608.12880：只翻转与行为无关的元数据就改变了 180 条中的 31 条判定）。CoRun 指出的是同一问题的更底层一层：**如果解码本身在数值上不可复现，那么“414 canary 零退化”这类判据就可能在测量噪声**。编辑判断：项目的 KV 长度逐步增长，形状**并非固定**，因此 tiling 与归约顺序在每一步都可能变化；`StaticCache` + 图捕获会把这一维度钉死。这给了 `decode-loop-cudagraph-static-kv-v1` 一个此前没有的第二收益，且这个收益**不依赖它在时延上是否成功**。

**第三，那个 45.41% 必须被非常谨慎地引用。** 项目基座是 Qwen3-Omni-30B-A3B，与论文测的 Qwen3-30B-A3B 同属 A3B MoE 族。论文给出的**机制性**结论——router 扰动会改变专家选择，使两条路径执行完全不同的计算而非仅仅低位比特差异——是架构层面的，对项目的 MoE 同样适用，这是可迁移的部分。但那个 **45.41% 本身不可迁移**：它是在 LLM_42 的投机-验证框架下、ShareGPT 语料、64 token 验证窗口、高并发条件下测得的“被拒绝候选 token 占比”，**不是**“单次 batch=1 推理有 45% 概率不可复现”。项目若要知道自己的数值稳定性，只能自测——而这恰好是一个零成本、不需要 GPU 申请的实验：**同一条音频、同一 seed、连续跑 N 次，逐 token 比较输出**。

**可执行动作（本轮新增建议项，不新增候选）**：在做任何判定器审计之前，先做一次**解码可复现性自测**——固定输入与 seed 重复运行，记录 token 序列是否逐次一致、以及首次分歧出现在第几个 token。若存在分歧，则所有既有的 ±1/±4 结论都需要重新评估其显著性；若不存在，则记为一条有价值的负结果并写进判定协议。这条实验的单变量清晰、可测、无需 GPU 申请，但它测的是**评测环境本身**而非被测系统，因此按既定规则留在报告而不进入候选。

### 局限与开放状态

论文公开：arXiv:2608.14376 v1，2026-08-14T15:17:14Z。

代码公开：**未知/否**。全文 29 个 URL 中与实现相关的是 `https://github.com/vllm-project/`（其依赖的 vLLM）与 `https://github.com/ise-uiuc/blazedit`（其使用的 Blazedit 工作负载），**均为他人仓库**；**没有发现 CoRun 自身的仓库链接**。实现方式论文写明为“我们在 vLLM 0.25.0 中实现 CoRun”，但未给出该实现的获取方式。

权重公开：不适用（不训练模型）。

**硬件：论文全文未声明 GPU 型号与数量**，本报告记为**未知**。这对一篇以吞吐与时延为主要指标的系统论文是一个显著缺口，其 19.4 / 88.4 / 32.6ms 与全部吞吐百分比因此**无法与项目的 H20 数字做任何量级对照**。

其余边界：

- 评测模型为 **Qwen3-235B-A22B、DeepSeek-V3、Hy3**，均远大于项目所用规模；Qwen3-30B-A3B 只出现在与 LLM-42 对照的 rollback ratio 图中。
- 场景是**多请求在线服务**：其核心机制“固定形状批量 decode”需要预先选定最大并发度，论文自陈“设得太高会在流量少时补入大量 padding token 并降低 decode 吞吐”。**项目是 batch=1 的单用户场景**，padding 调度这一层对项目不适用；可迁移的只有“固定形状 + 图捕获 ⇒ 可复现”这条性质本身。
- 确定性契约明确**不跨硬件、不跨软件栈、不跨张量并行度**成立。
- 它没有报告任何与语音、流式音频或工具调用相关的内容。

## 6. 本窗口其余论文的筛选结论

83 篇中，除第 1、2 节已展开的两篇外，以下若干篇按项目问题做了筛选。**全部为 v1，全部提交于 2026-08-13T18:00Z 之后**，此前从未出现在任何一份报告或 `data/items.json` 中（已逐个 arXiv ID grep 确认 0 条命中）。

| arXiv | 标题 | 与项目的关系 | 处置 |
|---|---|---|---|
| 2608.13959 | Repair, Not Improvement: Decomposing Constrained Decoding in Tool-Call Abstention | 对应项目“闲聊仍注入工具”与候选集约束这一长期问题 | **强负面证据，见 6.1** |
| 2608.14150 | Leading-Silence Augmentation and Multi-Stage Synthetic Supervision for the Second MLC-SLM Challenge | 前导静音增强；**本窗口唯一直接微调项目基座 Qwen3-Omni-30B-A3B 的论文** | 跟踪，见 6.1 |
| 2608.13717 | StreamHear: Domain-Adapted Pseudo-Labeling for Semi-Supervised Streaming Speech Recognition | 流式 ASR 的域适配，对应车内域外泛化；含一条“置信度过滤从未有帮助”的负结果 | 跟踪，见 6.1 |
| 2608.14191 | KV Cache Compression Through the Lens of Transform Coding | 明确面向长上下文内存瓶颈 | **不采纳；反模式家族第五个成员，见 6.1** |
| 2608.14333 | Beyond Capacity: Scalable MoE LLM Inference via High-Bandwidth Flash with Direct GPU and HBM Paths | 与 DeaMoE 同为 MoE 访存方向；**注意它确实评测了 B=1**，否掉它的是容量墙前提与不存在的硬件 | **不采纳，见 6.1** |
| 2608.14036 | Demystifying Agent Skills: Why They Work-Until They Don't | 其检索池规模劣化数字对项目 300 工具的 RAG 是设计告警 | 跟踪，见 6.1 |
| 2608.14376 | CoRun: Padding is Simple and Efficient for Deterministic LLM Inference | cs.OS/cs.PF，固定形状解码与确定性，直接给图捕获候选增加第二个收益维度 | **已展开，见第 5 节** |
| 2608.13926 | Never the Number: Structural Abstention for AI Systems Whose Answers Are Consumed as Fact | 与既有 abstention gate 记录同族 | 仅跟踪 |
| 2608.14015 / 2608.14035 / 2608.14380 / 2608.14047 | MedClaw（外科视频 harness）、自动芯片设计编排、AgentRewind、VLA 工具使用 | 均为其它领域的 agent 编排，与座舱工具通道无共享判据 | 不采纳 |
| 2608.14249 / 2608.13817 / 2608.14287 / 2608.14516 / 2608.13957 / 2608.13842 / 2608.13724 | 音频深度伪造检测、UAV 声学检测、歌声分离、音乐即兴与语料 | 与全双工座舱无关 | 不采纳 |
| 2608.14097 | Ambisonics Encoding of Room Impulse Responses using a Device-Agnostic Diffusion Model | 空间听觉方向，但为房间脉冲响应编码，非车内、非实时前端 | 仅跟踪 |
| 2608.14029 | S2Dialog: Multimodal Dialogue Retrieval with Semantic and Acoustic-Style Modeling | 对话检索，非流式、非工具通道 | 不采纳 |

> **口径声明**：下面六篇已完成**全文与制品的逐条核实**（PDF 正文 + `/Annots` 超链接注解 + 裸 URL 扫描 + GitHub/HF API + `LICENSE` 原文），证据等级与第 1、2、5 节相同。核实过程中没有克隆或执行任何仓库。

> **本批最重要的一条横向事实**：**六篇没有任何一篇提供作者自有的代码、模型或数据仓库。** 六份 PDF 中出现的每一个 GitHub / Hugging Face 链接都指向第三方软件或第三方权重（`NVIDIA-NeMo/Speech`、`scale-snu/LLMSimulator`、`anthropics/skills`、`harbor-framework/harbor`、`huggingface/open-r1`、`xai-org/grok-1`）。其中 **2608.13959 与 2608.14036 在正文里反复提到“已发布的仓库 / 已发布的制品”，却全文没有给出任何 URL、DOI 或仓库名**；**2608.14150 的 `/URI` 注解只有 1 条，就是 arXiv 自身的自链接**，即零条作者提供的链接。另一条横向事实：**六篇没有一篇提到 CUDA Graph、图捕获、主机同步或动态形状**，全批最接近的一句是 2608.14333 的“张量并行的跨 GPU 集合通信与同步开销”，那是 GPU 间通信，与项目定位的每层 GPU→CPU 同步是不同现象。**项目的图捕获硬筛子不受本批影响。**

### 6.1 逐篇筛选说明（全文核实）

**2608.13959 — Repair, Not Improvement（cs.CL，24 页，单作者 Janghoon Lee / Redrob，PDF `/License` CC-BY-4.0）。对项目“闲聊仍注入工具”是一条强负面证据，本批唯一可采纳的一篇。**

它测的正是项目一直想做但没做的事：用受约束解码（对工具名做 enum）去改善**工具弃权**。设计的关键是**在一条逐字节相同的 prompt 上设三个条件**，从而把约束语法的两件事分开：`contract-free`（无约束，256 token 预算）、`contract-short`（无约束但在第一行截停）、`constrained`（第一行做 enum 约束）。由此得到 Δ_mask（enum 的作用）、Δ_length（截停的作用）与 Δ_total。题目取自 MetaTool Task2，一次性不放回抽 200 个位置，得到 200 条弃权题与 200 条需调用工具题；韩语版**只翻译用户 query**，工具名、描述、指令与 few-shot 全部保持逐字节相同，使语言对比在同一条目内成立。区间为 10,000 次自助重采样的百分位区间。

条件：Qwen3-0.6B / 1.7B / 4B，温度 0，五个服务端种子，vLLM 0.27.1 的 `structured_outputs`，**NVIDIA L4（张数未声明）**，注册分析共 36,000 次请求。

Table 8 的 Δ_total（准确率百分点，弃权 / 工具选择 / 合并）：0.6B EN **−29.5** / +17.0 / −6.2；0.6B KO −0.5 / +20.0 / +9.8；1.7B EN −26.5 / +11.2 / −7.7；1.7B KO −24.5 / +10.1 / −7.2；4B EN −8.8 / +59.6 / +25.4；4B KO +1.5 / +62.7 / +32.1；均值 −14.7 / +30.1 / +7.7。

**一处必须精确表述、否则会被读成矛盾的地方**：摘要写“在六个单元格中的四个为负**且区间不含零**”，而 §4.1 写弃权列“在**五个**中为负”。两者并不冲突——按符号有五个为负（−29.5、−0.5、−26.5、−24.5、−8.8），但其中 0.6B KO 是 −0.5 [−9.5, +8.0]，区间含零，故区间不含零的只有四个。引用时必须带上“区间不含零”这个限定。

其余关键事实：**工具选择列在六个单元格中全部为正**；被修复的 698 条弃权中 545 条原本没有可读答案、0 条是评分器拒绝给出的判断（该组数字在摘要、贡献与 §1 三处完全一致）；掩码概率质量存在一个断点 **0.480 [0.320, 0.520]**，低于它则对逐条惩罚没有预测力，高于它则预测很强；作者写明“两条预注册的语言学主张均告失败”。

**全文没有任何时延数字**——没有毫秒、没有 tokens/s、没有吞吐；**全文没有出现过 “batch” 一词**，batch size 记为**未声明**；上下文长度也未以 token 计。

**开放状态是一条强负面**：36 条 `/URI` 注解中 1 条是 arXiv 自链接、其余 35 条全是参考文献；正文裸 URL 0 条；`github`、`zenodo`、`osf.io` 在正文中出现 0 次。而论文**反复声称存在一个已发布的 harness**（“PREREG.md 在已发布的仓库中即为注册文档”“发布内容包含 harness、逐请求原始记录与所有表图的生成器，`make verify` 会在任何已提交产物不一致时失败”），甚至列出了 make 目标，**却没有给出任何 URL、DOI 或仓库名**；GitHub 代码检索返回 0。可复现性状态：**不可核实**。

编辑判断：项目当前走的是候选工具集晚注入 + 确定性执行器校验（已拦住 45% 的失配大头），不是解码期语法约束；这篇支持维持现状，且比项目自己测过的 candidate-ID 强制覆盖负结果更具体。**最有价值的可迁移物是那个三条件设计**：停止 token 与 enum 符号相反且可以相消（−20.0 与 +19.5 合成 −0.5），因此任何只做两条件 A/B 的受约束解码实验，都会把停止 token 的代价错记到语法头上。另一条直接的告警：工具选择准确率六个单元格全升、弃权四个单元格显著降——**不得把工具召回的提升读成判断力的提升**。边界：模型是 Qwen3 0.6B–4B 而非 Qwen3-Omni-30B-A3B，语言是英语与韩语而非中文，硬件是 L4 而非 H20，全文零时延数字，且无代码可用。

**2608.14150 — Leading-Silence Augmentation（cs.CL，Ant Group + 电子科技大学，5 页，PDF `/License` CC-BY-NC-SA-4.0）。本窗口唯一直接微调项目基座同族模型的论文。**

Task 1 用 LoRA（rank 32、缩放 128、dropout 0.05、5 epoch）微调 VibeVoice-ASR-7B，保留原单遍架构与输出序列化，加两件事：**随机前导静音裁剪**——对时长 T、首个标注段起点 s₁ 的录音，采样 δ ∈ [0, s₁] 删去 [0, δ)，并把**全部**标注时间戳同步平移；以及参数 EMA，λ = 0.99，每步更新。Task 2 是四阶段合成监督后 LoRA 微调 **Qwen3-Omni-30B-A3B-Instruct**：Gemini 2.5 Pro 从训练音频生成约 210k 候选问答；**静音音频过滤**——用 Qwen2.5-Omni-7B 在把语音替换为静音的条件下作答，**仍答对的丢弃、变答错的保留**，留下约 67k 即候选池的 32%；分布匹配增强再产出约 60k；合计约 127k。推理时关闭采样、温度 0.0。

Table 1（Task 1 累积消融，官方 tcpMER 协议，越低越好）：LoRA 基线 **18.30** → 加随机前导静音裁剪 **17.27** → 加 EMA **16.73**。作者换算为绝对降 1.03 点（相对 5.6%）、再降 0.54 点（相对 3.1%），合计绝对降 1.57 点（相对 8.6%）。

Table 2（Task 2 评测集准确率 %）：基线（直答）**78.0** → 加候选生成 **81.0** → 加静音音频过滤 **83.0** → 加分布匹配增强 **85.0** → 加带标签直答 **86.0**。

**一处真实的源内不一致，按纪律原样保留、不代为取舍**：摘要与引言均写“联合应用分布匹配增强与带标签直答把准确率**由 83.0% 提到 86.0%**”；而 §3.3 写“准确率**由 78.0% 提升到 86.0%**”；Table 2 把 78.0% 一行标为 **“Baseline (direct answering)”**、把 83.0% 一行标为 **“+ silent-audio filtering”**；结论则称 83.0% 是**“reasoning-then-answer 基线”**。即同一份论文对“基线是哪一行、83.0% 代表什么”给出了互相冲突的三种说法。本报告三种都记录。

批大小已声明但只是训练侧：Task 1 每设备 1 × 梯度累积 4 × 16 个数据并行 worker = 全局 64；Task 2 微批 4、全局 64、16 个加速设备。**推理批大小未声明。** **硬件未声明**——全文只说“16 个加速设备”，**没有给出任何 GPU 型号或厂商**。**全文没有任何时延数字**，一个能单遍处理最长 60 分钟录音的系统未报告任何推理耗时。制品：`/URI` 注解**仅 1 条**且是 arXiv 自链接，正文裸 URL 2 条均为第三方（挑战赛主页与**主办方**的 Task 1 baseline `alanshaoTT/MLC-SLM-2nd-Task1-Baseline`，该仓库**无任何 LICENSE 文件**，API 报 `null`）。**无作者仓库、无权重发布。** 另一条需记为未知：论文所微调的 `VibeVoice-ASR-7B` 在 HF 上**无法解析**（`microsoft/VibeVoice-ASR-7B` 返回 401）；存在一个 `microsoft/VibeVoice-ASR`（MIT），但论文未给 HF ID，**无法确认是否同一 checkpoint**。

编辑判断：Task 1 与项目不同构（60 分钟未切分多语种会话的说话人日志与归属转写，tcpMER 口径；项目是车内短单说话人指令、micro CER 6.39%），前导静音裁剪针对的是长录音时间原点的变化，不是项目的失效模式。**真正值得记的是 Task 2 的静音音频过滤**：把音频换成静音后仍答对的样本一律丢弃，只留下会因此答错的——这是一个廉价、与模型无关的反事实过滤器，用来保证样本确实依赖音频而非文本先验，且它正是施加在**项目同族的 Qwen3-Omni-30B-A3B-Instruct** 上。项目若日后要构造任何“确实依赖音频”的评测子集，这个过滤器可直接借用。边界写死：全部收益是挑战赛集上的选择题准确率，走的是**微调**路线而项目不做微调，其 86.0% 与项目 80.49% 的严格工具召回**没有任何可比性**。

**2608.13717 — StreamHear（cs.CL/eess.AS，Capital One，5 页，PDF `/License` 为 arXiv 非独占分发许可，即非开放许可）。**

配方只有三步且**每步只跑一次**（无迭代、无 EMA 教师）：在有标注域内集上微调离线全上下文 transducer 教师 → 用它对无标注集做一次贪心解码得伪标签 → 在混合集上微调 cache-aware 流式学生。另加一个**先验正则化的动态规划重对齐**：把参考转写与逐块 ASR 假设摊平成两个词流并各自标注所属块，用 Needleman–Wunsch 对齐并对块索引位移加惩罚 λ|c_j−c_i|，匹配上的参考词继承其假设伙伴的块归属。

Table 2（WER %，**5 个种子**的均值，学生 = Nemotron-Speech-Streaming-EN-0.6B，**RC=1 即 80ms 块 + 80ms 右上下文 = 160ms 算法时延**，LC=70，经英文专用文本归一化后）——四个数据集的 test / unlabeled：

| 系统 | Earnings-21 | Earnings-22 | SPGISpeech | BankCall(test) |
|---|---|---|---|---|
| 教师 | 6.51 / 8.35 | 12.25 / 10.62 | 3.97 / 4.02 | 12.71 |
| 微调教师 | 6.03 / 7.68 | 11.24 / 9.55 | 2.20 / 2.15 | 8.85 |
| 学生 | 8.27 / 10.36 | 13.55 / 12.31 | 3.27 / 3.18 | 13.60 |
| 微调学生 | 7.19 / 9.62 | 12.46 / 10.81 | 2.77 / 2.70 | 10.68 |
| **StreamHear** | **6.63 / 7.77** | **11.76 / 9.56** | **2.59 / 2.26** | **9.80** |

即相对微调学生在有标注 test 上好 0.18–0.88 个百分点、在留出的无标注split 上好 0.44–1.85 个百分点；相对微调教师的差距被压到 test 至多 0.95 点、unlabeled 至多 0.11 点（Earnings-22 unlabeled 9.56% 对 9.55%，基本持平）。重对齐把块文本的对齐质量 WER 由 17.46→8.01（−9.45）与 20.54→10.97（−9.57），而块数变化不超过 0.4%。

**最有价值的是一条负结果**：置信度 top-K 过滤（K = 25/50/75/100%）在两个数据集两个 split 上**随 K 增大单调变好，K=100% 始终最优**，论文原话是“丢弃低置信度伪标签从未有帮助”。即这套配方里**过滤机制应当整个省掉**。

**时延必须精确表述**：论文有时延数字，但**全部是算法时延**，定义为 (1+RC)×80ms，是块配置的算术后果；**全文没有任何实测墙钟毫秒、没有 RTF、没有帧率或 token 速率**。训练硬件 **4 张 NVIDIA A100-SXM-40GB**（有效批 64、每卡批 4、梯度累积 4）；**推理批大小未声明**。制品：`/URI` 注解 2 条，其一是 arXiv 自链接，其二是脚注指向 `NVIDIA-NeMo/Speech`（第三方框架，Apache-2.0）；**无作者仓库、无 checkpoint、无代码发布声明**，且四个数据集之一 BankCall 是 Capital One 专有数据不公开——**主结果原则上不可复现**。

编辑判断：问题设定与项目同构（流式 ASR 在域外表现下降、有标注域内数据昂贵而无标注音频充足），配方骨架可迁移，`K=100%` 那条负结果尤其省事。但英文专用（含缩写展开、填充词删除的归一化管线）、学生是 NeMo 的 cache-aware FastConformer-RNN-T 而项目用 faster-whisper（其重对齐所解的块边界词位问题项目没有）、且项目瓶颈根本不在 ASR。维持跟踪。

**2608.14191 — AATC / KV Cache Transform Coding（cs.LG/cs.CL/eess.SP，19 页，CC-BY-4.0）。按既有判据不采纳，且全文核实后理由更硬。**

方法（注意力感知失真的可加分解 + 变换编码 + 反向注水法比特分配；离线每模型一次标定，在线每解码步每层投影、量化、重建）。约 **5.82×** 压缩下在 18 个格子上均与 FP16 在评测噪声内不可区分。

三条全文核实后新增的硬事实：其一，**全文没有出现过 “batch” 一词**，batch size 记为未声明；其二，**全文没有声明任何硬件**，GPU / NVIDIA / A100 / H100 均不出现；其三，也是最决定性的一条，§VII 局限原文：“在实证方面，我们的实现**没有做系统级优化，特别是缺少专门的 CUDA 实现**，而这在该方法能用于生产之前是必需的。”配合**全文零墙钟毫秒、零 tokens/s、零吞吐、零加速比**——唯一的绝对量是 1.07 GB → 184 MB 这一对静态显存数字，而**其对应的序列长度未声明**——可以确定：这篇的“压缩”是字节计数论证，从未被证明能转化为任何运行系统中的速度或实际显存收益。

另记两处源内不一致：摘要与贡献写“约 **5.8×**”而 §VI-A 与 Table II 一致写 **5.82×**；附录 F 在 LongBench 条目下写“最大新生成 token 随任务在 **32 到 512** 之间”，却又在 MMLU-Pro 条目下出现一句“LongBench 使用贪心解码、**最多 32 个新 token**，并把输入截断到 **4096 token**”——两者不能同真，且后者的 4096 截断在别处从未出现，而同一附录称 LongBench 文档长度约 1k 到 34k token。两说并记。

编辑判断：不采纳，三条各自独立成立。反模式判据（其立论明确是长上下文，评测全为 RULER 4k–32k 与 LongBench，而项目 KV≈2500）；**它会让项目的问题变得更糟**——Algorithm 1 的在线路径在**每个解码步、每一层**追加投影、量化与重建，正是加在项目想要图捕获的那个循环里，而项目的 110ms 根因是每 token 约 816 次主机同步而非带宽；以及无仓库、作者自述无 CUDA 实现、十九页零墙钟数字。

**2608.14333 — Beyond Capacity / DASH（cs.AR，KAIST，13+ 页）。不采纳，但本轮必须更正一处我方此前的表述。**

**更正**：此前把它与“需要大 batch”一类并列是不准确的。全文核实后：**它明确评测了 B=1**——图 9(a) 的批扫描包含 B ∈ {1, 4, 16, 64}，二十组序列长度扫描**全部在 B=1**，前瞻研究也在 B=1。因此**批大小判据抓不住它**。真正否掉它的是**容量墙前提**：论文立论是被调研 MoE 权重占用 281 GB–1.5 TB、专家权重占总权重 94.1–98.8%、单模型权重即超过 H100 的 80GB HBM，因而必须从闪存流式取专家。项目的 Qwen3-Omni-30B-A3B 在单张 96GB H20 上放得下，从不触碰这堵墙。

另两条独立否决：**硬件不存在**（DASH 是基于高带宽闪存 HBF 的仿真架构，论文自陈“公开的 HBF 资料未给出子阵列级并行度”“由于尚无具体 HBF 规格，我们把读时延 t_R 从 1 扫到 32µs”）；**全部主结果是仿真输出**，真实硬件只用于算子测速（一张 **NVIDIA H100 PCIe 80GB** 与一颗 Intel Xeon Platinum 8452Y）。

**一处必须记录的源内不一致**：摘要写“可达 **1.94×** 更高吞吐与 **1.90×** 端到端加速”，而 **1.94× 这个值在全文正文中一次都没有再出现、也没有对应的条件说明**；同时“1.90×”被归给两个不同条件——引言说是 **B=4** 下五模型几何均值、§VI-B 说是**跨全部五模型与全部四个批大小**的几何均值；端到端一侧则有三种说法：摘要的 1.90× 对应 47.4% 降幅、引言写 44.8%、§VI-B 写 42.2%。四个值全部原样记录。

**一条值得单独留存的机制**（与项目的既有负结果直接相关）：DASH 把路由 logit 重写为 P = α(P_base + P_attn)，其中 α = 1/RMS(·) 是**同一 token 所有专家共享的正标量**，因而**只改变量级、不改变排序**，于是 top-k 可以在残差相加与 RMSNorm **之前**精确算出。这是**非投机的**——与项目已实测为负优化的投机预取性质不同，它不猜、结果与常规 top-k 完全一致。但它在论文里的收益是掩盖 NAND 读启动时延，B=1、t_R=3µs 时 Qwen3 的 TPOT 仅降 3.86%；项目没有闪存读要掩盖，预期收益约等于零。制品：无作者仓库；其基座仿真器 `scale-snu/LLMSimulator` 是 MIT 但版权属**首尔大学**、非本文 KAIST 作者，且**不含任何 DASH 代码**（4 次提交，最后一次 2025-10-14）。

**2608.14036 — Demystifying Agent Skills（cs.AI，Princeton/UCSD/Stanford/USC/JHU）。不采纳，但保留一条检索侧告警。**

规模与方法核实无误：8,135 条试验记录规范化，240 条开放编码记录中保留 238 个有效唯一标签，最终 12 种模式，528 组配对三元组（raw / workflow / skill 同任务）。技能相对 Workflow Memory 高 **6.06 个百分点**（61.9% 对 55.9%，自助 95% 区间 [+0.76, +11.36]）；程序性锚定占 65.7%、显式知识注入仅 4.5%。

**对项目最有用的仍是 RQ4 的检索脱钩**，且全文给出的数字比摘要更完整：候选池由 5 增到 100 时，实际使用精度由 **29.6% 降到 3.3%**，而下游成功率几乎不动（**36.4% → 39.3%**）；离线诊断中 top-1 嵌入精度由 88.3% 降到 76.9%、显式代理选择由 70.0% 降到 63.7%；**Arm 3 在 k=100 时召回仍有 54.3–73.6%，而精度只有 0.7–8.1%**。可混淆池更糟：Arm 1 在相似池上 top-1 精度由 70.5% 降到 53.4%，而随机池是 97.7%→84.1%、不相似池 96.6%→93.2%。

编辑判断：这组数字说明**top-1 检索精度是下游成功率的很差代理**，且高召回可以与极低精度并存——这正对应项目“RAG Recall@24 98.78% 但上下文引力未根治”的处境，也与项目自己“工具清单压缩是实测负优化”的结论方向一致（代理对大而含噪的候选池的容忍度，比检索指标暗示的要高）。但不采纳：设定是多轮编码 agent 在 Docker 中跑 Terminal-Bench，用的是闭源前沿 API，全文**无时延、无 batch size、无 GPU**，唯一的成本量是 token——而 Table 13 显示 Skill 相对 Workflow Memory 每任务多花 **+93.8K 输入 token**，对项目约 2500 token 的 KV 预算是灾难性的。制品同样不可核实：正文提到“已发布的制品”却无 URL；其所引的 `anthropics/skills` **没有开源许可证**（无顶层 LICENSE，逐 skill 的 `LICENSE.txt` 是“保留所有权利”并指向 Anthropic 服务条款）。

## 7. Hugging Face 窗口扫描：148 条新条目，一手发布 3 条

### 它是什么

对 2026-08-15T02:30Z 至 2026-08-17T02:30Z 的 HF 补充发现通道扫描。因 arXiv 最近两天为零，这一段是唯一可能有新增的通道。

### 论文结果

以 14 个关键词（speech / audio / voice / tts / asr / duplex / omni / denoise / enhancement / echo / vad / diarization / multimodal / streaming）按创建时间检索 models 与 datasets，筛出 `createdAt` 落在窗口内的条目，去重后 **148 条**。逐条查看后，**一手发布 3 条**：

| 条目 | 创建时间(UTC) | 性质 | 许可证 | 处置 |
|---|---|---|---|---|
| `CentificAIResearch/Duplex-World` | 2026-08-15T16:50:32Z | **一手**：2608.10716 的官方数据集 | CC-BY-4.0 | 见第 3 节 |
| `X-Zhang/EchoMask` + `EchoMask-Inference-Data` | 2026-08-16T06:33:52Z | **一手**：arXiv:2504.09209 官方 checkpoint | `other`（未细读） | 见下方名称碰撞说明 |
| `laion/moss-voice-identity-loras` + `moss-voice-identity-repairs` | 2026-08-15T12:11:35Z | **一手**：LAION 的音色身份 LoRA 与修复集 | Apache-2.0 | 仅跟踪 |

其余 145 条为第三方微调、镜像、量化或无关条目。既有记忆里的“第三方量化镜像抬高计数”模式本轮再次成立，且出现了与项目直接相关的一组：`VikramPal/Qwen3-Omni-30B-A3B-*` 共 8 条（S2S 与 SLURP 两组，各含 QLoRA / bf16 / DynQuant-3bit / DynQuant-4bit），全部带 `base_model:Qwen/Qwen3-Omni-30B-A3B-Instruct` 系标签、下载与点赞均为 0——**它们是项目所用基座的第三方衍生，不是官方发布，不构成任何可引用证据**。另有 `mradermacher/lemura-arabic-asr-qwen3-GGUF`（量化）、`vttts/OmniVoice-Onnx-bidir` 与 `BinhNguyen94/omnivoice`（OmniVoice 第三方导出，官方仍是 `k2-fsa/OmniVoice`）、`RabbitDaisuke/tsukuyomichan-omnivoice-full-finetune-onnx` 等。批量单作者上传方面，`uam-wmi-asr-eval-labs/*` 波兰语课程集 8 条、`ghananlpcommunity/*` 4 条、`lingamvamshikrishnareddy/*` 4 条、`Devsyril/mms-tts-*-sherpa-onnx` 10 条、`anonymous4486/audio-flamingo-3-hf_*` 6 条——**按发布计而不是按行计，这些合计 32 条只相当于个位数的实际发布**。

**一处必须记录的名称碰撞。** `X-Zhang/EchoMask` 是本窗口内唯一命中 `echo` 关键词的一手发布，但它与回声消除**毫无关系**：其 README 与标签指向 arXiv:2504.09209《EchoMask: Speech-Queried Attention-based Mask Modeling for Holistic Co-Speech Motion Generation》（ACM Multimedia 2025，类别 cs.GR/cs.CV/cs.SD），是**语音驱动的协同手势/整体动作生成**，产物为 SMPL-X 动作与 BEAT2 数据集相关的 npz/权重压缩包。附带一条支持既有复核纪律的事实：该论文 **2025-04-12 投稿、2026-08-16 才发布官方 checkpoint**，制品滞后约 16 个月。

### 对 StreamingModelHarness 的帮助

除第 3 节的 Duplex-World 外无技术帮助。流程上确认两条：其一，`echo` 这类短关键词在 HF 上的假阳性率高，判定一手/二手之外还需判定**主题**，不能只看作者是否为论文单位；其二，项目基座 Qwen3-Omni 的第三方衍生已开始批量出现，后续扫描需把 `base_model:*Qwen3-Omni*` 明确列入噪声过滤规则，避免被误读为官方更新。

### 局限与开放状态

按关键词 + 创建时间排序的检索不保证召回完整，可能漏掉命名与关键词无关的一手发布——既有记忆里 PosterBench 就是只能通过跟踪仓库的新提交发现的。本节只声明“在上述 14 个关键词的检索结果内一手发布为 3 条”，不声明全站无其它一手发布。`X-Zhang/EchoMask` 的 `license: other` 未细读正文，其具体条款记为未知（本报告不采纳该项，故未展开）。

## 8. 持续跟踪项的现场复核汇总

全部于 2026-08-17 通过 GitHub API、Hugging Face API 与 arXiv API 复核。

| 跟踪项 | 上次记录 | 2026-08-17 复核 | 结论 |
|---|---|---|---|
| RT-SEMamba `RoyChao19477/RT-SEMamba` | 08-15/08-16：`0c9e8c7c42`，size 14368，1 star | `pushed_at` 仍 **2026-08-14T05:16:18Z**，size 仍 **14368**，仍 **1 star** | 连续第三轮字节级不变；`streaming-se-frontend-rtsemamba-kd1-v1` 的前置条件与处置全部不变 |
| AutoDesign `Yaxin9Luo/AutoDesign` | 08-16：724 条目，`pushed_at` 2026-08-15T08:24:24Z，47 star | `pushed_at` **2026-08-16T12:19:13Z**，但 `refs` 仅有 `refs/heads/main`=`cd5b2bd997`（**2026-08-15T08:24:24Z**）与两个 PR head；递归树仍 **724 条目**，42 个 json/jsonl/csv 集合未变，62 star，releases 0 / tags 0 | **默认分支未前进**。附录 A.5 承诺的逐案例分数存档与人评决策记录**连续第三次复核仍不存在**；引用 r=0.34 仍须附“原始人评数据不可第三方复核” |
| PACE 的 `CodeForZzc/GCM-Bench` | 08-16：最后推送 2026-08-11T06:29:47Z | 最后推送仍 **2026-08-11T06:29:47Z**（连续第六天不变），Apache-2.0 | 不变；`playback-boundary-ledger-offline-v1` 只借用数据格式与判定协议，不受影响；PACE 中间件本体仍未公开 |
| `ASLP-lab/SemBridge` | 08-16：3 条目占位仓库，无 LICENSE | `pushed_at` 仍 **2026-08-10T05:51:29Z**，size 160，18 star，`license` 仍为 null；`updated_at` 为 2026-08-15T14:02:18Z（元数据活动） | 不变；仍为占位。再次印证 `updated_at` 不可用于判断代码变更 |
| `ASLP-lab/Smart-Glass-Challenge` | 08-16：26KB、9 条目、无 LICENSE | `pushed_at` 仍 **2026-06-16T11:59:12Z**，size 仍 **26**，20 star，无 LICENSE | 不变；语料仍需注册获取 |
| FlashDrive `z-lab/flashdrive` | 08-16：MIT、最后推送 2026-07-06 | 仍 **MIT**，最后推送仍 **2026-07-06T00:54:50Z**，60 star | 不变；README 徽章仍早于论文的状态未改 |
| CASA `aalto-speech/CASA` | 08-16：Apache-2.0，无权重文件 | `pushed_at` 仍 **2026-08-13T09:59:25Z**，Apache-2.0，0 star | 不变；仍为纯代码，语料需另行取得 |
| NVIDIA VoiceChat 11B 权重 | 08-12：OpenMDW-1.1 | `lastModified` **2026-08-11T09:46:06Z**，非门控，1,536 次下载、390 likes | **本论文（08-13 投稿）未伴随任何新上传**；见第 1 节 |
| 20 个跟踪 arXiv ID 的版本 | 08-16：全部 v1 | 2608.07631 / 08097 / 08794 / 10716 / 10878 / 11593 / 11627 / 12034 / 12082 / 12099 / 12703 / 12715 / 12789 / 12880 / 12932 / 13101 / 13228 / 13263 / 13416 / 13560 **全部 `updated` 等于 `published`，无一有 v2** | 无修订版，无改名，无重复来源。该清单投稿于 08-07 至 08-13，至今无任何 v2，继续按节奏复核 |

## 今日建议路线

### 第一阶段（本轮就能做，不需要 GPU）

1. **完成快照“下一步”第 2 条的 clean/20dB/音色分桶归因。** 连续第三轮列为首位。它同时是 `streaming-se-frontend-rtsemamba-kd1-v1` 的硬前提与其 `expected_fixes` 上界的唯一来源，本轮无任何新证据改变这一点。
2. **判定器处理不变性检查仍然高于一切**，包括高于本轮新增的 TTS 只读任务。本轮唯一相关的新增素材是 DuplexWorld 把 `decision_tree` 与 `evaluation_criteria` 分成两个独立字段这一写法，可用作项目“判定器不得读取哪些字段”排除清单的参照。
3. **在判定器审计之前，先做一次解码可复现性自测（本轮新增，零成本、不需要 GPU 申请）。** 固定同一条音频与同一 seed，连续运行 N 次，逐 token 比较输出，记录是否逐次一致以及首次分歧出现在第几个 token。理由见第 5 节：CoRun 指出 MoE 的 router 扰动会把微小数值差异放大成完全不同的执行路径，而项目的 KV 长度逐步增长、形状并非固定。**这条实验测的是评测环境本身而不是被测系统**，因此不进入候选，但它的结论会决定既有 ±1/±4 结论是否需要重新评估显著性。若逐次一致，则记为一条有价值的负结果并写进判定协议。
4. **对 VoiceChat-TTS 做只读代码与制品审阅。** 目标是回答两个可判定的问题：977M TTS 子模块能否脱离 11B 主干单独实例化推理；22,050Hz 输出与项目 16kHz 链路如何对接。在这两个问题有明确答案之前不进入任何替换实验，也不登记候选。
5. **继续既有的三条零成本流程项**（ASR 段计时口径对齐、留出片与预先声明门槛、以及上条的判定器审计）。

### 第二阶段（在第一阶段结论落地之后）

1. **无同步 fast path 先行，图捕获后行**（`decode-loop-cudagraph-static-kv-v1`）。本轮新增了第三条独立旁证（DeaMoE 把 CUDA Graph 作为基线标配启用），**先验提高但顺序不变**。
2. **前端单变量离线对照**（`streaming-se-frontend-rtsemamba-kd1-v1`）。分桶归因与只读代码审阅都完成后再启动，验收口径与 08-15 记录完全一致，本轮无修改。
3. **TTS 方向暂不动。** `omnivoice-local-tts-16step-v1` 维持仅离线、`h20_request_allowed=false`。VoiceChat-TTS 是否能成为它的替代或补充，取决于第一阶段第 3 条的审阅结论。

### 第三阶段（只跟踪，不启动）

1. AEC/回声消除：连续第五轮无一手证据，继续跟踪，不写任何结论或预期。`EchoMask` 已确认与该方向无关，不再纳入。
2. Luna-TTS：按 30 天节奏复核，页内矛盾（1.28s 块 vs 0.36s 块）未澄清前其 41.6ms 不可用于跨系统比较。
3. PACE 中间件、X2-Turn 代码权重、SemBridge、Smart-Glass-Challenge、CASA：均无变化，继续按节奏复核。
4. 本轮新增的跟踪项：`2608.14376` CoRun（确定性推理与固定形状，与图捕获前置条件同向，下一轮优先阅读）、`2608.13926` Never the Number（abstention 家族）、`2608.14097` Ambisonics RIR 编码（空间听觉）、`laion/moss-voice-identity-loras`、`zeroweight-ai/ZeroBench-TTS`（CC-BY-NC-4.0，非商用）。
5. **arXiv 索引状态**：下一轮窗口起点为 2026-08-17T02:30Z，同时必须复核全站最新投稿时刻。若仍停在 2026-08-14T17:59:59Z，说明 08-15/08-16 两个批次仍未入库，会有一次更大的回补，一律按投稿时间归属。

## 风险提醒

1. **不要把 VoiceChat-TTS 的 9.62ms 当成首包时延。** 它是 **ITL**（稳态下产出一帧声学 token 的墙钟时间，每帧对应 80ms 音频）。论文在引言里讨论 TTFA，但**结果部分从未报告自己的 TTFA/首包数字**，该值记为未知。项目的瓶颈恰恰是首音频（P50 2,918ms），两者不是同一个量。
2. **不要把 VoiceChat-TTS 读成“可以替换 Edge TTS 了”。** 两条硬阻断：全文只有英文评测，无任何中文数据或指标；977M 的 TTS 子模块**没有独立权重文件**，被封在 44,382,749,892 字节的单一 `model.safetensors` 内，HF 上不存在独立的 TTS 模型页。
3. **“更快”不等于“更好”。** 同一张表里 Qwen3-TTS-12Hz-1.7B 在 CER 0.34%、WER 1.01%、SECS 0.827、Squim-MOS 4.45 四项上全面优于 VoiceChat-TTS 的 1.00%/2.00%/0.757/4.380，论文自己也写“常规 TTS 系统取得了最好的单轮可懂度、说话人相似度与预测整体语音质量”。任何替换实验必须同时报时延与质量两侧。另注意 Squim-MOS 是非侵入式**预测**质量，不是主观 MOS。
4. **代码许可证不传递给权重，这次是 Apache-2.0 与 OpenMDW-1.1。** NeMo 代码为 Apache-2.0，VoiceChat-11B 权重为 OpenMDW-1.1。**本轮未逐条阅读 OpenMDW-1.1 正文**，其义务记为未知；实际使用前必须先读许可证正文。
5. **DeaMoE 的“small-batch”下界是 4，不是 1。** 加速比 batch 4 时仅 1.02×、batch 32 峰值 1.33×、batch 128 回落 1.03×，且随 batch 减小而下降；项目是 batch=1，按其自身趋势外推收益低于 1.02×。此外它是**预训练期架构改动**且攻击的是**带宽**瓶颈，而项目实测瓶颈不是带宽（速度不随 KV 长度变化，与带宽下限差约 36 倍，根因是每 token 约 816 次主机同步）。三条理由各自独立成立。
6. **不要用 DeaMoE 的基线 TPOT 反推项目的 110ms 高出多少倍。** 模型、框架、硬件、batch 四项全部不同（7.3B vs 30B-A3B、vLLM vs HuggingFace、A40 vs H20、≥4 vs 1）。它只能作为方向一致的定性佐证。
7. **不要把 CoRun 的 45.41% 读成“项目的推理有 45% 不可复现”。** 它是 rollback ratio——在 LLM-42 的投机-验证框架下、ShareGPT 语料、64 token 验证窗口、高并发条件下，被拒绝的候选 token 占非确定性快路径生成 token 总数的比例。可迁移的是**机制**（router 扰动改变专家选择，两条路径此后执行不同计算），不是这个数值。项目要知道自己的数值稳定性只能自测。另注意 CoRun **全文未声明 GPU 型号与数量**，其 19.4/88.4/32.6ms 与全部吞吐百分比无法与项目的 H20 数字做任何量级对照；其“固定形状批量 decode”需预选最大并发度，属多请求在线服务机制，对 batch=1 的项目不适用，可迁移的只有“固定形状 + 图捕获 ⇒ 可复现”这条性质。
8. **DuplexWorld 数据集只有 18 条，且不含音频与 harness 代码。** README 的 `n<1K` 技术上成立但会严重高估规模；论文里最有价值的 harness 敏感性结论**并未因本次发布变得可复现**。不得并入项目 452 条评测集，不得据此做任何准确率结论。
9. **`pushed_at` 也会在没有可达新提交时前进。** AutoDesign 的 `pushed_at` 为 2026-08-16T12:19:13Z，而 `refs` 中只有 `main`（停在 2026-08-15T08:24:24Z）与两个 PR head，递归树仍 724 条目。判断代码是否真的变化，必须查到 `git/refs` 与提交列表这一层，`updated_at` 与 `pushed_at` 都不足够。
10. **`EchoMask` 不是回声消除。** 它是协同语音手势/动作生成（arXiv:2504.09209，ACM MM 2025）。AEC 连续第五轮零一手证据这一结论不受影响；本窗口 `speech enhancement OR noise suppression OR echo cancellation OR beamforming` 一组返回的 5 篇全部是无线通信波束成形与冷原子物理，无一为声学 AEC。
11. **不要把 `VikramPal/Qwen3-Omni-30B-A3B-*` 这 8 条读成基座更新。** 它们是第三方对项目所用基座的量化与微调衍生，下载与点赞均为 0，不构成任何可引用证据。
12. **第 6.1 节六篇里有两处“论文声称已发布制品但全文没有 URL”，引用其数字时必须带上这一点。** 2608.13959 反复提到“已发布的仓库”“PREREG.md”“`make verify`”，2608.14036 提到“已发布的制品”，两者**全文都没有给出任何 URL、DOI 或仓库名**，GitHub 检索也返回 0；2608.14150 的 `/URI` 注解只有 arXiv 自链接一条。这批六篇**没有一篇提供作者自有的代码、模型或数据仓库**，出现的每个 GitHub/HF 链接都指向第三方。另需记住三条源内不一致：2608.14150 对“基线是哪一行”有三种互相冲突的说法（摘要/引言 83.0%→86.0%、§3.3 78.0%→86.0%、Table 2 标 78.0% 为直答基线而结论称 83.0% 为 reasoning-then-answer 基线）；2608.14333 摘要的 **1.94× 在正文中一次都没有出现**，且端到端降幅有 47.4%/44.8%/42.2% 三种说法；2608.14191 附录 F 对 LongBench 的最大新生成 token 给出 32 与 32–512 两种互斥表述。
13. **2608.14333 确实评测了 batch 1，不要把它归进“需要大 batch”那一类。** 它的批扫描含 B ∈ {1,4,16,64}，二十组序列长度扫描全在 B=1。否掉它的是**容量墙前提**（专家权重 281 GB–1.5 TB、超过 HBM 容量）、**尚不存在的硬件**与**全部主结果为仿真输出**这三条。写错拒绝理由会让下一轮重复评估。
14. **窗口口径**：本报告的新论文全部来自 2026-08-14 的积压批次；2026-08-15T02:30Z 之后 arXiv 全站仍为零投稿零修订。下一轮不得把本报告已覆盖的 83 篇重新计数，也不得因“最近两天没有论文”而拔高弱相关工作。
15. **不重启已有独立实验**：参数化本体、云监督、SoulX 只消费 result manifest；candidate-ID/CMTF、conditional top-2 verifier、投机预取、工具清单压缩、主模型自判停的负结果不重跑。
16. **禁止事项复述**：本轮**没有运行任何论文仓库代码**，全部开放状态结论只来自 GitHub/HF 元数据 API、`LICENSE`/README/配置文件与源码文本、以及 arXiv API 与 PDF 正文；不调用 Kimi/Moonshot 或 `scripts/enhance_kimi.py`；不修改 `StreamingModelHarness`。
