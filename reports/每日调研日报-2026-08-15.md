# 每日调研日报｜2026-08-15

证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、最近 H20 A/B 2026-08-03，本轮无新提交与新基准）。**Radar 连续第四轮未刷新**：`data/items.json` 的 `generated` 仍是 2026-08-12 09:30，新增 0 条。**本轮 arXiv 全站零新增**：检索窗口 2026-08-14 02:30 UTC 至 2026-08-15 02:30 UTC（与前一份报告严格衔接、不重叠）内，13 组查询全部返回 0 篇，`submittedDate:[202608131800 TO 202608150230]` 全站 totalResults 为 0，不限类别的最新投稿是 2026-08-13T17:59:58Z 的 2608.13561，修订同样 0 条——即**上一份报告已把 arXiv 现有内容全部覆盖完毕**。因此今日**无新论文**，全部新增来自对既有跟踪项开放状态的现场复核（复核时刻 2026-08-15，晚于 02:30 检索截止）。另：main 上此前只有到 2026-08-10 的报告，08-11 至 08-14 四轮已在本轮先恢复到主线，故对比基线为 2026-08-14。

方向：降噪前端 — RT-SEMamba 由空仓库变为代码加权重（当前只有 clean/20dB 两档合成音频且未分桶归因，08-13 定下“时延有界 + 状态固定 + 1 帧进 1 帧出”的前端硬要求后，唯一形状合格的它却连续两轮是空仓库、只能停在纸面；2026-08-14T05:16:09Z 该仓库推送完整实现，由 size 0 变为 155 条目约 19.6MB，LICENSE 为 MIT，`ckpts/` 内直接带 KD8 教师（11,115,496 字节）与 KD1 学生（4,235,024 字节）两份权重，并给出论文没有的实现级参数——`n_fft=400 / win=400 / hop=100` 即 25ms 窗、6.25ms 跳、160 帧/秒，三个固定大小流式状态（帧缓冲、`conv_state`、`ssm_state`），`set_streaming(True)` 与 `reset_streaming_state()` 接口，`--streaming_mode realtime`（一帧一次前向）与 `chunked` 两种模式，PCS 后处理默认关闭；README 还补了完整深度扫描 1 层无 KD 3.06 / 2 层 3.19 / 4 层 3.29 / 8 层教师 3.32 / 8→1 学生 3.18，蒸馏弥合教师-学生差距 46.2%/19.2%/63.6%/34.5%；可以尝试的是**先做快照既定的 clean/20dB/音色分桶归因**，拿到“452 条里有多少条失败可归因于噪声致错”这个数作为上界，再以“是否在 ASR 前插入 KD1 流式前端”为唯一变量做离线对照——顺序不能倒，否则噪声致错与理解致错同时变化无法归因；三条边界写死：全仓库**没有任何下游 ASR CER 或工具正确率**，“PESQ 涨了车控更准”仍是零证据推测；仍是英文单通道 VCTK-DEMAND、**无中文无车内无 AEC**；权重在 git 里而非 HF，`releases`/`tags` 均为空，做实验必须记 commit SHA `0c9e8c7c42`）

方向：前端时延记账 — 25ms 不是它给项目增加的时延（当前若要把前端放进首动作 700–1094ms P50 的预算，容易直接引用论文的“25ms 算法时延”一个数；实际要分三个数报——25ms 是由 `win_size: 400` 与 `center=False` 界定的模型算法时延且设备无关，`inference_KD1.sh` 的默认管线块长是 `--chunk_size 1600` 即 **100ms**，实测每帧墙钟耗时再另算；可以尝试在离线对照里就把这三项分列成三行，混成一个数会低估约 100ms 量级，直接吃掉首动作预算的十分之一以上）

方向：前端部署成本 — CPU/Apple Silicon 推理路径改变了“抢卡”这条反对意见（当前 08-13 与 08-14 两轮都写过同一条反对：RTF 0.11–0.29 是 RTX 5090 上测的，前端要和主模型抢同一张卡；仓库公开后 README 支持矩阵把 `inference.py` 与 `rt_enhance.py` 在 CUDA/MPS/CPU 三列都标为可用，并附 `mamba_install_mps`——跳过 CUDA 编译、用纯 PyTorch 参考实现替换 selective scan、layernorm 与 state-update kernel，而 KD1 只有 1.05M 参数、20.56 GMACs/s；可以尝试把 CPU/MPS 实测 RTF 作为独立测量项，但**不能引用论文的 5090 数字**——README 明确声明不对其它设备作保证，CPU 与 MPS 的 RTF 目前记为**未知**，且 MPS 上因固定约 32ms 的每次调用开销，`rt_enhance.py` 改为按 100ms 块整批前向而非逐帧循环，**逐帧性质在该后端实际被放弃**，08-13 定的“1 帧进 1 帧出”在 MPS 上不再成立）

方向：许可证与可复现性 — 这份 MIT 带强制条款（当前项目对开源件的记录习惯是记一个 SPDX 标识；RT-SEMamba 的 `LICENSE` 正文虽是 MIT，却附两条强制条款——所有副本必须包含指明本软件基于 MP-SENet 与 CMGAN 的致谢声明，源码或二进制再分发必须保留该致谢，版权行另列 YXLU-0102 与 Ruizhe Cao 的部分版权；可以尝试在候选记录里把“许可证”一栏从单个标识改成“标识 + 附加义务”两栏；另记两条：GitHub API 的 `license` 字段本周**第三次**报 `NOASSERTION` 而实际文件是明确许可证——前两次是 AutoDesign=MIT 与 rana-m-ahmed=Apache-2.0，一律以文件为准；该仓库 `releases` 与 `tags` 均为空数组，没有版本化发布，`HEAD` 随时可变，不记 commit SHA 的实验不可复现）

方向：检索基础设施 — arXiv 索引在本窗口内完全静止（当前 Radar 已连续第四轮未刷新，直接 arXiv 检索是唯一发现通道，该通道零结果时必须能区分“没有论文”与“没有检索到”；本轮做了三级证伪——去掉日期约束的对照查询正常返回（cs.SD 全站 21,686 条）证明链路正常，不限类别按投稿时间倒序的最新一条是 2608.13561 投稿于 2026-08-13T17:59:58Z，直接查 `submittedDate:[202608131800 TO 202608150230]` 全站 totalResults 为 0，七个类别逐一复核最新条目全部不晚于 08-13 18:00Z，`lastUpdatedDate` 落在窗口内的修订也是 0 条；可以尝试把这套三级证伪固定成零结果时的必做动作；同时记住**这与 08-14 的“窗口很薄”性质不同**——那次是 cs.SD 3 篇需要判断筛选倾向，这次是一篇都没有不需要任何判断，下一轮窗口起点为 2026-08-15 02:30Z，并留意索引恢复后 08-13 18:00Z 之后的积压投稿会一次性出现，届时按投稿时间而非发现时间归属）

方向：复核纪律 — “一次 404 或空仓库不足以永久判定无制品”两天内第二次被证实（当前对论文当日无制品的判定容易变成永久结论；两次方向一致的反例已经出现——Luna-TTS 项目页 08-13 为 404、08-14 为 200，RT-SEMamba 仓库 08-13 与 08-14 两轮均为 size 0 而 08-14T05:16Z 发布完整实现，说明论文投稿到制品上线之间存在数天量级的滞后；可以尝试把“论文当日判为无制品”的跟踪项按固定节奏复核写进流程，本轮第 6 节即为该纪律的一次完整执行——13 个跟踪 arXiv ID 全部确认仍为 v1 无修订，`CodeForZzc/GCM-Bench` 最后推送仍 2026-08-11T06:29:47Z、`ASLP-lab/SemBridge` 仍 3 条目占位、`ASLP-lab/Smart-Glass-Challenge` 仍 26KB 9 条目无 LICENSE、`z-lab/flashdrive` 仍 18 条目 MIT 最后推送 2026-07-06、`Alpamayo-1.5-10B` 仍 `license:other`，全部无变化）

方向：TTS 候选 — Luna-TTS 页内矛盾仍未修正（当前 `omnivoice-local-tts-16step-v1` 的处置依据里包含“Luna 项目页首块时长自相矛盾，其 41.6ms 不可用于跨系统首包比较”这一条；2026-08-15 复核该页仍 HTTP 200，去标签后的正文里两处文本依然同时存在——摘要写 Realtime 按 **1.28s** 块自回归，推理表脚注写“首音频 = 引擎调用到第一个解码完成的 **0.36s** 块”，整页 `github.com` 出现 0 次、`huggingface.co` 0 次、`license` 0 次；可以尝试的只有继续按 30 天节奏复核，候选处置完全不变——仅离线、`h20_request_allowed=false`，验收仍拆首包/整句/取消残留三段且样本必须含长句与不规则文本）

方向：制品核对 — AutoDesign 论文承诺的评测存档仍未出现（当前引用其 r=0.34 与“0–3 分差人机一致率 51.9%”时，容易默认原始数据可核；2026-08-15 复核该仓库仍只有两条提交（`55586f66fa` 首次公开、`de85750ff4` docs: link the AutoDesign paper，均在 08-14），仍 721 条目，全树 42 个 `.json`/`.jsonl`/`.csv` 逐一查看后全部是配置与 skill 定义，数据类文件仍只有 `eval/benchmark_manifest.jsonl` 与 `eval/small_subset_ids.json`，**附录 A.5 声明的逐案例分数存档与人评决策记录仍不存在**；可以尝试在引用该 r=0.34 时固定附一句“其原始人评数据仍无法被第三方复核”，协议部分——每次只改一个组件、`J_train` 严格提升且 `J_dev` 不降、dev 分数不回传给优化器——来自论文正文不依赖仓库，可继续借用）

方向：去重 — Hugging Face 窗口内 84 条新条目，一手发布 0 条（当前 arXiv 零结果时容易把 HF 的新增当作补充来源；本轮以 14 个关键词按创建时间筛出窗口内 84 条，逐条查看后全部为第三方微调、镜像、量化或无关条目，抽查四条确认性质——`hoidhxd/NVIDIA-NemotronLabs-VoiceChat-11B-GGUF` 带 `base_model:quantized:` 标签、`aoiandroid/NVIDIA-NemotronLabs-VoiceChat-11B` 为镜像、`KitsuMate/omnivoice-onnx` 为第三方 ONNX 导出（官方仍是 `k2-fsa/OmniVoice`）、`Iambackup/Qwen3-TTS-12Hz-1.7B-Base` 为再上传，四者下载与点赞均为 0；可以尝试把“`base_model:` 标签族 + `ATTRIBUTION.md` 一类文件 + 作者是否为论文单位”作为一手/二手的快速判据；另记一处名称碰撞——`rc19477/gh200-semamba-docker` 与 `rc19477/x86-semamba-docker` 作者标识与 RT-SEMamba 作者相符但创建于 2025-05-12，是**前作 SEMamba** 的 docker 镜像，不是本轮制品，RT-SEMamba 在 HF 上没有任何条目）

方向：回声消除 — 连续第三轮无重大新增（当前 AEC 与扬声器泄漏仍完全没有一手依据；本窗口 arXiv 全站零新论文，今日唯一的技术更新 RT-SEMamba 明确是单通道降噪，README 与配置中都没有任何回声消除或扬声器泄漏内容；继续跟踪，不写任何 AEC 结论或预期）

方向：持续跟踪不重启的项目（判定器处理不变性检查仍是优先级最高的一件事，**高于本轮的前端候选**，08-14 已列出且本轮无新证据，不需要 GPU；ASR 段计时口径对齐、留出片与预先声明门槛两条流程项同样维持；Qwen3-Omni MoE 无同步 fast path 仍是本周期唯一的解码时延实验，`decode-loop-cudagraph-static-kv-v1` 顺序不变——fast path 先行、图捕获后行且两步不合并；PACE 中间件本体仍未公开；X2-Turn 代码权重仍未公开，可用的仍只有 `mistralai/Voxtral-Mini-4B-Realtime-2602` 与 `ASLP-lab/Easy-Turn-Testset`（均 Apache-2.0、非门控）；candidate-ID/CMTF、conditional top-2 verifier、投机预取、工具清单压缩、主模型自判停的负结果不重跑；参数化本体、云监督与 SoulX 不重复启动）

今日新增 handoff 候选：1 条，`streaming-se-frontend-rtsemamba-kd1-v1`（仅离线、`h20_request_allowed=false`，状态为 `proposed_offline_blocked`）。它的单变量是“是否在 ASR 之前插入 RT-SEMamba KD1 流式前端”，但被两个前置任务挡住且都写进了依赖：一是快照“下一步”第 2 条的 clean/20dB/音色分桶归因必须先完成——它同时提供靶子与 `expected_fixes` 的上界；二是必须先对该仓库做一次只读代码审阅（重点是 vendored `mamba_install`/`mamba_install_mps` 的版本兼容性、`utils/streaming.py` 环形缓冲是否与逐帧一致、`rt_enhance.py` 的 `sounddevice` 设备访问范围），这是项目“不运行陌生论文仓库代码”边界的落实方式。`expected_fixes` 诚实记为 **null**：没有任何一篇降噪论文报告过下游 ASR CER 或工具正确率，分桶前的任何预估都是编造。其余内容全部只留在报告：arXiv 零结果的三级证伪、Luna 与 AutoDesign 的跟踪复核、HF 去重记录，都没有可执行单变量。既有 `omnivoice-local-tts-16step-v1`、`tool-hold-message-first-audio-v1`、`playback-boundary-ledger-offline-v1`、`decode-loop-cudagraph-static-kv-v1` 全部保持仅离线、`h20_request_allowed=false`，本轮无一条状态变化。

去重与来源关系：**本报告没有引用任何新论文**，因此不存在新 arXiv ID 入库。三处需明确的来源关系：其一，RT-SEMamba 不是新工作而是 **2608.12099（2026-08-12 投稿、08-13 已展开）的制品发布**，其 PESQ 数字本轮未变，新增的只有实现级参数与深度扫描表，不得作为新论文计数；其二，`rc19477/*-semamba-docker`（2025-05-12、MIT）是**前作 SEMamba** 的 docker 镜像，与 RT-SEMamba 是同作者的不同工作；其三，HF 窗口内 84 条全部为第三方衍生，`NVIDIA-NemotronLabs-VoiceChat-11B` 的 GGUF 与镜像两条、`omnivoice` 的 ONNX 导出与两条 singing 微调，都是既有工作的下游产物而非新增。另记本轮的源内不一致与状态：RT-SEMamba 的加速倍数现有**三个说法**——摘要 2.75×、正文约 2.6×、README“0.11 对 0.29 ≈2.6×”，README 没有任何勘误表述，因此这是新增的第三个说法而不是对摘要的更正，引用时两值并列；Luna-TTS 项目页的 1.28s 与 0.36s 首块矛盾**未修正**；AutoDesign 附录 A.5 承诺的制品**仍不存在**；13 个跟踪 arXiv ID 经逐一复核**全部仍为 v1**，无改名、无修订版、无重复来源。
