# 每日调研日报｜2026-08-21

证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、最近 H20 A/B 2026-08-03，本轮无新提交与新基准）。**Radar 连续第九轮未刷新**：`data/items.json` 的 `generated` 仍是 2026-08-12 09:30、条目仍 499，新增 0 条。**索引继续按每天一批推进**：08-20 报告记录全站最新投稿为 2608.19197（2026-08-19T17:58:56Z），本轮同一查询返回 **2608.20340（2026-08-20T17:59:59Z）**，只新增 08-20 一个批次，截止时刻仍落在 17:5x 的 arXiv 每日截稿边界上（**连续第五次印证索引按整批推进**）。窗口按投稿时间取 **2026-08-19 18:00 UTC 至 2026-08-21 02:30 UTC**，与 08-20 报告实际覆盖到的最后一篇严格衔接、不重叠，14 组查询去重后 **78 篇**（对照：`cat:cs.SD` totalResults 21,749、`cat:eess.AS` 22,209，两者本轮只增 9 与 7，是记录以来最小的单批增量之一）。**本轮四组语音查询直接为 0**（语音语言模型、流式 ASR、空间听觉、音频评测），**而 P0 来自 `cat:cs.HC`——第 14 组查询连续第二轮独家命中当轮最重要线索**。**逐版复核本轮零新增，是本项目首次**：47 个追踪 ID 一次批量查询，3 个非 v1 全部是此前已差分过的条目。另：main 此前仍停在 2026-08-10（**连续第九轮**），08-11 至 08-20 的 14 份报告与 8 份候选台账已在本轮先恢复到主线。制品复核时刻为 2026-08-21。

方向：判停与应答旁路（本轮 P0，核心价值在制品不在论文） — MaAI（当前问题：`probe_turn_end` 已实测主模型自判停不可行，而 08-20 找到的替代路线 X2-Turn 要再加一个 4B 模型、和 110ms/token 的 MoE 主模型抢同一张卡；`MaAI-Kyoto/MaAI` 是京都大学维护的**实时连续非语言行为预测软件**，覆盖判停（VAP）/ 语音活动检测 / 应答预测与检测 / 点头四类，**源码 MIT**（LICENSE 1,078 字节逐字读为标准 MIT）、224 blob 合计 88,247,753 字节、120 stars、最新 tag v0.2.15，主 README 自述"支持英语、中文与日语"且"**即便完全只用 CPU 也能高效运行**"；**它完全不做 ASR、不做语义、只从声学流输出概率**，正是项目负面结果所指向的那种彻底解耦的旁路形状；**帧率是用户可设参数**——判停与应答支持 5/10/20 Hz（CPC 编码器）或 12.5 Hz（Mimi），VAD 支持 10/20/50 Hz 或 12.5 Hz，**这是追踪表上第一个把"算法块长 / 精度 / CPU 占用"这个取舍交给使用方的判停制品**；另有 `MaaiMultiple` 让多模型共享同一音频编码器、"（相对昂贵的）编码器每音频帧只运行一次"，可直接照搬到项目的多头旁路上；可以尝试的是用中文 `vap_bc_ch` 在项目 452 条基准音频上做**离线应答时机预测、只做旁路观测不接播报路径**，分别记录 5/10/20 Hz 下的 CPU 占用、每帧耗时与触发点分布；**但三条边界必须写死**：**本轮没有从任何一手来源核实到 MaAI 的精度数字**（论文只有主观量表，README 把精度全部指向四篇未核实的外部论文，自测前项目文档里不得出现任何准确率）、**权重是四个许可证家族的拼盘而不是 MIT**（见下条）、以及它最近十二天的四次功能更新全部落在本项目此前的报告窗口内却全被漏掉）

方向：许可证 — "MaAI 是 MIT 的"这句话是错的，必须逐模型看（当前问题：代码许可证从不传递到权重，而这次是追踪表上最碎的一次拼盘；34 个 HF 模型分四个家族——**MIT**：`vap_bc_{jp,en,ch,tri}`、`vap_*_kyoto` 系列、`vap_nod_jp`、`vap_nod_para_jp`、`vap_prompt_jp`；**CC-BY-NC-ND-4.0**（非商业**且**禁止衍生）：`vap_{ch,en,jp}`、`vap_mc_*`、`vap_fr`、`vap_ca`、`vad_jp`、**`vad_ch`**、`bc_det_jp`、**`bc_det_ch`**；**CC-BY-NC-SA-4.0**：`vap_tri`、`vap_mc_tri`；**CC-BY-4.0**：`continuous-mimi-onnx`；**完全无许可证标签**：`vap_bc_2type_jp`、`bc_det_en`（按未知处理）；**最关键的一条：中文判停主力权重不可商用，而它的 MIT 替代品能力严格更少**——`vap_ch`（NC-ND）含 9 个权重文件覆盖 5/10/20 Hz 与 2,500/3,000/5,000/20,000 毫秒多种上下文加 Mimi 12.5 Hz，而 MIT 的 `vap_ch_kyoto` **只有 2 个文件**（5Hz 与 10Hz、均 20,000 毫秒），**无 20 Hz、无 Mimi、无短上下文**；**同样关键：最近十天新增的三个模型全部是 NC-ND**——`vad_ch`（2026-08-12）、`bc_det_ch` 与 `bc_det_jp`（均 2026-08-17），即**"双通道串音归属判定"这个对项目最有吸引力的能力，其中文权重恰恰不可商用**；可以尝试的是在任何技术评估之前先做一张"可商用/不可商用/未知"三分表，**目前可商用的中文权重只有 `vap_bc_ch`（应答预测）与能力更少的 `vap_ch_kyoto`（判停）**；上游自己写明了这条规矩："源码为 MIT……**至于训练好的模型，请遵循各模型 README 或 HF 仓库中所述的许可证**"）

方向：应答频次 — 倒 U 型，"越多越好"被显著性检验证否（当前问题：项目若要做座舱应答，天然会把频次当成单调越高越自然；2608.19527（京都大学 Inoue/Kato/Kawahara + Sony CSL，ICMI 2026 LBR track，N=35 日语母语者、被试内、AB/BA 配衡）的主结果是加入应答与点头后**注意感 5.29 对 4.80（p=.018）、真实感 4.77 对 4.29（p=.006）、共在感 4.60 对 3.94（p=.002）**，但**真正有用的是那条曲线**——**注意感对应答次数呈显著倒 U 型、二次项 p=.009、在中等数量处达峰**，论文原话 "simply providing more was not necessarily better"，点头与其余各项均无显著曲线关系；系统实际量级为每段对话 27.29 次（SD 7.55）、每条用户话语 2.25 次（SD 0.92）；可以尝试的是**把"应答频次设上限"直接写进座舱交互参数表**，**抄形状不抄数值**——2.25 次/话语来自日语实验室平板场景；**同等重要的是它自己报的负面结果：用户行为一点没变**——话语条数 12.89 对 13.11（p=.516）、平均话语长度 7.30 对 6.93 秒（p=.195）、对话时长 4.90 对 4.83 分钟（p=.300），**因此不得用它论证应答能提升效率或缩短交互**；另注九项量表中六项无显著优势，**包括节奏感 Q9**——加了实时应答之后用户并没有觉得节奏更顺）

方向：全双工的边界 — 那篇论文的主管线是按键说话，不是全双工（当前问题：容易把 2608.19527 读成全双工证据；论文原文写明 "The system adopts a push-to-talk interface: the user presses and holds a button ... and the recorded speech is processed through the ASR→LLM→TTS pipeline upon release"，主管线全是云服务（ASR Deepgram nova-2、LLM GPT-4.1、TTS Cartesia sonic-3），**它证明的是"半双工主通道 + 并联一条连续的倾听反馈旁路"有感知收益，不提供任何关于打断、抢话或重叠语音的证据**；另注全文**零时延数字、零硬件规格**（只写明经浏览器跑在一台 Android 平板上），应答音频是预生成固定片段随机抽取、点头是头像图片垂直动画（单点头 70%/双点头 30%），属原型级实现；可以尝试的是把它归档为"旁路通道的证据"而不是"全双工的证据"，本篇不能为任何时延预算提供依据）

方向：回声消除 — 连续第九轮零一手依据，但第一次出现了"泄漏的下游后果"的可用制品（当前问题：AEC 与扬声器泄漏仍完全没有一手证据；本窗口 `speech enhancement OR noise suppression OR echo cancellation OR beamforming` totalResults=**1**，该 1 篇是 2608.20278 无人机六维可动 IRS 辅助 ISAC 的保密速率最大化，**非声学**，"beamforming" 连续第二轮 100% 落在非声学领域；**但 MaAI 的双通道 VAD 值得单列**——它用**跨通道注意力**联合处理两个通道，官方原话"即便一位说话人的声音泄漏进另一位的麦克风即串音，模型仍能判断实际是谁在说话"，训练含噪声与**混响 RIR 增强**故"`vad` 本身就是多条件模型"、无单独 `vad_mc`，二值化默认阈值 0.5、官方另注 **0.54 在开发集上取得最佳 F1**；可以尝试的是把它用在"现在这段声音是用户在插话、还是我自己的播报漏回来了"这个判定上；**但绝不能记成 AEC 有进展**——沿用判别式：**ANC = 副扬声器 + 误差麦克风；AEC = 自身播放信号的参考/回环抽头；MaAI VAD = 串音条件下的归属判定**，第三类接受泄漏存在并做归属，不消除也不抵消，**AEC 计数保持九轮为零**；另注它的双通道是"用户 vs 系统"而不是"乘员 A vs 乘员 B"，**不可直接当多乘员方案**）

方向：判定器构造效度（优先级仍最高） — Thinkingbox 直接挑战了项目现在的头条指标（当前问题：项目对外的数字是**单次运行的严格工具名召回 80.49%**，它既是工具调用层面的信号又是单次聚合值；2608.19741（微软，45 页，CC-BY-4.0）用 507 任务 × 每任务 20 次独立尝试 × 12 模型（每模型 10,140 次试次）给出——**GPT-5.4 的 pass@20 达 91.12%，而 pass^20（20 次全中）只有 25.25%**，论文概括为"重复推理常常能找到一条成功轨迹，却并不能让 agent 变得可靠"；已核实的另两行：MiniMax-M2.5 pass@1 0.19% [0.00%, 0.53%]、pass^20 0.00%、pass@20 0.59%、**504 个任务 0/20 且 0 个任务 20/20**；Qwen3-8B 0.13% [0.01%, 0.34%] / 0.00% / 0.99% / 502 / 0；论文的核心主张一句话："**工具调用层面的信号不是端到端任务完成的清晰代理**"，**恰好同时命中项目头条指标的两个弱点**；加上 08-20 从 ComponentBench 记下的"两次运行聚合完全相同（52.5%）却有 17.3% 逐条翻转"，现在有两条独立证据说明单次聚合会系统性高估可靠性；可以尝试的是**把 452 条自建判定改报 pass@k 与 pass^k 一对数字**并在聚合值旁并列逐条翻转率，**先只改报告口径与最小子集试算，不急于跑满 20 遍**；**但对座舱有一层特殊含义必须写清**：论文场景允许重试所以 pass@20 有意义，**而用户说"打开空调"没有第 20 次机会——pass^k 才是对齐业务语义的那个指标，pass@k 只用于区分"能力存在但不稳定"与"能力不存在"**，照抄论文以 pass@k 为主的呈现顺序会误导）

方向：判定架构 — 08-20 那批的干净反例，第一份正面外部先例（当前问题：08-20 记录的三篇（SemaPLC、ComponentBench、2608.18351）判定器或真值都由作者或 LLM 造、人工一致性统计全部为零，项目"判定链路不得引入 LLM 判定器"这条规则此前只有反面教训；Thinkingbox 反过来——**主判定是确定性的哈希断言**，比对终态数据库与任务特定黄金态，"agent 只有在产生了确切要求的副作用、且没有引入错误的额外副作用时才算成功"，解码失败/测试执行失败/API 错误一律计为失败试次，**LLM 只用于给失败轨迹打归因标签、不进入判定**，且论文把取舍写明了："我们接受这种不对称，以保持主指标的确定性；把 LLM 评判的评分细则扩展到每个任务，会把评判者方差重新引入判定"；它的失败归因也自我降级为"可观测的诊断，而不是唯一的因果解释"；**它的任务筛选规则里有一条与头条主张自洽、值得单独抄**：剔除"**检查依赖于工具调用轨迹**"的任务——既然主张轨迹不是完成度的代理，判定就不能建立在轨迹上；可以尝试的是整体照搬这个分层：判定确定性、归因才允许用模型、归因结论明确降级）

方向：试次隔离 — 报 pass^k 之前必须先做的一步（当前问题：项目若直接跑 20 遍统计 pass^k，共享状态会让数字系统性偏高；Thinkingbox 把这条写成硬要求："**同一任务的两次尝试不得共享数据库行、缓存的工具状态或副作用，否则 pass@k 与训练奖励会变得不可靠**"；另注其 20 次试次并非确定性重跑——模拟用户是 GPT-5.4-mini、温度 0.3、无推理努力、4,096 token 上限、最多 10 个追问轮次且采样非确定，**因此它度量的是"对一族用户交互分布的鲁棒性"而不是"对单一固定对话的重复"**，论文对此很诚实；可以尝试的是把"先证明两次运行之间没有共享状态"作为改报口径的第一步，而不是第二步；论文自己也逐条列出了模拟用户不具备的真实属性："不会记错或说错事实、不会中途改变目标、不会在反复失败后变得不合作、简短是固定指令而非因人而异的行为特征"）

方向：prefill 与 KV 复用 — ReCache 命中了成本结构，但它的 TTFT 定义正是上一轮记下的那个陷阱（当前问题：08-19 从 2608.14036 记过"技能每任务多花 93.8K 输入 token"，工具 schema 跨请求高度重复；2608.19662 把可复用的 schema 编码与选择性资源访问分开，资源级注意力单独就做到 Inv-F1 **82.3% 对 Dense 的 82.4%** 而 **TTFT 26.319ms → 7.200ms（3.655×）**，完整框架再把已分配 KV 张量显存降 **92.43%**、注意力加速 **1.423×**；规模扩展上 Dense 的 TTFT "从几十毫秒涨到 5,000 毫秒以上"而 ReCache 近乎平坦、略高于 5ms，TPOT 从约 11ms 降到略高于 6ms，注意力时延从约 5ms 降到低于 0.2ms，显存从逼近 8 GiB 封顶到 **0.03 GiB**；硬件为 Qwen3-4B 一张 A800、Qwen3-1.7B 一张 RTX 5000；**但决定性的否决理由是它的指标定义**：原文"我们用 TTFT 量化**缓存命中下**的在线 prefill 时延，包含请求局部的 KV 物化，但**排除离线缓存构建**"——**这与 08-20 从 `Qwen3TTS-Streaming` README 挖出的 `prefix-cache 命中` 是同一个陷阱，只是这次写在论文自己的指标定义里；车机上电后的第一句话必然是冷的，因此这组数字对项目最关心的那一次请求不成立**，论文也没给任何冷启动数字或离线构建耗时；可以尝试的是只带走它的指标拆分纪律——**把 TTFT、TPOT 与"注意力时延"分开报**，论文自己解释了原因：注意力上的收益"会被 TPOT 里未变化的投影与前馈计算所掩盖"；**明确不以批大小否决**——本篇批大小从未在正文报告，属"未知"这第三种情况，不是"从未在 1 上评测"也不是"收益随批变小而缩水"，按未知记录）

方向：开放状态 — 新增第七种形态"承诺两件、交付一件"，外加"公开但无许可证"（当前问题：分档要能指导后续动作；**第七档**：Thinkingbox 论文写 "We release both THINKINGBOX and THINKINGBOX-BENCH: https://github.com/microsoft/thinkingbox"，该 URL **确实解析且仓库真实**（MIT，created 2026-04-22、pushed 2026-08-20T17:19:40Z 与论文同日，142 blob 合计 1,001,955 字节，6 stars），**但 507 个任务不在里面**——全树 `dataset/` 下只有 `scenario/cloud_drive.yaml`(396B)、`test_case/cloud_drive.meta.yaml`(807B)、`test_case/cloud_drive.py`(6,177B) 三个文件，而 `cloud_drive` **不属于论文五个业务域（零售/预订/保险/新型银行/咨询）中的任何一个**；它既不是第五档（URL 失效）也不是第三档（无 URL 的 unverifiable），而是"**承诺 A 与 B，交付了 A**"；**并且这是 08-20 记录的"发布产品不发布实验"形态的第二个独立实例**——该 README 长 20,489 字节而 `arxiv`/`thinkingbox-bench`/`507`/`pass@`/`retail`/`insurance`/`neobank` 出现次数**全部为 0**，第一例是 `midea-ai/SemaPLC`，**两例形态完全一致：机构把工程框架开源，把论文实验留在内部**；**另一种新形态是"公开可读但无许可证"**：`EIT-NLP/ReCache` 确实存在（created 2026-08-19T03:12:43Z、pushed 同日、1,881KB、1 star）且论文摘要写 "The code is available at ..."，**但 GitHub API 的 `license` 字段为 `null` 且全树递归确认没有任何 LICENSE/COPYING 文件**，按 GitHub 默认条款保留全部权利；可以尝试的是把"许可证缺失"记为独立状态，**"公开可读"不等于"开源可用"，不得与"已发布可核"合并**）

方向：发现通道 — 制品通道是第三条独立通道，不是可选补充（当前问题：MaAI 建于 **2025-07-04**、比本项目追踪表还早，却到今天才被发现，原因是它十三个月里没有产出过一篇会被 14 组关键词命中的论文；而它最近十二天连续新增了实质功能——2026-08-09 加入 VAD 模型、2026-08-12 加入中文 `vad_ch`、2026-08-17 加入应答检测模型、2026-08-19 加入单通道模式，**这四次更新全部落在本项目最近几轮的报告窗口内、全部被漏掉，原因都是没有论文**；可以尝试的是把三条通道并列固化：**第一条 `submittedDate` 窗口查询（看不见修订版）、第二条追踪表逐版复核（只能看见已在表内的条目）、第三条制品通道即按机构/作者反查 GitHub 与 HF（能看见根本没有论文的活跃工程）**；08-20 已记过一次制品通道的价值（一次 `/users/X-Square-Robot/repos` 调用同时查清三件事），本轮是第二次，**且这次它找到的是一个完全不在追踪表里的项目**）

方向：关键词扫描盲区 — 全双工组连续第二轮既空又漏，另新增一处最大跨度假阳性（当前问题：`full duplex OR turn-taking OR barge-in OR interruption` totalResults=**3** 且 **3 篇全是假阳性**（2608.19893 基座语言模型周期性话题更换对判定惊讶度的影响、2608.19887 时变时滞诱导的间歇性集群、2608.19622 数据中心灵活性与电网），**而本轮真正的判停/应答进展来自 `cat:cs.HC`，这是连续第二轮出现同一形态**——08-20 的两条 P0 也都不是这组命中的，**结论强化：这组关键词对"全双工"主题存在系统性盲区，其空满程度不得单独用于任何方向性结论**；`interrupt` 词根新增第三个假阳性来源（前两个是并行机排程与统计学固定术语 "Interrupted Time Series"）；**TTS 组 3 篇中 2 篇跨领域假阳性**，其中 2608.19651 是**粒子物理**的电弱重子生成，是本项目记录到的跨度最大的一次关键词假阳性；可以尝试的是继续保留第 14 组查询——**它连续第二轮独家命中当轮最重要线索**，本轮该组 4 篇中 1 篇是 P0）

方向：查询写法缺陷 — 上一轮的车内组已修复并做了对照，本轮新发现 `endpoint` 过宽（当前问题：**车内组修复对照**——旧写法 `abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver" AND abs:"speech"` 本窗口 totalResults=**15**，新写法 `(abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver") AND (abs:"speech" OR abs:"voice" OR abs:"dialogue" OR abs:"assistant")`=**3**，**且修复后剩下的 3 篇仍全部与座舱语音无关**（UAV IRS-ISAC、IoT 车载应用测试流水线、近海风场 AUV/ASV 导航），**因此车内语音方向本轮确实为零是双重确认的，不是查询写法造成的假象**；**本轮新缺陷**：`abs:"voice activity detection" OR abs:"endpoint" OR abs:"endpointing"` 返回 **22 篇、22 篇全部与语音无关**，覆盖泛函分析（"endpoint estimates" 是该领域固定术语）、Tor 网站指纹防御、地球观测数据架构、心脏形状补全、全息对偶、几何相位等，**根因是裸词 `endpoint` 在数学与安全领域都是高频固定术语**——与上一轮的车内组同属"查询写法缺陷不等于该方向无产出"，但**成因不同：上一个是布尔结合律，这一个是词义歧义**；可以尝试的是下一轮改写为 `abs:"voice activity detection" OR abs:"endpointing" OR abs:"end-of-turn" OR abs:"turn-end detection"` 即删去裸词 `endpoint`，**并同样记录改写前后的 totalResults 对照，以免把口径变化误读成产出变化**）

方向：零结果本身 — 四组语音查询直接为 0，而本轮仍出了 P0（当前问题：容易用查询组的空满程度判断产出；本窗口 `speech language model OR spoken dialogue OR speech-to-speech`=**0**、`speech recognition AND (streaming OR online OR latency)`=**0**（连续第二轮）、`spatial audio OR ambisonic OR binaural OR sound source localization`=**0**（连续第二轮）、`speech evaluation OR audio benchmark OR MOS prediction`=**0**；语音类目控制总数本轮只增 **9** 与 **7**（cs.SD 21,740→21,749、eess.AS 22,202→22,209），是记录以来最小的单批增量之一；**四组同时为零而本轮仍出 P0，本身就是对那种做法的又一次证伪**；**另注逐版复核本轮零新增是本项目首次**——47 个 ID 一次批量查询，3 个非 v1 全部是此前已差分过的（StreamTTT 与 Ambisonics 于 08-19、X2-Turn 于 08-20），**必须与"没查"区分开来记录**）

方向：术语辨析 — 12.5 Hz 第三次出现，但这次不是矛盾（当前问题：08-19（VoiceChat-TTS）与 08-20（Qwen3-TTS 生态）记录的 12 Hz 与 12.5 Hz 不一致，是同一模型的两种说法互相打架；**MaAI 这里不同**——它明确写出 12.5 Hz 对应 Mimi 编码器、5/10/20/50 Hz 对应 CPC 编码器，**是两类不同模型而非同一模型的两种说法**；可以尝试的是记录在案但不并列，**防止日后把三处混成"同一个生态问题"**；另注 2608.19527 引文里出现的 PersonaPlex（arXiv:2602.06053）与本项目 08-11 的判定一致——它是一个独立的 S2S 模型，本轮不改变该判定）

方向：prefill 稀疏注意力 — 第一篇基准硬件就是 H20 的论文，但按构造与项目无关（当前问题：项目首音频瓶颈含 prefill，而此前所有相关论文的硬件都无法直接对照；2608.19758 FlashPrefill V2（中科院自动化所 + 微信/腾讯）原文写明 "All methods are evaluated ... on **NVIDIA H20 GPUs**"，算子级单张 H20、端到端 TP=4 四张 H20，128K 下相对 FA2 为 27.19×（BF16）/47.26×（FP8）、相对 FA3/4 稠密算子为 17.5×/30.5×（**后者才是诚实基线**），批 1、Qwen3-30B-A3B 的端到端 TTFT 在 128K 为 13.78s→4.17s（3.30×）；**但四条否决理由必须写准以免日后按错理由重新评估**：其一它按构造只管 prefill，原文"**decode falls back to dense attention, since a single query token per step leaves no room for block-level sparsity**"；其二项目 KV 约 2,500 token，整个落在其最短列 4K 之下，而 4K 处 BF16 只有 1.01–1.09×、密度仍有 70–76%；其三**其官方仓库出厂默认 `flashprefill_min_sparse_q_len = 4096`**，低于该值整批走 FA3，即对项目直接失效；其四 **FP8 路径在 RTX 5090 上不存在**——451 个 kernel 实例化里 sm90 有 310 个、sm100 仅 1 个且只有 BF16、**sm120 为 0**，全部 80 个 FP8 实例化都是 sm90 专属；**明确不以批大小否决——它真的评测了批 1（1/4/8/16）且收益基本不衰减（128K 上 MoE 批 16 的 3.40× 到批 1 的 3.30×），但其头条 47.26×/27.19× 是批 4 的算子数字**；可以尝试的是只带走它的五条原语，全部与项目 CUDA Graph 与无同步 MoE dispatch 直接对口——**靠"各自重算"而非"互相通信"消除分歧**（"Producer and consumer evaluate the same sequence independently, so their control flows are **identical by construction** ... **without any synchronization**"）、**用掩码跳过而非分支跳过**（`todo = valid & ¬selected`，固定形状无主机分支）、把不规则工作折叠成一次固定形状的额外迭代且"**without any additional kernel launch**"、**项目自身根因的第二条外部佐证且首次来自跑得起来的代码**（"a **per-stream workspace cache whose capacity-matched reuse avoids any host synchronization or allocator round trip** in steady state"，第一条是 FreeToken）、以及按工作量而非按位置切分负载；另带一条负面结果：**分块 prefill 会侵蚀稀疏收益**，Qwen3-30B-A3B 的 P50 TTFT 加速从不分块 2.2× 掉到 8K 分块 **1.2×**）

方向：论文与自家仓库互相矛盾 — 本轮最严重的一处，且矛盾在头条方法学声明上（当前问题：08-20 记录过"论文与自家仓库对同一量给出不同数字"，本轮升级为**对同一方法学声明给出相反陈述**；FlashPrefill V2 论文 §4.1 写 "every model shares a single configuration **without per-model tuning** ... α = 0.1"，而其官方仓库 `flashprefill_ops/flashprefill/prefill.py` 的注释写 "**Paper-calibrated values** (block size 128): Llama-3.1-8B 0.18, Qwen2.5-7B 0.08, Qwen3-30B 0.12."，`server_args.py` 帮助字符串重复同样说法——**这是逐模型调校的三个值、被仓库明确标为"论文校准"，而其中 Qwen2.5-7B 在论文里根本没出现过**；**同时仓库还暴露了一个论文全文从未提及的开关** `--flashprefill-full-attention-layers`（README 示例传 4、代码默认 0），因此论文 Table 4/5/6 用的哪个取值无法从论文得知；可以尝试的是把"**核实论文对自己仓库的方法学声明，而不只是核实数字**"列为常规检查项，两边原样并存不替作者选择；**同一轮还出现了它的姊妹形态**：2608.19557 发布的代码里含有一个**比论文所报更公平的基线**（`lib/baselines_windowed.py` 文档写明是 "windowed PSO, GA, DDQN at W=40"，而论文全文没有报告任何 W=40 的元启发式数字，偏偏该论文自己的消融证明 W 是主导变量），**故追加规则：比对仓库的基线文件与论文的基线表**）

方向：KV 驻留 — 不要预测，要测（当前问题：项目在为自己约 2,500 token 的 KV 建预算模型时，天然倾向于用容量与速率去推算命中率；2608.19677 CacheRoute（Meta，单作者，60 张 H100 / 30 个目标）在**通过了七项隔离检验、且精确测得每个目标 40,071 个 KV 块**的前提下，其单特征时间占用模型仍然 "missed served hit rate by **14.3 percentage points at the median and 44.7 points at p90**"，论文自述观测曲线"急降后走平，形状落在所测模型族之外"；**这个失败是全文最有价值的结果**；可以尝试的是把"**不要用容量与速率预测缓存驻留，去测**"写进项目 KV 账本的方法说明；另带走两条——**影子回放作为上线门**（"A cache-hit increase is not enough; the plan is enabled only when **p99 or capacity improves**"，即中间指标改善永远不构成上线理由，与项目"前端改动必须以自己的 CER 验收"同源）、**只有局部性没有均衡是没有价值的**（Sticky 路由拿到第二高命中率 87.3% 和**最差的 p99 8.5s**，"locality alone does not help once a queue dominates the tail"）；**论文本体三条规则同时触发否决**：多机前提、**批大小根本没有被评测过**（全文 `batch` 只出现两次且都是行文用词，自变量是 QPS、最低一档 30）、以及**其头条改进就是 KV 命中率 64.1%→93.2% 而座舱第一句话永远是冷的**；开放状态为第六档最纯粹一例——**全文 PDF 的 `https?://` 命中数为 0，连自链接都没有**）

方向：慢通道不得门控快通道 — 一条被证成不可能性论证的既定设计（当前问题：项目的 PACE 式设计一直把"慢推理通道绝不可门控快音频通道"作为自设约束，缺外部佐证；2608.19557 把它写成了不可能性论证，措辞可直接引用："A model call takes seconds, whereas an admission decision must be taken in the time it takes a PU to free; **placing a language model on the per-task path is therefore not a tuning choice but an impossibility**"；配套第二条同样可直接搬——**必须有陈旧策略回退，且理由是推导出来的**："Because control-plane latency is not bounded by the window duration, a deployment cannot assume a policy update will arrive before the window it was requested for. **The data plane therefore always holds a committed Θ and proceeds with it if no reply has arrived.**"；第三条是一个可引用的成本锚点：**小型托管模型 + 受约束输出 schema 的策略调用实测均值 3.0s、中位 2.8s、p95 4.3s**，日后有人提议把模型调用放到 1,000ms 首音频门附近时可直接引用；可以尝试的是把这三条并入项目异步策略（路由提示、工具预选、说话人归属旁路）的设计说明；**论文本体否决**：仿真器结果、全文零硬件声明（`gpu|cpu|nvidia|hardware` 命中数为 0）、多机前提，**且最锋利的理由是它自己的消融**——全部优势归于一个 40 个待处理任务的批处理视野，**W=1 时 0.795 与朴素 EDF 完全相同**，而座舱任一时刻只有一个人在说话）

方向：语音 A/B 的前置条件 — 变调会悄悄改变响度（当前问题：项目未来若做音色或韵律对比，天然假设只有基频被改变；2608.19826（RO-MAN 2026，N=27 荷兰儿童）原文："Reexamination of the stimuli revealed an **unintended intensity variation, possibly introduced by speech manipulations**. A second survey using **intensity-normalized** stimuli (70dB) was implemented to improve reliability."——**他们是在第一轮问卷已经跑完并得出零结果之后才发现的**；可以尝试的是把"**任何音色/韵律 A/B 必须先做响度归一化（70 dB）**"写成硬前置条件，**不归一化的对比是被混淆的、其零结果不可解释**；论文本体否决——因变量是听者情绪（项目按既定规则排除），且它本身就是零结果：固定 ±35 Hz 基频偏移把自报压力推向**与假设相反**的方向且不显著（**F(1,25)=1.70, p=.20，只解释 6.4% 方差**）；**可留的第二条是反对静态韵律预设的负面证据**——作者自诊 "stable pitch manipulations ... may not reflect how pitch modulation is naturally used throughout a session ... pitch rarely operates in isolation"；另记两处不一致：摘要写压力上升"**confirming** the stress-inducing nature of the task"而其指定的主分析项 Item 1 只有 **p=.09 且论文自称 a nonsignificant trend**（p=.04 来自脚注的 Item 2），以及脚注 2 印 "R²=0.7, F(1,25)=1.83" 而该自由度下 F=1.83 只对应 R²≈0.068，**算术上不可能**）

方向：多乘员与身份旁路 — 两条既定判断各自拿到一条新的独立佐证（当前问题：项目的"身份判定走音频通路之外的旁路"与"多乘员应来自目标说话人选择而非空间编码"此前各只有一到两条依据；2608.20198（德累斯顿工大 + FAU，BioCAS 2026，GF22FDX 22nm 已流片）**在两条上同时给出佐证且论据类型都是新的**——其一，它对 AAD 输出的定位原文是 "The decoded attention information can **provide a control signal** for a hearing-assistance system to select or enhance the attended speech stream"，**这是"身份/注意力判定必须是控制信号而非音频通路内一级"的第二篇独立佐证**（第一篇是 08-19 的 2608.17972），**两篇互不相关、来自不同子领域、收敛到同一约束**；其二，它主动放弃空间线索："many of these approaches rely on **spatial cues** ... which may **limit their robustness** in practical acoustic environments. In contrast, stimulus reconstruction-based AAD remains **independent of speaker locations**"——**这是空间编码路线的第三条否定佐证，且论据类型是新的**：前两条讲的是渲染成本（DiffM2A 的 2.66s/4s、Meta/Aalto 自述"适合离线"），**这一条讲的是线索本身的鲁棒性，而且是一个做实硅的团队出于鲁棒性主动放弃**；可以尝试的是把这两条写进多乘员方向的设计依据；**论文本体否决且理由单一决定性：注意力判定每秒只更新一次**（"only eight correlation coefficients are maintained in a circular buffer and **updated once per second**"），**一个每秒刷新一次的控制信号无法参与打断仲裁，因为打断的全部预算比一个更新间隔还小**；**必须防一个误读——7.34 ms 不是判定时延**，那是每次推理的算力耗时、被设计成塞进 125 Hz 脑电流的 8 ms 采样周期内；另注该论文**从未声明自己的判定窗长度**（"decision window" 全文只出现一次且在参考文献标题里），8 个系数缓冲若是 8 次连续 1 秒更新则积分约 8 秒、否则更短，**记为待核，但最有利读法也已是 1 Hz，两种读法都不通过**；第二个独立否决理由是 31 通道头皮电极加**按人在 listening sessions 之间重载权重**；**另注全文没有给出任何 AAD 准确率**；可迁移技巧一条：**把皮尔逊相关从中心化形式改写为原始矩形式**以支持在线累加、消除历史缓冲，项目任何流上在线指标都可用同一替换把状态降为 O(1)）

方向：主观评测协议 — 本轮唯一一篇完全干净的论文，且它恰好修好了 X2Streaming-TTS 那 120 名听众评测的每处缺陷（当前问题：项目若做主观评测，08-20 已记录 X2Streaming-TTS 的反面样板——120 名听众但未说明母语构成、未报评分者间一致性、未做显著性检验；2608.19959（布尔诺理工，SPSC 2026，N=82）逐项交叉核对**全部通过**——表 2 条目数 108+108+36=252=63×4=9×7×4 自洽、判定数 2569+2514+2309=7,392 与 2519+2491+2419=7,429 精确相加、摘要 77%=100−22.65 与表 3 一致、人口统计两次独立加总均为 82；可以尝试的是逐条照搬其六条协议——**其一报告评分者间一致性并在系数很低时去解释而不是藏起来**（用 Krippendorff's α，并把准确率约 90% 下 α 近零解释为**基率悖论**："when the marginals are highly skewed ... **α therefore measures consistency, not correctness, and must be read jointly with the accuracy metrics**"，同时把它与边缘均衡时的真歧义区分开——**这条还能防止项目在自己偏斜的评测集上把低 α 误读成评分者不可靠**）；**其二把准确率拆成敏感度与偏倚**（报 d′ 与判据 c；本文 d′ 为 2.75/2.88/**0.73**，而**真实音频上的虚警率 17.5%** 正是原始准确率会掩盖的量）；**其三用配对逐被试的非参数检验加按被试重采样的 cluster bootstrap 区间**，因为每位听者贡献多个相关判断，**项目 452 条评测集正是这种聚簇结构**；**其四在解释任何指标之前先算出它的随机水平**（他们算出 All-OK 的随机水平是 **6.25%**，随后发现 33% 的得分"sits essentially at its chance level"——朴素读法会当成正面结果）；**其五用一族严格度递进的指标**，其中 **All-OK（全部片段都对）最值得抄**，正是它暴露了整段 F1 掩盖掉的定位失败；**其六在结果之前先声明自己的设计引入了哪些人为效应**；论文本体否决——它与合成质量、时延完全无关，全文 `latency` 只出现一次且是说明**没能**测量；**并且它的标题构成一种新的不一致形态**："Tracking the Trend" 而正文**三处**否认（"our results **do not establish a general temporal trend in speech synthesis**"），**故这里没有任何可用的 TTS 自然度纵向数据**；**另带一条与项目最高优先级同源的负面结果**：六个源基准 EER 仅 0.57–5.34% 的检测器在未见过的现代商用合成上崩到 **F1 0.09、比未受训练的人类 0.48 还差**，部分伪造的严格定位率 **0.00**——**一个检测器在自家基准上的 EER 几乎不能预测它在未见生成器上的行为**，这与 2608.12880 的构造效度失效是同一模式却来自完全不同的子领域，适用于项目任何想凭已发表分数直接接入的预训练分类器，VAD、打断检测、说话人归属都在内）

方向：脚手架敏感性 — 从三条佐证一次涨到六条，应当按既成事实处理（当前问题：项目一直把"脚手架能把指标挪到与换模型同量级"当作待验证假说，此前三条佐证是 DuplexWorld、SemaPLC、ComponentBench；本轮一次加三条且操纵变量各不相同——**EnvHarness Table 12 是六条里最强的一条**：同一策略、同一批 100 个 ALFWorld 任务、**同一个验证器**，只换掉包在外面的动作/转移/观测封装层，逐任务成功率落在 [0.4,0.6] 内的比例从 **6.0% 涨到 80.0%**、**平均成功率从 0.74 移到 0.48**，论文原话 "A single interface therefore suffices to calibrate an environment against an explicit, measurable objective, **without any access to its internals**"；**MaliciousSkillBench Table 42**：同一来源、同一检测器，只改划分协议，SRC011 良性误报率 **0.113→0.644**、SRC012 **0.000→0.522**，总体 Macro-F1 0.932→0.665（作者自己写明这不是配对因果估计，按原样记录）；**StateMemBench Table 8**：只拨一个与行为无关的开关、判定器保持不变，**20% 的条目翻转而聚合只动 8 个百分点**；可以尝试的是从本轮起把"脚手架可以把指标放到几乎任何位置"按既成事实处理，并把"聚合稳定而逐条大幅翻转"同样按**已被两个互不相关团队在两个完全不同领域各自复现过一次**的现象处理——ComponentBench 17.3%、StateMemBench 20%）

方向：判定器量具（本轮工作量最小、收益最大的一项） — StateMemBench Table 8 就是项目一直想做却没做的那个实验（当前问题：项目至今没有任何重复运行翻转率测量，而两篇论文现在都说聚合会撒谎；2608.19652（UIUC）的设计可逐字照搬——取 LongMemEval 多会话 oracle 的 **50 条配对条目**，**提示完全相同、全部历史都在上下文里因此没有检索混淆**，唯一操纵是把 DeepSeek-V4-Flash 的原生推理轨迹开关打开或关掉，**判定器在两臂之间保持不变**；结果：thinking off **42/50=84.0%**、thinking on **38/50=76.0%**、差 **−8.0 个百分点**，而四格转移表是**关→开由错变对 3、开→关由对变错 7、两臂都对 35、两臂都错 5**，即 **10/50=20% 的条目翻转**；论文自己的读法很克制："the −8.0pp point estimate rests on **3 rescues against 7 new failures**"；可以尝试的是把这张表的格式原样搬过来——**选一个与行为无关的拨杆（该论文选推理轨迹开关，与 2608.12880 的元数据标志位同构）、判定器固定不变、报四个转移计数而不是那个差值**，零 GPU）

方向：判定器构造 — 封闭选项池 + 可执行"坏读者"基线，让注入故障召回率按构造成立（当前问题：项目 452 条判定既没有已知错误答案，也无法测注入故障召回；2608.19652 的构造方式是：每个场景是一段**符号化事件程序**（规则声明、取值更新、带作用域的例外、承诺、撤回），正确答案由**确定性求解器重放算出而非人工标注**；随后实现一族**可执行的 lazy reader 策略**，每个对应一种可能失效（只信最近一次提及、只信最频繁取值、直接返回已陈述值而不重算、过早丢弃已锚定的决定），**陷阱场景恰好取自"至少一个策略与完整重放不一致"的那些，那些不一致的策略随即构成该场景的失效模式签名**——**这等于让每条样本自带一个已知错误答案**；评分为**封闭选项池**：生成时构造好金标准 + 被针对策略算出的**漂移答案** + 3–4 个中性干扰项，**提问时不展示**，且 "Gold and drift are computed **before rendering**, so the renderer cannot alter trap semantics"；**它还有项目完全没有的泄漏扫描器**——programmatically verify 每条承重事实出现在其指定会话中、且**逐条禁用短语不出现在渲染结果里**，失败则**重渲染直到通过**；可以尝试的是把 452 条改造成封闭选项池并**公布确定性份额**——该论文的对照值是"**纯字符串匹配能定下 28% 的答案，判定器在该子集上一致率 94.1%**"；另加一类**反陷阱**样本（锚定答案才是对的，只有过度作废的策略会偏离），用来抓住奖励过度作废的判定器）

方向：判定器硬规矩 — LLM 最多只能给二元判定，失效类别必须由构造固定（当前问题：08-20 记录的三篇（SemaPLC / ComponentBench / 2608.18351）都用 LLM 做失效归因且人工一致性统计全为零，项目此前只能说"它们没做核对"；2608.19652 给出了量化的否定依据——判定器间一致性显示**二元漂移判定的 κ 可达 fair-to-substantial（LME oracle κ=0.67），而"是哪一种失效机制"的 mode-κ 在每个基准上都是 0.00–0.09**，论文原话："**judges agree on *whether* a failure is drift but not on *why* the wrong fact won.** This is the empirical basis for … **mechanism must be fixed by construction rather than inferred post hoc**"；**因此 LLM 事后归因的机制标签在统计上等于噪声，不是"没验证"而是"验证了、没有信号"**；可以尝试的是把这条写成项目硬规矩；**配套第二条同样重要——判定器给出的漂移率是上界不是真值**：其人工锚定显示两名标注者与判定器的二元一致率只有 **58.3% 与 41.7%**，且两人给出的漂移都**比判定器少**（8.3% 与 25.0% 对 50.0%），论文因此自称 judge-labeled upper bounds；**其人工锚定的设计也值得抄**——Track A 让标注者回答固定子问题、**由回答推导出类别**"so the taxonomy cannot be imposed by the form"，Track B 让标注者在看到分类体系**之前**先自由描述，结果二元 83.3%/κ=0.43 而**完整类别标签只有 50.0%**、盲测表层标签 κ=0.80 且 12 条盲测描述全部落进已有类别）

方向：播报前动作门 — 第一个可读的实现形状，而且它是运行时不是 LLM 审阅者（当前问题：项目此前只有 SemaPLC 给出的三条门不变量，没有任何可读实现；**EnvHarness（2608.19880，Google Cloud AI Research）的 `envharness/harnesses/rules.py`（8,274 字节，Apache-2.0，已一手读过）**提供三个纯函数式逐步钩子 `filter_action` / `modify_transition` / `filter_observation`，**这是本项目找到的第一个"播报前确定性动作门"的真实形状，且正是 08-20 从 SemaPLC 得出的"唯一可能塞进时延预算的形状"**；四条可直接搬——**其一拒绝必须返回实时状态加理由，绝不能是光秃秃的拒绝**（返回带类型的 `Blocked(reason=...)`，拦截路径保持环境不变、重新观测、把当前状态连同理由交回；源码注释解释了为什么："A bare `[blocked]` Observation would strip env-critical `obs.data` … and **strand or crash format-specific policies on their next `act()`**"，座舱对应物是动作门拒绝工具调用时必须把当前车态与理由一起返回否则模型下一轮无法恢复）；**其二层次信任分离由结构强制**——`Rules` 碰不到初始状态（那是独立的 `Setup` 层）也碰不到评分（那是验证器），论文侧表述为 "Because all interventions remain external, **the ground-truth evaluation logic is preserved**"，**应作为项目 452 条判定的硬不变量：门可以拒绝或改写动作，但永不得触碰评分器**；**其三生成出来的门代码在隔离子进程里运行**"so a bad mutation becomes a **recorded trace** instead of a dead run"；**其四跨进程边界传的必须是数据不是句柄**，若项目为保时延预算把门放到进程外则直接适用；**另加 MaliciousSkillBench 的 `ABSTAIN_ERROR` 第三状态**："Technical failures … are **never silently converted to benign predictions**"，**即门自身出错绝不可退化成放行，且门的覆盖率必须与门的准确率分开报告**；可以尝试的是只读审阅 `rules.py` 并把这五条写进动作门设计文档，不运行该仓库任何代码）

方向：动作门选型 — 反对把它做成学出来的分类器（当前问题：项目尚未决定动作门是确定性校验还是学出来的分类器；2608.19901 给出的跨来源分布漂移结果是最强的外部论据——**一个学出来的文本门只能用 62.4% 的良性误报率去买 95.6% 的召回**（word-SVM 在 source-disjoint 协议下标掉 **340/545 条良性**而只漏 **37/839 条恶意**），作者结论是"**no detector occupies the desired high-recall, low-FPR regime on held-out sources**"；**同一篇还给出一个存在性证明：确定性方法的重复运行翻转率可以真正等于零**——种子 42/43/44、划分冻结，"**standard deviation 0 in the frozen result table**"，因为 TF-IDF 加线性模型本身就是确定性的，**即"重复运行不稳定"不是评测的固有属性而是方法选择的后果**；可以尝试的是把"播报前动作门必须是确定性的 schema 与车态校验"写死；**另两条纪律值得抄**——**证据分档门控**（把"标注强度"与"证据类型"做成两条正交轴、明写 evidence type must **not** be interpreted as an ordinal confidence score、并设硬门"仅有扫描器证据的记录永不进入主要真值"，据此仅凭证据类型就排除了 182,699 条中的 **169,676 条**；**并预先声明自己没用到的档位** `human`/`llm`/`unknown`，使 LLM 标注在表里可见而非被悄悄提升）、以及**作用在模型特征侧的泄漏扫描器**（审计高权重线性特征里是否出现来源 ID / 基准 ID / 数据集名标记，记录到零命中，且对结论作了正确限定"does **not** establish source invariance"）；**它还明确拒绝给单条误报/漏报打语义"错误成因"标签**——"Automated keyword summaries … have **no human-verified causal-annotation status**"，**这是日后有人提议 LLM 失效归因时可以引用的先例**）

方向：本轮第二篇完全干净的论文，以及一处被聚合许可证掩盖的风险（当前问题：干净的论文要明说才能校准其余各篇；2608.19901（南京大学 + 格里菲斯 + 南洋理工 + 维克森林 + 新南威尔士，**49 页**）逐项交叉核对全部通过——摘要 0.932/0.916/0.665 与 Table 2、README 三处逐位一致，95.6%/62.4% 与 340/545=62.39%、802/839=95.59% 精确对得上，9,740=7,505+2,235 成立；**它还主动交代了七处上游不一致而不是抹平**（例如 SRC005 的 README 报 350 行而固定修订版只有 347 行，"The three missing malicious records are **not reconstructed**"），并给出一条规矩"**'Paper-reported size' 与 'artifact-level samples available for a frozen evaluation snapshot' 是两个不同的量**"；**它对硬件的处理是本项目见过最好的**："The frozen run package does not record an exact host Python version, OS build, CPU model, RAM size, or GPU model, **so we do not invent those fields here**"；**它也是少有的把"少数类指标必须给召回"当成论文主张本身的一篇**，每个工作点都同时给出召回与良性误报率；**但许可证有一处实质风险必须点名**：仓库层面是 Apache-2.0 代码 + CC-BY-4.0 数据，而 `THIRD_PARTY_NOTICE.md` 写明第三方 Skill 制品 "**are not relicensed** … retain their respective upstream terms"，其 Table 6 里 **SRC001 为 academic-research-only、SRC006 为 terms not provided、SRC010 为 CC-BY-NC-SA-4.0**——**聚合的 CC-BY-4.0 只清理了基准自身的元数据层，对这三个来源什么都没清理**，这是本轮第二个"聚合许可证掩盖成分许可证"的例子，第一个是 MaAI 的逐模型拼盘；另有一处文件级缺口：附录 G.6 声称冻结包保留五类制品，而 GitHub 56 条目与 HF 18 文件里对 `joblib|seed|estimator|minilm|embedding` 的命中数**全部为零**，**五类里有四类两处都不存在**；**还有一处来源独立性问题论文没说明**：SRC002 就是 `ProtectSkills` 这个组织自己的 HF 数据集、与本论文发布方同一组织，而"author-provided historical artifact snapshots"这个措辞没有披露这层关系）

方向：开放状态与术语 — 本批次两条（当前问题：**StateMemBench 是追踪表上价值最高的第三档条目**——论文写 "We release StateMemBench" 且写 "The annotation kit (forms, keys, and the analyzer) is released with the benchmark"，而 **23 页全文没有任何 URL**：超链接注解只有一条 arXiv 自链接，全部五条裸 URL 均为第三方引用（`microsoft/STATE-Bench` 是**被引用的他人基准**、OpenAI 与微软博客、Qwen 博客、Instacart 数据源），故代码与数据均为第三档 unverifiable、许可证未知；**而那套标注工具包恰恰是本批次对项目最直接可用的制品**；**EnvHarness 则是"仓库存在 ≠ 论文声称的制品存在"的又一例**：论文 Table 4 报告了 ALFWorld 与 **WebShop** 两个 RL 结果，而 242 条仓库路径中 `webshop` 出现 **0 次**、README 也从不提及，**整个 WebShop 列没有任何已发布的桥接或实验**；另注其两处不一致——OfficeQA 的 F1 增量论文写 **+1.96** 而 README 写 **+1.97**（57.73−55.77=1.96，**README 是错的那个**），以及摘要说"为 RL 提供更优优化信号"而 Table 4 四项里**有一项退化**（ALFWorld OOD 88.8 对 89.6）**且该表全表无标准差**（正文诚实写了，属摘要层面省略）；**术语：`harness` 现在有五个义项**——在既有三义（训练脚手架 / 执行脚手架 / 普通动词）之外新增**（四）环境脚手架**（EnvHarness 自定义为 "a programmable layer that wraps an existing, static environment"）与**（五）可执行测试运行器制品**（MaliciousSkillBench 只在此窄义使用），**且 EnvHarness 一篇之内同时使用义项（二）与（四）并专设对照表说明，属有意为之而非混用**；StateMemBench 全文从不使用这个词）

方向：一条项目假设第三次未被回答（当前问题：08-19 从 2608.14036 推出的"缩小可达工具集能否提升准确率而不只是安全性"这条假设，08-20 已由 2608.18351 证明不适用，本轮 EnvHarness 是第三次尝试且同样不适用——**它是在训练期限制动作集**（Contract 的动作过滤器封掉捷径），**然后在标准的、不受限的实例上评测**，因此它证明的是"训练期限制能改善不受限的评测"而不是"推理期限制"；可以尝试的是把这条明确记为**第三次未获回答**，避免第四次再按同一形状误判；另注 EnvHarness 的 `skill` 是"检索进提示词的自然语言程序性记忆条目"（沿用 ReasoningBank 设计并说明了），**与 2608.14036 的可安装包义项不同**，而 MaliciousSkillBench 用的正是可安装包义项）

持续跟踪项（本轮无进展，逐条列出以免被"有 P0"掩盖）：判定器处理不变性检查（2608.12880，**优先级仍为最高**，与本轮无关且仍未完成）；X2Streaming-TTS 论文声明的仓库 `X-Square-Robot/X2Streaming-TTS` 是否出现（08-20 设的两周期限，本轮为第二天，未复核）；`X-Square-Robot/Qwen3TTS-Streaming` 是否出现提及论文的提交；离线复现 X2-Turn 的 EasyTurn-zh 判停指标（权重与代码仍是开放非门控 Apache-2.0，08-20 列为可立刻动手项，本轮未动）；测项目自身上游 token 速率与抖动（X2Streaming-TTS 的 ρ 论证在项目条件下仍未验证）；TensorRT/Triton 链路环境评估；`streaming-se-frontend-rtsemamba-kd1-v1` 的 PESQ→CER 阻塞（RT-SEMamba 仍须自测中文 CER）；PACE（2608.07631）仍是最强全双工命中；AutoDesign 附录 A.5 人评数据（第六次待核）；GCM-Bench 的 PACE 中间件、SemBridge、Smart-Glass-Challenge、flashdrive、CASA、`duplexgen/personaplex-finetune` 六个占位仓库；DuplexGen-B 承诺的 180 段基准；FireRedTTS3 是否补出任何时延/流式基准；Real-World AVSE 挑战赛数据是否公开。
