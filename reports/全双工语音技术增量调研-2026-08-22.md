# 全双工语音技术增量调研

更新日期：2026-08-22

对比基线：
- `reports/全双工语音技术增量调研-2026-08-21.md`

面向项目：
- StreamingModelHarness
- Cockpit Agent Radar
- 视觉、音频、文本与车控融合的智能座舱助手

## 本轮证据边界（先写清，后面所有数字都受它约束）

**项目侧无新增。** 项目快照仍是 2026-08-10 10:30 那一份，最新提交仍为 2026-08-04，最近一次 H20 A/B 仍是 2026-08-03。本轮没有新提交、没有新基准、没有新负面结果。

**Radar 连续第十轮未刷新。** `data/items.json` 的 `generated` 仍是 `2026-08-12T09:30:35+08:00`，条目仍为 499，新增 0 条。`data/explanations.json` 仍为 304 条。直接检索仍是唯一发现通道。

**索引本轮进入"冻结"相位，而不是"每天一批"。这是 08-15 之后第一次观测到冻结。** 08-21 报告记录当时全站最新投稿为 `2608.20340`（2026-08-20T17:59:59Z）。本轮同一查询（`all:a`，不限类别，按投稿时间倒序）返回的**仍然是 `2608.20340`（2026-08-20T17:59:59Z）**，前三条逐条相同（`2608.20340` / `2608.20339` / `2608.20337`）。五个对照总数与 08-21 **逐位相同**：`cat:cs.SD` **21,749**、`cat:eess.AS` **22,209**、`cat:cs.CL` **116,790**、`cat:cs.MM` **9,947**、`cat:cs.HC` **32,292**（`cs.HC` 为首次记录）。**索引在 26 小时内一篇未动。**

窗口按投稿时间取 **2026-08-20T18:00Z 至 2026-08-22T02:30Z**：起点严格衔接 08-21 报告实际覆盖到的最后一篇（投稿时刻 2026-08-20T17:59:59Z）之后的批次边界。本轮把查询组从 14 组扩到 **17 组**，**17 组全部 totalResults = 0**，去重后 **0 篇**。

**因此本轮属于"无重大新增"。这个结论经过三级证伪，不是"没查出来"：**

1. **每一个零结果组都做了去掉日期约束的对照**，全部返回大数，证明解析路径正常：8,751 / 952 / 847 / 726 / 152 / 3,967 / 6,501 / 7,429 / 7,388 / 2,432 / 2,049 / 2,461 / 6,920 / 25,672 / 3,306 / 21,152 / 2,551 / 16,336。
2. **全站最新投稿时刻已记录**（见上，与前一轮相同）。
3. **不限类别的窗口查询** `(all:a) AND submittedDate:[202608201800 TO 202608220230]` totalResults = **0**，即窗口内全站一篇都没有，不只是语音方向没有。
4. 另加第四道：`(cat:cs.SD OR cat:eess.AS) AND lastUpdatedDate:[202608201800 TO 202608220230]` totalResults = **0**，修订通道在语音类目上同样为空。

**下一轮窗口起点仍为 2026-08-20T18:00Z，不得前移。** 依据是既定规则"窗口起点定在**最后一篇实际被覆盖到的投稿时刻**，而不是上一轮的时钟时刻"——本轮实际覆盖到的投稿数为 0，因此起点不变。**把起点误改成 2026-08-22T02:30Z 会永久丢掉 08-21 与 08-22 两批（如果它们后来被回填的话）。**

**但"论文通道为零"不等于"本轮无产出"。** 本轮的产出全部来自另两条通道，且其中一条给出了一个影响此前多轮结论的方法学发现：

- **通道一（`submittedDate` 窗口查询）：零。** 见上。
- **通道二（追踪表逐版复核）：窗口内零新修订，连续第二轮为零；但本轮把追踪表从惯例的 47 个 ID 扩到 173 个，发现非 v1 条目从"已知 3 个"变成 20 个，即 17 个从未做过逐版差分。** 详见第 3 节。
- **通道三（制品反查）：进行中，结论以单独提交并入本报告。**
- **额外一条（本轮 P0）：对检索工具本身做了一次证伪实验，发现 arXiv `abs:` 字段做词干归并（stemming），据此 08-21 报告提出的查询改写方案被实测证明无效，且此前多轮记录的一整类"词根假阳性"由此获得统一机制解释。** 详见第 2 节。

制品复核时刻为 2026-08-22，晚于 02:30 的论文截止时刻。

## StreamingModelHarness 当前进展与问题

| 当前问题 | 项目证据 | 今日相关技术 | 可执行实验 | 成功门槛 |
|---|---|---|---|---|
| 首音频约 3 秒，瓶颈在外挂 Edge TTS | A/B 首音频 P50 2,918ms / 3,491ms，P90 3,453ms / 4,171ms，而首文本最快 131ms | **本轮无新增。** TTS 组本窗口 totalResults = 0。**另需修正一条口径**：TTS 组此前的假阳性并非"关键词跨领域"，而是 `abs:"TTS"` 被词干归并成 `tt`（见第 2 节），该组历史召回质量比此前认为的更差 | 沿用 08-20 的 X2Streaming-TTS 只读审阅计划；另把 TTS 组查询改写为只保留短语形式 | 首音频 P90≤1,000ms；中文 CER 不劣于 Edge |
| 判停/打断"该不该发生"缺判据来源 | `probe_turn_end` 已实测主模型自判停不可行（负面结果） | **论文侧本轮零。** 判停组（VAD/endpointing）本窗口 0；`cat:cs.HC` 组本窗口 0。**但 08-21 建议的判停查询改写被实测证否**——改写后与改写前在同一窗口返回**完全相同的 22 篇**，正确改法见第 2 节 | 08-21 立项的 MaAI 旁路观测（`vap_bc_ch`，MIT，纯 CPU）**本轮未动，仍是待办**；先按第 2 节的正确改法修好判停组查询，再谈召回 | 判停信号完全走音频通路之外的旁路；应答频次设上限而非越多越好 |
| 判定器处理不变性未验证（**优先级仍最高**） | 2608.12880：只翻转与行为无关的元数据即改变 180 条中的 31 条判定 | **本轮无新论文证据，且本项本身仍未开工。** 08-21 已把 StateMemBench Table 8 的四格转移表定为第一步样板，本轮无任何进展 | 照搬四格转移表（选一个与行为无关的拨杆、判定器固定、报四个转移计数而非那个差值），零 GPU | 聚合通过率旁必须并列逐条翻转率；判定器最多只能索取二元判定 |
| 工具/动作通道缺"播报前动作门" | 工具名严格召回 80.49%，无泄漏扫描器、无重复运行翻转率 | **本轮无新增。** EnvHarness `rules.py` 的只读审阅仍未做 | 只读审阅 `rules.py`（8,274 字节，Apache-2.0），把四条规则与 `ABSTAIN_ERROR` 写进动作门设计文档 | 拒绝必须返回实时车态加理由；门永不得触碰评分器；门错误永不等于放行 |
| 回声消除与扬声器泄漏零一手依据 | 项目从未获得任何 AEC 正文证据 | **连续第十轮为零。** 本窗口该组 totalResults = **0**。**另新增一条与该组直接相关的口径修正**：`abs:"beamforming"`、`abs:"beamform"`、`abs:"beamformer"` 三者 totalResults **同为 6,618**，即该词的历史噪声是词干归并的结果；而 `abs:"AEC"` 全库仅 **259**，`abs:"echo cancellation"` 仅 **142**，**这两个才是该方向真正的可用探针** | 不写任何 AEC 结论；把该组查询改成以 `abs:"echo cancellation"` 与 `abs:"AEC"` 为主、去掉 `beamforming` | 需要一篇因果、可流式、带下游指标、且含自身播放参考抽头的一手论文 |
| 多乘员插话、"谁在说话"无任何一手依据 | 项目评测集为单说话人合成音频 | **本轮零。** 说话人组本窗口 0。`abs:"VAD"` 与 `abs:"VADs"` 同为 504，该探针无词干污染，可信 | 沿用架构约束（身份判定走旁路） | 判定信号与音频通路解耦；多乘员需另找证据 |
| MoE 解码 110ms/token 与首包 prefill | 每 token 约 816 次 GPU→CPU 同步 | **通道二发现一条此前漏掉的直接相关修订**：`2608.15018` S2-MoE《Enabling Efficient Self-Speculative Decoding for Mixture-of-Experts》**已于 2026-08-19T09:26:35Z 出到 v2 而从未被逐版差分**，其主题正对口本项目的 MoE 解码 | 已委派该篇的 v1→v2 差分与全文首读，结论以单独提交并入 | 任何 prefill 收益必须在冷首次请求下复测；批大小四态必须先判准再谈否决 |

## 一、先说结论

### P0：arXiv `abs:` 做词干归并——08-21 提出的查询改写实测无效，且此前多轮的"词根假阳性"由此获得统一解释

**这一条的性质与以往任何一条都不同：它不是一篇论文，而是对本项目唯一发现通道所依赖的检索工具本身做的一次证伪实验。** 论文通道为零的这一天，正好是做这件事的时机——因为窗口内没有新论文，可以把 08-21 的完整窗口原样重放，做严格的 A/B。

#### 1.1 直接证伪：08-21 提出的改写方案返回完全相同的 22 篇

08-21 报告第 6.7 节记录了一处查询缺陷：`abs:"voice activity detection" OR abs:"endpoint" OR abs:"endpointing"` 在其窗口返回 22 篇、**22 篇全部与语音无关**（泛函分析的 "endpoint estimates"、Tor 网站指纹、心脏形状补全、全息对偶等）。该报告的诊断是"**根因是裸词 `endpoint` 在数学与安全领域都是高频固定术语**"，并建议"**下一轮改写为 `abs:"voice activity detection" OR abs:"endpointing" OR abs:"end-of-turn" OR abs:"turn-end detection"`，即删去裸词 `endpoint`**"。

**本轮把 08-21 的窗口（`submittedDate:[202608191800 TO 202608210230]`）原样重放，对旧写法与新写法各跑一次：**

| 写法 | 该窗口 totalResults | 返回的 ID 集合 |
|---|---|---|
| 旧写法（含裸词 `endpoint`） | **22** | 2608.20295, 2608.20283, 2608.20266, 2608.20243, 2608.20216, 2608.20160, 2608.20152, 2608.19938, 2608.19932, 2608.19846, 2608.19845, 2608.19708, 2608.19703, 2608.19679, 2608.19593, 2608.19553, 2608.19540, 2608.19497, 2608.19403, 2608.19387, 2608.19373, 2608.19369 |
| 08-21 建议的新写法（删去裸词 `endpoint`） | **22** | **与上一行逐条完全相同** |

**改写没有去掉任何一篇假阳性。** 全库口径下改写还略微更差：旧写法 **7,388**，08-21 建议的新写法 **7,429**（新写法多出的 41 来自新加的 `end-of-turn`，而删掉 `endpoint` 一个都没减）。

#### 1.2 机制：`abs:` 对单词做词干归并，五个词族各自坍缩成同一个数

逐词测全库 totalResults，**同一词族的所有变体返回完全相同的总数，且按投稿时间倒序的最新三条 ID 也逐条相同**：

| 词族 | 各变体的全库 totalResults | 最新三条 ID（各变体相同） |
|---|---|---|
| `endpoint` / `endpointing` / `endpoints` | **7,145 / 7,145 / 7,145** | 2608.20295, 2608.20283, 2608.20266 |
| `interruption` / `interrupt` / `interrupted` / `interrupting` | **2,491 / 2,491 / 2,491 / 2,491** | 2608.19893, 2608.19887, 2608.19622 |
| `stream` / `streaming` / `streamed` | **30,701 / 30,701 / 30,701** | 2608.20334, 2608.20290, 2608.20280 |
| `beamforming` / `beamform` / `beamformer` | **6,618 / 6,618 / 6,618** | 2608.20278, 2608.19102, 2608.18458 |
| `cancellation` / `cancel` / `cancelling` | **15,005 / 15,005 / 15,005** | 2608.20298, 2608.20296, 2608.19414 |

**结论（我的判断，基于上述实测）：`abs:` 在匹配前把单词归并到词干，因此 `endpointing` 与裸词 `endpoint` 在检索层面是同一个词，删掉哪一个都不改变结果集。08-21 的诊断（词义歧义）方向对了一半——那 22 篇确实是词义问题，但可操作的根因是词干归并，因此按"删掉裸词"这个思路做的改写必然无效。**

**多词短语不会被整体坍缩，但短语内部的成分词同样被归并：**

| 探针 | 全库 totalResults | 说明 |
|---|---|---|
| `abs:"voice activity detection"` | **246** | 短语邻接约束确实生效 |
| `abs:"activity detection"` | **718** | 与上一行不同 ⇒ 短语匹配不是简单的词 AND |
| `abs:"echo cancellation"` | **142** | |
| `abs:"echo cancel"` | **142** | **与上一行相同 ⇒ 短语内成分词也被归并** |
| `abs:"end-of-turn"` | **42** | |
| `abs:"turn-end detection"` | **0** | **全库为零，见 1.4** |

#### 1.3 缩略词同样被归并，且这解释了"记录以来跨度最大的一次假阳性"

08-21 报告记录 TTS 组 3 篇中 2 篇是跨领域假阳性，其中 `2608.19651`（顶夸克 philic 第三类双希格斯二重态模型中的电弱重子生成，**hep-ph**）被称为"本项目记录到的跨度最大的一次关键词假阳性"。

**本轮查清了机制，而且它根本不是"关键词"假阳性：**

| 探针 | 全库 totalResults |
|---|---|
| `abs:"TTS"` | **4,956** |
| `abs:"tts"` | **4,956** |
| `abs:"TT"` | **4,956** |
| `abs:"tt"` | **4,956** |
| `abs:"ttses"` | 1 |

**`abs:"TTS"` 与 `abs:"tt"` 是同一个查询。** 词干器把缩略词 `TTS` 当成 `TT` 的复数，剥掉尾部 `S`，而 `tt` 正是粒子物理里顶夸克对的记法。该论文标题即为 "... motivated by the $t\bar{t}$ Excess at the LHC"。另一篇 `2608.19789` 的标题是 "**TT-net**: Quantum Inspired Tensor Network Denoising in Conditional GANs"，同一机制。

**并且我核实了两篇的摘要全文：`speech` / `synthes` / `text-to` / `TTS` 四个模式的命中数全部为 0。** 也就是说这两篇从来不是"摘要里出现了某个语音词"，它们与语音检索的唯一联系就是被剥掉的那个字母 S。**"跨度最大的关键词假阳性"这个说法应当撤回，改记为"缩略词词干归并导致的伪匹配"。**

其余缩略词探针的归并情况（用于判断哪些探针可信）：

| 探针 | 全库 totalResults | 是否受污染（我的判断） |
|---|---|---|
| `abs:"ASR"` / `abs:"ASRs"` | 5,164 / 5,164 | 归并，但两形态都指 ASR，**可用** |
| `abs:"VAD"` / `abs:"VADs"` | 504 / 504 | 归并，两形态都指 VAD，**可用** |
| `abs:"AEC"` | 259 | 无尾部 S 可剥，**干净，是该方向最好的探针** |
| `abs:"KV cache"` / `abs:"KV caches"` | 1,730 / 1,730 | 归并，**可用** |
| `abs:"TTS"` | 4,956 | **等于 `tt`，严重污染，应删除** |

#### 1.4 附带发现：08-21 建议的写法里有一个永远不可能命中的词

`abs:"turn-end detection"` 的**全库** totalResults = **0**。这不是窗口内为零，是 arXiv 全库没有任何一篇摘要能匹配它。**08-21 建议的改写里包含了一个按构造无法命中任何东西的词。** 保留它无害但也无用，会让该组看起来比实际覆盖得更宽。

#### 1.5 正确的改写，以及它的量级

| 写法 | 全库 totalResults |
|---|---|
| 原写法 `VAD OR endpoint OR endpointing` | **7,388** |
| 08-21 建议 `VAD OR endpointing OR end-of-turn OR turn-end detection` | **7,429**（比原写法更差） |
| **本轮建议**：`abs:"voice activity detection" OR abs:"end-of-turn" OR abs:"turn-end detection"`（**两个被归并的词都删掉**） | **288** |

**7,388 → 288，减少 25.6 倍**，而保留的三个短语里 246 与 42 都是真正的判停语义探针。**这一条可以立刻改，零成本，且下一轮必须照旧记录改写前后的对照总数——否则口径变化会被误读成产出变化。**

#### 1.6 最重要的一条限定：归并的方向是"多召回"，不是"漏召回"

**必须把这条写清楚，否则本节会被过度解读。** 词干归并让一个查询匹配到**更多**文档，从来不会让它匹配到更少。因此：

- **此前所有记录为 `totalResults = 0` 的零结果，全部仍然有效。** 一个被人为放宽了的查询若仍返回零，那个零比原本更强，不是更弱。08-13 以来 AEC 的十轮为零、空间听觉的连续为零、流式 ASR 的连续为零，都不受本节影响。
- **受影响的是精度，不是召回。** 此前记录的"某组返回 N 篇、N 篇全是假阳性"这一整类观察，现在有了机制解释。
- **因此"全双工组存在系统性盲区"这个结论只有一半被解释。** 该组的**噪声**来源清楚了（`interruption` 一词就带来 2,491 篇全库量级的词干污染，且 08-21 该组返回的 3 篇假阳性正是 `abs:"interrupt"` 族最新的 3 篇：2608.19893、2608.19887、2608.19622，逐条吻合）。**但"漏掉真正的全双工进展"这件事，词干归并解释不了**——归并只会多给，不会少给。**两个缺陷必须继续分开记录：精度损失已归因，召回损失仍未归因。**
- 同理，`interrupt` 词根的三个假阳性（并行机排程、统计学 "Interrupted Time Series"、`2608.19893` "Interrupting the Loop"）**应从"三次巧合"改记为"一个机制的三次显形"**。

### P1：通道二的覆盖面此前被系统性低估——追踪表从 47 个 ID 扩到 173 个后，非 v1 条目从 3 个变成 20 个

**窗口内的结论没变：零新修订，连续第二轮为零。** 但本轮换了做法：不再用惯例的 47 个 ID，而是把 `reports/` 里出现过的**全部 173 个 arXiv ID** 一次性扫完（4 批，每批 ≤45，`?id_list=`，173 个全部解析成功、0 个缺失）。

结果：

- **窗口内（2026-08-20T18:00Z 起）修订数 = 0。** 与通道一的 `lastUpdatedDate` 查询（同为 0）互为独立印证。**这一条必须明写，否则"版本通道无产出"会被误读成"版本通道没查"。**
- **但非 v1 条目共 20 个，而 08-21 记录的是 3 个。** 已完成差分的仍是那 3 个（`2608.13416` StreamTTT 于 08-19、`2608.14097` Ambisonics 于 08-19、`2608.10878` X2-Turn 于 08-20）。**其余 17 个从未做过任何逐版差分。**

从未差分的 17 个，按对项目的相关度排序（相关度是我的判断，版本与时刻是实测）：

| arXiv ID | 版本 | 最后修订时刻 | 题名（截断） | 相关性（我的判断） |
|---|---|---|---|---|
| `2608.15018` | v2 | 2026-08-19T09:26:35Z | S2-MoE: Enabling Efficient Self-Speculative Decoding for MoE | **直接对口 MoE 解码 110ms/token** |
| `2608.06718` | v2 | 2026-08-12T18:57:42Z | Do Audio Language Models Use Paralinguistic Evidence? | 直接对口判定器构造效度 |
| `2607.06540` | v4 | 2026-08-10T08:03:44Z | Hierarchical Acoustic-Semantic Modeling: Modality Separation | 声学/语义分离，对口首音频链路 |
| `2608.07346` | v2 | 2026-08-10T09:34:51Z | $A^2E$: An End-to-End Agent Auditing Engine | 对口判定链路 |
| `2608.09096` | v2 | 2026-08-11T02:53:55Z | Evo-Bench: Can Language Models Improve Agent Harness? | harness 义项（二） |
| `2604.00688` | v3 | 2026-04-21T12:14:57Z | OmniVoice: Omnilingual Zero-Shot Text-to-Speech | TTS |
| `2608.12951` | v2 | 2026-08-17T05:54:53Z | VoxAudio: Vocalized Audio Synthesis via Multi-Reward AR | 音频合成 |
| `2602.06036` | v2 | 2026-05-28T09:13:25Z | DFlash: Block Diffusion for Flash Speculative Decoding | 投机解码 |
| `2608.02508` | v3 | 2026-08-10T04:22:00Z | RoMeRL: Feedback Coverage / Memory-Reward Tradeoff | Agent 训练 |
| `2608.03571` | v2 | 2026-08-06T02:46:18Z | Beyond Simply Environment Scaling | harness 义项（一） |
| `2608.08311` | v2 | 2026-08-11T09:39:00Z | Ouroboros: Self-Developing Frontier Coding Agent | Agent |
| `2608.09448` | v2 | 2026-08-12T10:16:02Z | VANE: Reliable Test-Time Training for VLA | 测试时训练 |
| `2608.14546` | v2 | 2026-08-18T07:27:55Z | CPI-Bench | 评测 |
| `2603.22120` | v2 | 2026-03-26T11:06:17Z | StreamingClaw Technical Report | 流式 |
| `2604.26904` | v3 | 2026-05-16T05:33:51Z | ClawGym | Agent 框架 |
| `2608.14513` | v2 | **2026-08-19T18:36:03Z** | Visualizing Uncertainty in Non-linear Projections | **无关（可视化）** |
| `2608.16790` | v2 | 2026-08-18T03:29:44Z | Rank-Aware Element Grouping for Multiuser ISAC | 无关（通信） |

**其中一条构成对 08-21 报告的一处必须写明的修正。** `2608.14513` 的 v2 修订时刻是 **2026-08-19T18:36:03Z**，**落在 08-21 报告自己的窗口（2026-08-19T18:00Z 至 2026-08-21T02:30Z）之内**。08-21 报告写"没有任何追踪项在本窗口产生新修订"，**该陈述对它实际扫过的那 47 个 ID 成立，但对本项目追踪过的全部 ID 不成立**。

**同时要把这条修正的实际份量说准，不要夸大：`2608.14513` 是一篇非线性投影不确定性可视化的论文，与本项目任何方向无关，因此 08-21 的实质结论（版本通道本轮无有价值产出）没有被推翻，被推翻的是它的覆盖面陈述。** 这两件事必须分开说。

**教训（我的判断）：通道二此前被"追踪表"这个词悄悄限制成了"人工挑出来的 47 条"，而通道二的定义本应是"曾经进入过报告的全部条目"。这与 08-21 记录的 MaAI 失败是同一形状——不是通道失效，是通道的覆盖范围被无声地缩小了。** 固化规则：**逐版复核的输入集合必须由 `reports/` 机械提取，不得人工挑选**；提取命令与本轮所用完全一致，`grep -ohaE '\b26[0-9]{2}\.[0-9]{5}\b' reports/*.md | sort -u`，173 个 ID 一次扫完耗时约 28 秒，成本不构成理由。

### 本轮无 P0 级技术候选，且这一条要说清楚

**本轮不新增任何工程候选。** 论文通道为零，通道二窗口内为零。上面两条 P0/P1 都是**方法学与流程发现**，不是可以进候选台账的技术。**08-21 立项的那个候选（`backchannel-timing-sidecar-maai-offline-v1`，用 MaAI 的 MIT 中文应答权重做纯 CPU 旁路观测）本轮没有任何进展，仍然阻塞在同一个前置条件上：项目缺带应答/打断标注的中文评测数据。**

## 二、逐条对比 2026-08-21

| 项目 | 08-21 | 08-22（本轮） | 变化性质 |
|---|---|---|---|
| Radar 刷新 | 连续第九轮未刷新 | **连续第十轮未刷新**，`generated` 与条目数逐位相同 | 无变化 |
| 索引相位 | 每天一批（连续第五次印证） | **冻结**：全站最新投稿仍是 `2608.20340`，26 小时零推进 | **相位改变**，08-15 后首次 |
| 窗口产出 | 14 组去重 78 篇 | **17 组去重 0 篇** | 从中等产出到零 |
| 语音类目增量 | cs.SD +9、eess.AS +7（记录最小之一） | **cs.SD +0、eess.AS +0** | 新的最小值 |
| 通道一 | 4 组为零，仍出 P0 | **17 组全零**，三级证伪 + `lastUpdatedDate` 第四道 | 零，且证伪更严 |
| 通道二 | 47 ID，3 个非 v1，窗口内 0 修订（首次全空） | **173 ID，20 个非 v1，窗口内 0 修订（连续第二轮）**；发现 17 个从未差分；并修正 08-21 的覆盖面陈述 | **同结论、更强覆盖，且发现一处覆盖缺口** |
| 通道三 | MaAI（完全不在追踪表内的项目） | 进行中，以单独提交并入 | 待补 |
| 判停组查询 | 诊断为裸词 `endpoint` 过宽，建议删该词 | **该建议被实测证否**（同窗口返回完全相同的 22 篇）；根因是词干归并；给出 288 篇的正确改法 | **推翻前一日的诊断与建议** |
| TTS 组假阳性 | 记为"跨度最大的关键词假阳性"（hep-ph） | **改记为缩略词词干归并伪匹配**：`abs:"TTS"` = `abs:"tt"` = 4,956，两篇摘要的语音词命中数为 0 | **改写前一日的归因** |
| `interrupt` 词根假阳性 | 记为第三个独立来源（共三次） | **改记为一个机制的三次显形**；08-21 该组的 3 篇假阳性正是 `abs:"interrupt"` 族最新 3 篇，逐条吻合 | **合并归因** |
| AEC | 连续第九轮零，组内 1 篇非声学 | **连续第十轮零**，组内 0 篇；另查明 `beamforming` 的噪声是词干归并，`AEC`(259) 与 `echo cancellation`(142) 才是干净探针 | 零延续 + 探针改进 |
| 项目最高优先级项 | 判定器处理不变性检查（未开工） | **仍未开工，仍为最高优先级** | 无变化 |
| main 状态 | 停在 08-10（第九轮），本轮恢复 08-11..08-20 | **仍停在 08-10（第十轮）**，本轮恢复 08-11..08-21 共 18 份报告与 9 份台账 | 同一故障第十次复发 |

## 三、可执行动作与优先级

### 第一阶段（零 GPU、零成本，本轮新增两条排在最前）

1. **立刻改判停组查询为 `abs:"voice activity detection" OR abs:"end-of-turn" OR abs:"turn-end detection"`**，并在下一轮报告中记录改写前后的窗口对照总数。依据：第 1.5 节，全库 7,388 → 288。**这是本轮唯一一条改完就见效、且已被实测验证的动作。**
2. **改 TTS 组查询：删掉 `abs:"TTS"`**，只保留 `abs:"text-to-speech"`（2,316）与 `abs:"speech synthesis"`（1,204）。依据：第 1.3 节，`abs:"TTS"` 在检索层面等于 `abs:"tt"`。
3. **改 AEC 组查询：删掉 `abs:"beamforming"`，改以 `abs:"echo cancellation"`（142）与 `abs:"AEC"`（259）为主。** 依据：第 1.2、1.3 节。**同时必须记住这不会改变"AEC 十轮为零"这个结论**——归并只多召回不少召回，去掉噪声词只会让该组更干净，不会变出新论文。
4. **把逐版复核的输入集合改成从 `reports/` 机械提取**，不再人工维护 47 条清单。依据：第 P1 节。
5. **判定器处理不变性检查**，仍是全部事项中优先级最高的一条，**连续第四轮列为待办且连续第四轮未开工**。第一步仍是照搬 StateMemBench Table 8 的四格转移表：选一个与行为无关的拨杆、判定器固定不变、报四个转移计数而不是那个差值。
6. **只读审阅 EnvHarness 的 `envharness/harnesses/rules.py`**（8,274 字节，Apache-2.0），把动作门四条规则与 `ABSTAIN_ERROR` 第三状态写进设计文档。08-21 立项，本轮未动。
7. **解码可复现性自测**（同一音频、同一 seed、连续 N 次、逐 token 比较），依据 CoRun（2608.14376），本轮无新证据。
8. **`streaming-se-frontend-rtsemamba-kd1-v1` 的 clean/20dB/音色分桶归因**，连续第五轮列为待办；验收口径仍为只以项目自测中文 CER 为准，PESQ 不得作为收益依据。

### 第二阶段（只读审阅，不执行任何论文代码）

9. **`2608.15018` S2-MoE 的 v1→v2 差分与全文首读**（已委派，本轮以单独提交并入）。这是通道二扩表后浮出的最相关一条。
10. **`2608.06718` v2 与 `2607.06540` v4 的差分**（已委派）。
11. **MaAI 中文应答权重 `vap_bc_ch` 的纯 CPU 旁路观测**：仍卡在"项目缺带应答/打断标注的中文评测数据"这个前置条件上，**且该前置条件本轮无进展**。
12. **离线复现 X2-Turn 的 EasyTurn-zh 判停指标**（Apache-2.0、非门控权重，08-20 起列为可立刻动手项，连续第三轮未动）。
13. **测项目自身的上游 token 速率与抖动**（08-20 既定项，未动）。

### 第三阶段（需要环境或授权评估）

14. **若中文 VAD 的串音归属能力被判定为必需，需就 `vad_ch` 的 CC-BY-NC-ND-4.0 与作者接洽授权。** 授权落定之前不得把 `vad_ch` 写进任何架构图。
15. **评估 TensorRT/Triton 链路是否可接受**（08-20 既定项，未动）。
16. **跟踪 `X-Square-Robot/X2Streaming-TTS` 是否出现**（08-20 设的两周期限，**到期日 2026-09-02，本轮为第三天**）。

## 四、本轮负面结果与零结果（与正面结果同等重要）

**这些是"查了、没有"，不是"没查"。每一条都记录了查询与 totalResults，且每一条都配了去掉日期约束的对照数以证明解析路径正常。**

窗口 `submittedDate:[202608201800 TO 202608220230]`，17 组全部为零：

| 组 | 查询 | 窗口 | 去日期约束对照 |
|---|---|---|---|
| 回声消除/增强 | `speech enhancement OR noise suppression OR echo cancellation OR beamforming` | **0** | 8,751 |
| 语音语言模型/S2S | `speech language model OR spoken dialogue OR speech-to-speech` | **0** | 952 |
| 流式 ASR | `speech recognition AND (streaming OR online OR latency)` | **0** | 847 |
| 空间听觉 | `spatial audio OR ambisonic OR binaural OR sound source localization` | **0** | 726 |
| 音频评测 | `speech evaluation OR audio benchmark OR MOS prediction` | **0** | 152 |
| 全双工/判停 | `full duplex OR turn-taking OR barge-in OR interruption` | **0** | 3,967 |
| TTS | `text-to-speech OR speech synthesis OR TTS` | **0** | 6,501 |
| 判停（新写法） | `voice activity detection OR endpointing OR end-of-turn OR turn-end detection` | **0** | 7,429 |
| 判停（旧写法） | `voice activity detection OR endpoint OR endpointing` | **0** | 7,388 |
| 车内 | `(in-vehicle OR cockpit OR driver) AND (speech OR voice OR dialogue OR assistant)` | **0** | 2,432 |
| 说话人 | `speaker diarization OR target speaker OR speaker verification` | **0** | 2,049 |
| KV/投机解码 | `KV cache OR key-value cache OR speculative decoding` | **0** | 2,461 |
| 工具/Agent | `tool use OR function calling OR agent harness OR tool calling` | **0** | 6,920 |
| 语音类目 | `cat:cs.SD OR cat:eess.AS` | **0** | 25,672 |
| cs.HC / cs.OS（第 14 组） | `(cat:cs.HC OR cat:cs.OS) AND (speech OR voice OR audio OR latency)` | **0** | 3,306 |
| cs.CL 语音 | `cat:cs.CL AND (speech OR audio OR voice OR duplex OR agent)` | **0** | 21,152 |
| 流式多模态 | `streaming AND (speech OR audio OR multimodal)` | **0** | 2,551 |
| 多媒体类目 | `cat:cs.MM OR (cat:cs.AI AND (speech OR audio OR voice agent))` | **0** | 16,336 |

**关于第 14 组的一条限定：** 它连续两轮独家命中当轮最重要线索（08-20、08-21），本轮为零。**但本轮全站窗口都是零，所以这不能算该组的表现下滑，只能算窗口本身为空。保留该组的决定不受影响。**

**关于"17 组全零"这件事的正确读法（我的判断）：** 08-21 记下过一条规则——"篇数与查询组的空满程度都不是'有无重大新增'的代理"。本轮不构成对该规则的反例，因为本轮的零**不是语音方向没产出，而是索引一篇都没进**（不限类别的窗口查询也是 0）。**"方向为零"与"索引为零"必须分开记：本轮是后者。这是本项目第二次遇到后者（第一次是 08-15）。**

## 五、持续跟踪项（本轮无进展，逐条列出以免被"有 P0"掩盖）

- **判定器处理不变性检查（`2608.12880`）——优先级仍为全部事项中最高，连续第四轮未开工。**
- `2608.19652` StateMemBench：追踪表上价值最高的第三档条目（论文称已发布基准与标注工具包，23 页全文无任何 URL）；其标注工具包仍是最直接可用而不可得的制品。
- `2608.19741` Thinkingbox：507 任务基准是否落地（第七档"承诺两件、交付一件"）。
- `2608.19662` ReCache：是否出现 LICENSE（公开但无许可证）。
- `2608.19880` EnvHarness：WebShop 桥接是否出现。
- `2608.19901` MaliciousSkillBench：附录 G.6 缺失的四类制品是否出现。
- `2608.19557`：是否出现 LICENSE。
- `X-Square-Robot/X2Streaming-TTS`：论文声明的仓库是否出现（**到期日 2026-09-02，本轮第三天**）。
- `X-Square-Robot/Qwen3TTS-Streaming`：是否出现提及论文的提交。
- `MaAI-Kyoto/MaAI`：中文 `vad_ch` / `bc_det_ch` 是否移出 CC-BY-NC-ND-4.0；`readme/vap_bc.md` 里"目前仅支持日语"这句陈旧说明是否修正；**是否首次出现任何可核实的精度数字**。
- 离线复现 X2-Turn 的 EasyTurn-zh 判停指标（权重与代码为非门控 Apache-2.0，连续第三轮未动）。
- 测项目自身上游 token 速率与抖动（X2Streaming-TTS 的 ρ 论证在项目条件下仍未验证）。
- TensorRT/Triton 链路环境评估。
- `streaming-se-frontend-rtsemamba-kd1-v1` 的 PESQ→CER 阻塞（RT-SEMamba 仍须自测中文 CER）。
- PACE（`2608.07631`）仍是最强全双工命中。
- AutoDesign 附录 A.5 人评数据（第七次待核）。
- GCM-Bench 的 PACE 中间件、SemBridge、Smart-Glass-Challenge、flashdrive、CASA、`duplexgen/personaplex-finetune` 六个占位仓库。
- DuplexGen-B 承诺的 180 段基准。
- FireRedTTS3 是否补出任何时延/流式基准。
- Real-World AVSE 挑战赛数据是否公开。
- **本轮新增：17 个从未做过逐版差分的非 v1 条目**（清单见第 P1 节），其中 `2608.15018`、`2608.06718`、`2607.06540` 已委派。

## 六、方法学固化（本轮新增，建议写入长期规则）

1. **`abs:` 做词干归并。** 任何单词探针实际匹配的是它的词干，因此：不要用"删掉某个宽泛单词"来提精度（无效）；缩略词若以 S 结尾会被剥成另一个词（`TTS`→`tt`）；短语内的成分词同样被归并（`echo cancellation` = `echo cancel`）。**提精度的唯一有效手段是改用多词短语。**
2. **归并只会多召回，不会少召回。因此所有历史零结果依然有效，受影响的只有精度。** 不得用本发现去回溯质疑任何一条已记录的零结果。
3. **每次改写查询都必须做同窗口 A/B 并记录两个总数。** 本轮正是靠 A/B 才发现 08-21 的建议无效——若只换写法不做对照，会把"改写生效"当成既成事实带进后续若干轮。
4. **提出一个新查询词时，先测它的全库 totalResults。** `abs:"turn-end detection"` = 0 说明它永远不会命中；一个全库为零的词进了查询组，会让该组看起来比实际覆盖得更宽。
5. **逐版复核的输入集合必须机械提取，不得人工维护。** 依据是本轮 47→173 的对比。
6. **窗口起点绑定"最后一篇实际覆盖到的投稿时刻"，而不是上一轮的时钟时刻。** 本轮覆盖 0 篇，所以起点不前移。**这是索引冻结日必须显式处理的一步，否则会永久丢批。**
7. **区分"方向为零"与"索引为零"。** 前者是领域没产出，后者是检索源没推进；两者的后续动作完全不同（前者继续等，后者下一轮必须把窗口起点保持不动并预期回填）。
