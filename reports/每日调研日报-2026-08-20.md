# 每日调研日报｜2026-08-20

证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、最近 H20 A/B 2026-08-03，本轮无新提交与新基准）。**Radar 连续第八轮未刷新**：`data/items.json` 的 `generated` 仍是 2026-08-12 09:30、条目仍 499，新增 0 条。**上一轮的四批次大回补之后索引恢复常态**：08-19 报告记录全站最新投稿为 2608.18072（2026-08-18T17:57:08Z），本轮同一查询返回 **2608.19197（2026-08-19T17:58:56Z）**，只新增 08-19 一个批次，截止时刻仍落在 17:5x 的 arXiv 每日截稿边界上（连续第四次印证索引按整批推进）。窗口按投稿时间取 **2026-08-18 18:00 UTC 至 2026-08-20 02:30 UTC**，与 08-19 报告实际覆盖到的最后一篇严格衔接、不重叠，14 组查询去重后 **50 篇**（对照：`cat:cs.SD` totalResults 21,740、`cat:eess.AS` 22,202）。**本轮 50 篇对上轮 235 篇，但信号密度更高**：出了近若干轮质量最高的一条 P0，外加一项追踪项的开放状态翻转。另：main 此前只到 2026-08-10，08-11 至 08-19 七轮报告已在本轮先恢复到主线。制品复核时刻为 2026-08-20。

方向：流式 TTS（本轮 P0） — X2Streaming-TTS（当前问题：项目首音频 P50 2,918/3,491ms、P90 3,453/4,171ms，瓶颈是外挂 Edge TTS 整句合成，而首文本最快只要 131ms；2608.18661 是**严格零前瞻的 token 级流式 TTS**，上游每吐一个 text token 就可在不看任何未来 token 的前提下决定是否发声，靠"因果提交"压住发音未定的跨度、靠"语音状态继承"跨段搬运完整 Code2Wav 状态，SEED-TTS-Eval 中文 CER **0.78%**、MiniMax 中文 CER **0.78%**，单请求 TTFT 中位数 **15.8ms**、128 并发 260.8ms，**测量硬件就是一张 RTX 5090，与项目环境同级**，且正文称与离线参考"使用同一离线骨干与完全相同的模型权重"、全文无训练章节；可以尝试的是**先借它的三条"有界纪律"而不是三个数字**——固定继承长度 H=4、注入增益夹在 0.0075–0.015、状态健康检查一票清零；**但四条边界必须写死**：TTFT 是首个音频 *token* 不是首音频到达听者、论文正文给出的仓库地址 `X-Square-Robot/X2Streaming-TTS` **本轮复核为 404**、两个自建评测集**均未命名未发布**故 Table 2/3/4 第三方不可复现、以及论文全部论证建立在"声学膨胀比 ρ 足以掩盖文本到达"之上而**项目上游是 110ms/token 的 MoE、比论文的固定速率喂入慢得多，该前提在项目条件下未经验证**）

方向：判停与打断（本轮并列 P0，是状态翻转不是新论文） — X2-Turn v2（当前问题：`probe_turn_end` 已实测主模型自判停不可行，判停"该不该发生"一直缺可对照的外部实现；2608.10878 本轮升到 v2，**v1→v2 逐页比对是一次只改两行的修订**——5 页对 5 页、第 2/3/4/5 页字符数完全一致、标题一致、所有实验数字一个都没变，实质变化只有摘要末尾新加的一句 "Code and models are available at https://github.com/X-Square-Robot/X2-Turn"；一手核实结果：代码仓库存在、**Apache-2.0**、148 条目 / 107 blob / 669,029 字节，权重 `x-square-robot/X2-Turn-4B-0812` 存在、**Apache-2.0、非门控**、11 文件合计 10,485,013,748 字节、语言标签 `zh, en`、`base_model` 为 Voxtral-Mini-4B-Realtime-2602，仓库另附含帧级 turn controller 的 full-duplex demo；**这是追踪表上第一个"中文可用 + 代码开放 + 权重开放 + 许可证宽松 + 非门控"五项齐全的判停制品**；可以尝试的是立刻离线复现 EasyTurn-zh 判停指标再跑项目自己的 452 条基准音频，**这是本轮唯一一项无需任何申请或谈判就能动手的实验**；边界：**开放状态变了、论文结论一个字都没变**——联合训练损害 ASR 的 AISHELL-1 2.57→3.94 仍然成立，288ms 时延仍是**相对词尾**的口径，复现时必须同时测 ASR 退化）

方向：同源辨析 — X2-Turn 与 X2Streaming-TTS 是一条脉络（当前问题：重复计数与漏并会同时污染追踪表；两篇共享 **5 名作者**（Rime Wen、Shawn Qin、Roy Gan、Hao Wang、Qian Wang）、同一通讯作者、同一机构 X Square Robot，一篇管输入侧"流式 ASR + 判停"、一篇管输出侧"零前瞻流式合成"，而 X2-Turn 仓库里的 `full-duplex-demo` 正是把两侧接起来的那层；可以尝试的是按**一条技术脉络**记录，与 VoiceChat 11B ↔ VoiceChat-TTS 完全同构；**注意这与 08-19 的 DuplexGen 同名案方向相反**——那里是两篇无关论文用了同一个名字，这里是两篇不同名论文属于同一个栈，**两个方向的误判都会发生，判据一律是作者/机构重合 + 是否互相引用 + 投稿间隔**）

方向：开放状态判定 — 新增第五种形态"声称发布、地址失效"（当前问题：此前只有四档（明确未发布 / 已发布可核 / 声称发布但全文无任何 URL 即 unverifiable / 明确"来函索取"）；X2Streaming-TTS 给出了**具体 URL 而该 URL 返回 404**，既非 unverifiable 也非明确未发布；可以尝试的是单列第五档，并附一条判据——**GitHub 对仓库改名会做重定向，404 说明这不是改名而是仓库尚未创建**；同机构确有一个高度吻合的 MIT 仓库 `X-Square-Robot/Qwen3TTS-Streaming`（494 条目、LICENSE 逐字读为未修改标准 MIT、最后 push 2026-08-11 早于论文投稿 8 天），但**其 README 全文没有出现 arxiv / causal commitment / speech-state inheritance / X2Streaming 任何一个词，两边互不引用**，故只能记为"疑似实现"，不合并）

方向：时延口径 — 论文与官方仓库对同一系统给出不同数字（当前问题：项目自身首音频 P50/P90 属于哪一种口径尚未自查；同一张 RTX 5090、同一 all-bf16，论文报 **15.8ms 客户端中位数**，仓库 README 报 **14.9±0.3ms 服务端**（min 14.4、p99 15.9、n=50）与 **~16.1ms 客户端 p50**，128 并发论文报 260.8ms 中位数、README 报 242–275ms 均值（按传输方式，p99 341–494ms）；**README 列出的条件里有一条论文完全没提——`prefix-cache 命中`**，而车机的第一句话恰恰是冷的，README 另注明冷/懒 gRPC 通道约多 13ms；可以尝试的是把 README 的两条方法论警告直接抄进项目埋点规范——**服务端 TTFT 与客户端 TTFT 不可混用**（同一系统同一时刻，README 自述 WebUI 面板 128 路读约 100–170ms 而对外可引用的客户端突发口径是 242–275ms，**差出一倍以上**）、**突发到达与斜坡到达不可混用**；两边数值原样并存保留，不替读者选择）

方向：CUDA Graph — 第五次独立佐证，且首次来自可读源码的生产引擎（当前问题：项目 110ms/token 的根因是 HF MoE 实现每 token 约 816 次 GPU→CPU 同步；`Qwen3TTS-Streaming` README 列出的三轮解码优化中，2026-07-06 那轮明写为 "CP in-graph KV + **CUDA-graph decode replay** + arena-ized KV gather"，优化后 RTX 5090 上 128 路并发解码步 **42.1ms / 80ms 音频帧**、RTF≈**1.90**，优化前 **119.8ms/帧**、RTF≈**0.67 低于实时**，64 路时 24.5ms/帧、RTF≈3.3；此前四次佐证分别来自 FlashDrive、vToken、DeaMoE、CoRun，**全是论文，本次首次来自可读源码的生产级 TTS 引擎**；可以尝试的是维持项目既定顺序不变——先落地无同步 `index_select`+`bmm` 快路径，再把整环图捕获作为**第二个独立变量**；README 自己给了一句很克制的话："128 streams is the tested ceiling, not a safe production target."）

方向：状态继承成本 — 跨段/跨打断保留语音状态的代价是可算且有界的（当前问题：PACE 的 playback boundary ledger 与 VoiceChat-TTS 的 "KV never reset" 都指向"打断后保留而非重置状态"，但项目一直没有这笔账的具体数字；`Qwen3TTS-Streaming` 的官方设计文档 `docs/dev/investigation/code2wav_state_size.md`（**一手仓库文档，非论文正文，引用须区分**）把 Code2Wav 状态算死了：解码器 8 层 / 16 KV 头 / hidden 1024 / head_dim 64、**KV 滑动窗口 72**（来自 Qwen 官方 `sliding_window=72` 默认值），KV 单路 8 层 2,359,296 字节≈**2.25MB**、卷积历史 17 个状态 263,504 字节≈0.257MB、转置卷积重叠 4 个状态 18,240 字节≈0.018MB，**单路合计≈2.53MB（BF16）且与解码长度无关**，16 路≈40.3MB，FP32 翻倍；可以尝试的是把这个数字直接写进项目设计文档，同时注意第二个推论——**滑动窗口 72 步意味着声学侧历史依赖本来就只有约 6 秒，跨越 6 秒的状态继承在架构上没有意义**）

方向：许可证陷阱 — 竞业限制条款（当前问题：具名许可证可能附带非标准义务，此前记录的都是致谢/署名类的弱义务；`Qwen3TTS-Streaming` 的 NOTICE 点明其可选依赖 TEN VAD 是 **"Apache License 2.0 WITH ADDITIONAL CONDITIONS"**，附加条款含**竞业限制**（不得部署用于与 Agora 的产品竞争）、单一申请人使用、衍生作品继承同一许可证；可以尝试的是在选型文档里点名——**该依赖默认关闭且非捆绑，但竞业限制对商业车机项目属实质性风险**，是目前记录到的约束最强的一例；同一 NOTICE 还写明权重不随仓库分发、由用户自行从 ModelScope/HF 下载且**许可证以各自模型卡为准**，是"代码许可证不传递到权重"的又一例证）

方向：部署前提 — NGC 与 TensorRT 的硬约束（当前问题：项目尚未评估该链路；该仓库运行路径依赖 NVIDIA NGC 容器（`nvcr.io/nvidia/tensorrt`、`nvcr.io/nvidia/tritonserver`），README 写明"拉取 NGC 镜像即构成接受 NVIDIA EULA"，硬件要求 NVIDIA GPU、建议 ≥16GB 显存、磁盘约 20–40GB，且 **TensorRT 引擎绑定特定 GPU/驱动/TensorRT 组合，一台机器构建的 `.plan` 无法可靠地在另一台运行**；可以尝试的是把这一条列为第三阶段的前置门——**在环境评估完成之前，不把 X2Streaming-TTS 的任何时延数字写进项目预期**）

方向：逐版复核方法 — 差分大小第三次证明预测不了结论变化，且本次是新极端（当前问题：追踪表的逐版复核容易只比数值；三次已完成的 v1→v2 差分结果各不相同——StreamTTT 改 184 行而结论不变、Ambisonics 只改 2 字节且改的是标题里漏掉的字母 `l`、本次 X2-Turn **只加一句话却翻转了一项挂了 8 天的开放状态判定**；本次的全部实质变化藏在**裸 URL 从 4 条变成 5 条**这一个差别里，而数值 token 只从 364 变成 365、新增的那个还是页边日期戳纯属噪声，**任何只看数值的比对都会漏掉它**；可以尝试的是追加一条操作规则——**逐版复核必须单独比对两版的 URL 集合（超链接注解 + 裸 URL），与字词/数字计数并列**）

方向：回声消除 — 连续第八轮零一手依据（当前问题：AEC 与扬声器泄漏仍完全没有一手证据；本窗口 `speech enhancement OR noise suppression OR echo cancellation OR beamforming` totalResults=**3**，而这 3 篇**没有一篇是声学论文**——2608.19102 是射电天文的实时射频干扰抑制、2608.18458 是 RIS-ISAC 联合波束成形与相移设计、2608.18252 是医学超声平面波角度域零陷成像，**"beamforming" 在本窗口 100% 落在非声学领域**；可以尝试的是查询继续保留——08-19 那批里的 4 篇声学论文中出过窗口 P0；沿用已记录的判别式：**ANC = 副扬声器 + 误差麦克风；AEC = 自身播放信号的参考/回环抽头**）

方向：多乘员与空间听觉 — 两组查询本轮 totalResults 直接为 0（当前问题：多乘员插话与"谁在说话"仍无一手依据；`speaker diarization OR target speaker OR speaker verification` 与 `spatial audio OR binaural OR ambisonic OR sound source localization` 在本窗口均返回 **0 篇**；可以尝试的是沿用 08-19 记下的架构约束——身份判定走音频通路之外的旁路控制信号，不进入 音频→ASR→判停→生成→播报 的串行链）

方向：关键词扫描盲区 — 本轮出现三类新假阳性/漏检（当前问题：14 组关键词既漏也误；**漏检**：`full-duplex OR turn-taking OR barge-in OR interruption` 返回 4 篇而**4 篇全是假阳性**（并行机排程的 interruption、LLM 协作的贝叶斯伙伴建模、ViT 注意力迁移、统计学固定术语 "Interrupted Time Series"），**本轮真正的全双工进展两篇都没被这组命中**；`speech recognition AND (streaming OR online OR latency)` 返回 0 篇，但 X2-Turn 的 v2 正是流式 ASR 方向的实质进展——**`submittedDate` 窗口永远看不见修订版，逐版复核是不可替代的第二通道**；**缩写级假阳性**：2608.18386 的 `TTSD` 展开为 Test-Time Self-Distillation 而非 TTS，主题是缺失模态情绪识别；**术语第三义**：2608.19013 "Harness Continual Learning" 里的 `harness` 既不是训练脚手架也不是执行脚手架，而是动词"驾驭"）

方向：查询写法缺陷 — 车内一组的布尔优先级写错了（当前问题：该组写成 `abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver" AND abs:speech`，而 **`AND` 的结合优先级高于 `OR`，因此 `abs:speech` 只约束了 `driver` 一个词，`in-vehicle` 与 `cockpit` 是无约束匹配**；后果是本轮该组返回 12 篇而 12 篇全部与语音无关——V2X 安全、自动驾驶检测器、无人机视觉定位、电动车与电动巴士充电规划、自动驾驶心理安全评估两篇、**水下图像增强**、无人机视觉里程计、工程电动车充电调度、中断时间序列、公交线路设计、SIGMA 自适应控制；可以尝试的是下一轮改写为 `(abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver") AND (abs:speech OR abs:voice OR abs:dialogue OR abs:assistant)`，并**记录改写前后的 totalResults 对照，以免把口径变化误读成产出变化**；边界：这是查询写法缺陷，不等于该领域本轮无产出，两者必须分开陈述）

方向：判定器构造效度（优先级仍最高） — 脚手架敏感性同一窗口两条独立佐证（当前问题：2608.12880 只翻转与行为无关的元数据即改变 180 条中的 31 条判定，项目判定器处理不变性至今未验证；**SemaPLC（2608.18565）用 7 个模型 × 2 个条件的完全析因**给出——固定模型加脚手架提升 **8.5–33.3 个百分点、均值 +17.3**，而固定脚手架换模型跨度 **37.6 个百分点**，同一量级；**ComponentBench（2608.18307）在同一 harness、同一模型、同一批任务下只改观察与动作空间，通过率 83.1% → 48.9%**，且它的 harness 与 40.97GB 原始运行归档是 MIT 开放非门控的（DuplexWorld 至今只有 18 条场景规格、无音频、无 harness 代码）；加上 DuplexWorld 已是三条独立佐证；可以尝试的是把下一个评测周期花在判定器与 harness 构造效度上而不是换模型；**但同等重要的反向记录：三篇没有一篇验证了自己的归因方法**——人工一致性统计（kappa/一致率/注入故障召回）全部为零，ComponentBench 的"确定性"标注器以组件类型先验为起点、第二套标注直接是 GPT-5.4、唯一复核者是另一个 LLM（20 案例单次采样、改标 5 个即 25%），**所以外部没有人替项目做掉这件事，优先级不变、理由变强**）

方向：打断路径 — 掩码式 KV 回退，一个来自被否决论文的可实现原语（当前问题：PACE 的 playback boundary ledger 与 VoiceChat-TTS 的 "KV never reset" 都指向"打断后不重置状态"，但项目一直缺一个"在不改变形状的前提下撤回已写入 cache 的 token"的可实现做法；2608.19147 的做法是**不物理裁剪 KV cache，而是把作废位置的 attention mask 置零，同时让 `position_ids` 跟踪逻辑序列长度而非物理 cache 长度**，实测与物理裁剪**逐位等价**（修正后 logits 最大绝对差 **0.0000**，fp32 机器精度相等），代价在 K=3 时**低于每步墙钟 1%**，而物理裁剪一次要**约 48ms**，生成到 2048 token 时 cache 膨胀收敛到 **1.02 倍**逻辑长度；可以尝试的是把它作为打断/撤回路径的原语写进设计文档——**这正是固定形状、无主机分支的形状，即 CUDA Graph 捕获解码环所要求的形状**；配套陷阱必须一起记：**空闲行要在自己的 query 列上开一个口，不能整行掩掉——整行被屏蔽会让 softmax 产生 NaN 并污染共享同一次推理的其它活跃行**；边界：论文本体因需要多机而否决，其单机切分净负 11–15%、并发机制在单用户处反转、全栈 Intel OpenVINO 零 CUDA）

方向：验证门控 — SemaPLC 的三条门不变量，以及它自己重写了判定器（当前问题：项目工具/动作通道没有"播报前动作门"，模型说做完了就算做完了；2608.18565 的三条不变量可直接搬——**有界重试**（每项检查至多 2 轮修复）、**编辑失效**（任何修改作废此前全部 verdict、全部重跑，"verdict 附着在确切的字节上"）、**挣得的声明**（每个结果都是机器可读哨兵并与工具调用日志交叉验证，未记录的声明降级为未检查），作者的总结句是"没有任何自报通过能在缺少工具日志的情况下存活"；**关键是它的检查器不是模型调用而是编译器与运行时**——这是唯一可能塞进时延预算的形状，任何座舱版本同样必须是确定性校验；它也是本轮唯一把门的代价测出来的论文：**3.79 倍 token、5.37 倍请求买到 +31.0 动态分**；边界：**它自己重写了判定器**——发布的 oracle"包含会让正确实现失败的缺陷属性"，工程师修复了 **117 个中的 43 个**、属性数 629 → 607，缓解措施异常认真（三轮审计、盲审裁决、只修两轮以上确认者、无依据的属性直接删除）但**未发布修复数据、未在原始 oracle 上做对照消融、也未声明与既往 Agents4PLC 数字不可比**，这是 2608.12880 的新形态：不是意外的标志位，而是**有意的、有记录的、未发布的重写**；另注全文未声明硬件、连运行时本身都没命名，而头条指标恰是运行时测量；一条正面记录：**全篇没有 LLM-as-judge**，用 PLCverif + nuXmv + 编译器与确定性断言）

方向：静态验证的盲区 — 定时器类构造 100% 无结论（当前问题：项目若用静态打分替代端到端实测，会系统性看不见跨时间的错误；SemaPLC 的形式化验证覆盖表显示 **32 个含定时器（TON）的程序、174 条属性，结论性判定为 0，全部 100% inconclusive**，不支持的模式与翻译失败占主导；可以尝试的是把这条作为坚持端到端播放边界与判停时序实测的外部依据——**静态判定与实际执行行为会分叉，而分叉恰好发生在有状态、跨时间的构造上**，判停与打断正是这一类）

方向：工具调用准确率 — 一条项目假设被更正（当前问题：项目此前从 2608.14036 推出"既然技能池 5→100 会让实际使用精度从 29.6% 崩到 3.3%，那么任何能合法缩小可达工具集的东西都可能提升准确率而不只是安全性"；2608.18351 是名单上**第一篇同时测量权限效应与任务准确率效应并发现二者不冲突**的论文——MetaTool 通用工具选择准确率 **81.9% → 83.6%（+1.7pp，p=4.55×10⁻⁴）**同时越权下降，论文自己点破了那个失败模式"只测越权的安全指标可以通过让 agent 更不作为来轻易改善……这是自我挫败的"；**但它不缩小可达工具集**——原文明写"模型看到的是一份完整的工具清单"，broker 在环境侧且对模型不可见，训练的是让策略**选择**更低权限的动作，**因此它不能作为那条假设的证据，那个问题仍然开放**；两条阻塞：**"越权成功"按定义等于（任务成功 − 安全成功），分子只统计成功的 episode**，基座另外约 900 个失败 episode 的越权行为从未报告——**在失败轮次中发生的越权正是车辆执行场景最危险的一类，这套指标一次都没计入**（摘要写"越权错误事件"而结论写"**成功的**越权事件"，后者才准确）；以及**定义"越权"的充分权限包络本身未经验证**，任务与包络是 Codex 与 Claude Code 生成的，无 kappa、无人工审计抽样、无注入故障召回、无风险向量消融；零成本可借的两条：**"尝试"与"被允许"分开记录**（门拦住了也要记下策略请求过不正当权限，这是只靠门防御会掩盖的先行指标）、以及**提示词消融设计**（完整/去安全措辞/一行，同一评测集两个策略同跑——训练后策略跨三种提示只动 0.24pp，基座动 3.70pp））

方向：本轮否决 — 三篇按前提不适用（当前问题：避免日后按错误理由重新 litigate；**WhiteMatter（2608.18486）跨层 KV 混合**是从零预训练的架构改动且全部代价记在 prefill 上（**3.05–3.30 倍原版 prefill FLOPs**），而 prefill 正是项目首音频瓶颈，其 depth-causal 消融证明这笔代价躲不掉（去掉迭代后困惑度 34.71→37.30），全是 48.7M–约 100M 稠密小模型、全文 `MoE` 出现 0 次、无解码墙钟；可留一条技巧——**为让注意力对角线不可读又不破坏 FlashAttention-2，它在 KV cache 前预置一个可学习哑元 token 把 cache 相对 query 偏移一位**，而不是掩掉对角线。**DART-SD（2608.18524）**在项目真正测量的轴上几乎没动——RoTBench 工具选择 8B 上 **75.52→75.83 即 +0.31**，同表 FTRL-GRPO 是 77.02 比它高，4B 上参数正确率 42.38 **输给普通 SFT 的 44.40**；**它自己的消融拆穿了标题**：总增益 22.18 里普通自蒸馏占 **+14.62（66%）**，标题命名的 CTB(+1.41) 与 ISTG(+1.73) 合计 **14%**；按构造不可能改善时延，且写出每条训练目标的 `AugGen` 从头到尾没命名。**LEDGER（2608.18398）全文零数字**——没有错误率、时延、稳定性、人工一致性、基线、节点或边计数，**不是藏在图里而是不存在**；**它不是 PIPES 那一族**：PIPES 在运行时把溯源挂在工具回执上并有实测效果（84.7%→2.3%），LEDGER 是事后人工审阅面板、无运行时强制、无任何错误率指标，相似之处只有"溯源"这个词；可留一条纪律——**显式的层次信任分离**，把审计记录每个字段标记为"确定性捕获"还是"模型推断"，且推断字段永不用于设定阈值）

方向：口径警告 — 三条不要照抄的数字（当前问题：本轮细读发现三处会误导的聚合；**不要引用 DART-SD 的 `Avg.` 列**——九个数值列的无权重平均，FTRL 占 3/9 且第三列是前两列的导出量（等于三重加权且一列重复计数），而 RoTBench TS 一列在所有行包括未训练基座都落在 60.36–77.02 窄带，注入近似常数偏置压缩了方法间差异；**DART-SD 报告的 Solve-F1 在全部 18 行上都不等于其报告的 Solve-P 与 Solve-R 的调和平均**（8B 应为 47.30 而报 45.66），方向一致偏低，最可能是按任务先算 F1 再宏平均但论文从未声明聚合顺序，按原样记录；**ComponentBench 的跨模型时延比较不成立**——七个模型经由五种不同服务通道（Google AI Studio、OpenAI API、Duke LiteLLM 代理、本地 vLLM FP8、Amazon Bedrock），论文自己也拒绝相信自己的时长："我们把效率结论建立在动作计数而不是受时延污染的时长上"）

方向：评测协议 — ComponentBench 三条可直接照搬的做法（当前问题：项目 452 条自建判定没有泄漏扫描器，也不报逐条翻转；**其一，把判定器输入隔离做成可测试的不变量**——成功谓词存放在组件闭包里、测试用 DOM 属性由实时 MutationObserver 剥除，且**有一个 CI 就绪的扫描器验证全部 2,910 个页面上没有任何此类属性存活**；**其二，把聚合稳定性与逐条翻转分开报告**——其实例是同一模型两次运行通过率完全相同（52.5%）**却有 17.3% 的任务结果翻转，pass→fail 与 fail→pass 在聚合上互相抵消**，在通过率旁同时公布一致率与 Jaccard 是零成本的且**直接适用于项目的 80.49% 严格工具名召回**；**其三，把"设计意图难度"与"实测实现难度"分开记录**，与 08-19 从 DuplexGen-B 得到的"合成重叠数据系统性偏易"是同一条纪律；边界：浏览器 GUI 组件、无语音无中文无工具调用语义无每轮时延，只借协议不借技术）

方向：开放状态 — 第五档本轮出现第三例，并新增第六档"沉默式未发布"（当前问题：分档要能指导后续动作；**第五档"给了 URL 而 URL 失效"本轮第三例**：2608.19147 两处印出 `labscommunity/pipeline-sharded-inference-paper` 并二十余次引用 `reproduction/` 下的具体文件名，该仓库 404（注意**已认证的 `gh` 对私有仓库同样返回 404**，不能区分"不存在"与"私有"，但两种情况今天都不可核实），而"近似命中"的仓库是真实且实质的——同组织 `labscommunity/cascadia`（Apache-2.0，884 条目/12,280,772 字节，22 stars）在**论文投稿前 17 分钟**推送、实现了本文技术并逐字提到本文制品名，**但它不是承诺的那个包**（全树 `reproduction` 出现 0 次，且它是 Rust 而论文基准全是 Python）**且两边互不引用**；**新增第六档"沉默式未发布"**——论文对代码/权重/数据不作任何声明，没有可证伪的承诺，比第三档 unverifiable 更诚实但同样不可核实，本轮两例：WhiteMatter（14 条裸 URL 全是参考文献）与 DART-SD（`releas`/`available`/`github` 全文各 0 次）；另注 SemaPLC 属第三档——正文写 "The released data ..." 却全文无 URL）

持续跟踪项（本轮无进展，逐条列出以免被"有 P0"掩盖）：判定器处理不变性检查（2608.12880，**优先级仍为最高**，与本轮无关且仍未完成）；`streaming-se-frontend-rtsemamba-kd1-v1` 的 PESQ→CER 阻塞（08-19 的反向证据维持不变，RT-SEMamba 仍须自测中文 CER）；PACE（2608.07631）仍是最强全双工命中；AutoDesign 附录 A.5 人评数据（第五次待核）；GCM-Bench 的 PACE 中间件、SemBridge、Smart-Glass-Challenge、flashdrive、CASA、`duplexgen/personaplex-finetune` 六个占位仓库；DuplexGen-B 承诺的 180 段基准；FireRedTTS3 是否补出任何时延/流式基准；Real-World AVSE 挑战赛数据是否公开。
