# 每日调研日报｜2026-08-22

**本轮无重大新增。** 证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、`probe_turn_end` 负面结果 2026-08-03、最近 H20 A/B 2026-08-03，本轮无新提交与新基准）。**Radar 连续第十轮未刷新**：`data/items.json` 的 `generated` 仍是 2026-08-12 09:30、条目仍 499、`data/explanations.json` 仍 304 条，新增 0 条。**索引本轮进入冻结相位，是 08-15 之后第一次**：08-21 报告记录全站最新投稿为 2608.20340（2026-08-20T17:59:59Z），本轮同一查询返回的**仍是 2608.20340**，前三条逐条相同，五个对照总数与 08-21 逐位相同（cs.SD **21,749**、eess.AS **22,209**、cs.CL **116,790**、cs.MM **9,947**、cs.HC **32,292**），**索引 26 小时零推进**。窗口按投稿时间取 **2026-08-20 18:00 UTC 至 2026-08-22 02:30 UTC**，查询组从 14 组扩到 **17 组，17 组全部 totalResults = 0**，去重 **0 篇**。三级证伪全部到位（每个零结果组的去日期对照均返回大数、全站最新投稿时刻已记录、不限类别的窗口查询也是 **0**），另加第四道 `lastUpdatedDate` 语音类目查询同为 **0**。**下一轮窗口起点仍为 2026-08-20T18:00Z，不得前移**——本轮实际覆盖投稿数为 0，起点必须绑定"最后一篇实际覆盖到的投稿时刻"。另：main 此前仍停在 2026-08-10（**连续第十轮**），08-11 至 08-21 的 18 份报告与 9 份候选台账已在本轮先恢复到主线。制品复核时刻为 2026-08-22。

方向：检索工具本身 — arXiv `abs:` 做词干归并，昨天提出的改写实测无效（**本轮 P0，也是本轮唯一实质产出**）（当前问题：08-21 报告发现判停组 `voice activity detection OR endpoint OR endpointing` 返回 22 篇、22 篇全与语音无关，诊断为"裸词 `endpoint` 在数学与安全领域是高频固定术语"，并建议**删去裸词 `endpoint`**；**本轮把 08-21 的窗口原样重放做 A/B，结果是改写没有去掉任何一篇假阳性**——旧写法 totalResults=**22**、新写法 totalResults=**22**，**22 个 ID 逐条完全相同**，全库口径下改写还略微更差（7,388 → **7,429**）；机制查清了：**`abs:` 在匹配前把单词归并到词干**，五个词族各自坍缩成同一个数且最新三条 ID 逐条相同——`endpoint`/`endpointing`/`endpoints` 同为 **7,145**、`interruption`/`interrupt`/`interrupted`/`interrupting` 同为 **2,491**、`stream`/`streaming`/`streamed` 同为 **30,701**、`beamforming`/`beamform`/`beamformer` 同为 **6,618**、`cancellation`/`cancel`/`cancelling` 同为 **15,005**；**短语不会整体坍缩但短语内成分词同样被归并**——`echo cancellation` 与 `echo cancel` 同为 **142**，而 `voice activity detection`（246）与 `activity detection`（718）不同，说明短语邻接约束确实生效；可以尝试的是把判停组改成 **`abs:"voice activity detection" OR abs:"end-of-turn" OR abs:"turn-end detection"`**，即**把两个被归并的单词都删掉**，全库 **7,388 → 288，减少 25.6 倍**，改完就见效、零成本；**并把"提精度的唯一有效手段是改用多词短语，删宽泛单词无效"写成长期规则**）

方向：缩略词陷阱 — `abs:"TTS"` 在检索层面就等于 `abs:"tt"`，粒子物理那篇不是"关键词假阳性"（当前问题：08-21 把 2608.19651《顶夸克 philic 第三类双希格斯二重态模型中的电弱重子生成》记为"本项目记录到的跨度最大的一次关键词假阳性"；**本轮查清它根本不是关键词问题**——`abs:"TTS"`、`abs:"tts"`、`abs:"TT"`、`abs:"tt"` 四者全库 totalResults **同为 4,956**，词干器把缩略词 `TTS` 当作 `TT` 的复数剥掉尾部 S，而 `tt` 正是顶夸克对的记法，该论文标题即含 "$t\bar{t}$ Excess at the LHC"，同组另一篇 2608.19789 标题是 "**TT-net**"，同一机制；**并且我核实了两篇摘要里 `speech`/`synthes`/`text-to`/`TTS` 四个模式的命中数全部为 0**，即它们与语音检索的唯一联系就是被剥掉的那个字母 S；可以尝试的是**撤回"跨度最大的关键词假阳性"这个说法，改记为"缩略词词干归并导致的伪匹配"**，并把 TTS 组的 `abs:"TTS"` 直接删掉、只留 `text-to-speech`（2,316）与 `speech synthesis`（1,204）；**顺带查清哪些缩略词探针可信**：`ASR`/`ASRs` 同为 5,164、`VAD`/`VADs` 同为 504、`KV cache`/`KV caches` 同为 1,730，三者两形态都指同一概念故可用，而 **`AEC` 全库仅 259 且无尾部 S 可剥，是该方向最干净的探针**）

方向：回声消除 — 连续第十轮零一手依据，但该组的噪声来源终于查清（当前问题：AEC 与扬声器泄漏仍完全没有一手证据，本窗口该组 totalResults=**0**（08-21 为 1 篇非声学）；**此前记录的"`beamforming` 连续两轮 100% 落在非声学领域"现在有了机制**——`beamforming`/`beamform`/`beamformer` 全库同为 6,618，该词把无线通信、ISAC、雷达、医学超声整片带进来，而它带进来的量级远超真正的声学 AEC 文献；可以尝试的是**把该组改成以 `abs:"echo cancellation"`（142）与 `abs:"AEC"`（259）为主、删掉 `beamforming`**；**但必须同时写清这不会变出新论文**——词干归并只多召回不少召回，**去掉噪声词只让该组更干净，"AEC 十轮为零"这个结论不受任何影响**；沿用三分判别式：**ANC = 副扬声器 + 误差麦克风；AEC = 自身播放信号的参考/回环抽头；MaAI 双通道 VAD = 串音条件下的归属判定**，第三类接受泄漏存在只做归属，不得记为 AEC 进展）

方向：全双工组的两个缺陷必须分开记 — 精度损失已归因，召回损失仍未归因（当前问题：08-20 与 08-21 连续两轮记录"全双工组既空又漏"，并下结论"这组关键词对全双工主题存在系统性盲区"；**本轮只解释了其中一半**——该组的**噪声**来源清楚了，`interruption` 一词就带来 2,491 篇全库量级的词干污染，且**08-21 该组返回的 3 篇假阳性（2608.19893、2608.19887、2608.19622）正是 `abs:"interrupt"` 族按投稿时间倒序的最新 3 篇，逐条吻合**；**但"漏掉真正的全双工进展"这件事，词干归并解释不了，因为归并只会多给不会少给**；可以尝试的是把两个缺陷永久分开记账：**精度损失＝词干归并（已归因）、召回损失＝原因仍未知（未归因，且不得用本发现冒充解释）**；**顺带把 `interrupt` 词根的三个假阳性从"三次独立巧合"改记为"一个机制的三次显形"**——并行机排程、统计学固定术语 "Interrupted Time Series"、2608.19893 "Interrupting the Loop"，三者同源）

方向：查询词的事前体检 — 昨天建议的写法里有一个永远不可能命中的词（当前问题：新查询词此前从不做上线前检查；**`abs:"turn-end detection"` 的全库 totalResults = 0**，不是窗口内为零，是 arXiv 全库没有任何一篇摘要能匹配它，**而它恰恰在 08-21 建议的改写方案里**；一个全库为零的词进了查询组无害但也无用，**会让该组看起来比实际覆盖得更宽**；可以尝试的是把"**提出新查询词时先测它的全库 totalResults**"写成规矩，并把"**每次改写查询都必须做同窗口 A/B 并记录改写前后两个总数**"一并写死——本轮正是靠 A/B 才发现 08-21 的建议无效，**若只换写法不做对照，会把"改写生效"当成既成事实带进后续若干轮**）

方向：逐版复核的覆盖面 — 追踪表从 47 个 ID 扩到 173 个，非 v1 条目从 3 个变成 20 个（当前问题：窗口内结论没变——**零新修订，连续第二轮为零**，且与 `lastUpdatedDate` 查询的 0 互为独立印证，**这一条必须明写否则"版本通道无产出"会被误读成"版本通道没查"**；但本轮改了做法：不再用惯例的 47 个 ID，而是把 `reports/` 里出现过的**全部 173 个 arXiv ID** 一次扫完（4 批、每批 ≤45、`?id_list=`、173 个全部解析成功 0 缺失、耗时约 28 秒），**结果非 v1 条目共 20 个而 08-21 记录的是 3 个，即 17 个从未做过任何逐版差分**；**其中一条构成对 08-21 的必须写明的修正**：2608.14513 的 v2 修订时刻是 **2026-08-19T18:36:03Z，落在 08-21 报告自己的窗口之内**，而 08-21 写"没有任何追踪项在本窗口产生新修订"——**该陈述对它实际扫过的 47 个 ID 成立，对本项目追踪过的全部 ID 不成立**；**同时要把份量说准不要夸大**：2608.14513 是一篇非线性投影不确定性可视化的论文，与项目任何方向无关，**因此 08-21 的实质结论没被推翻，被推翻的是它的覆盖面陈述，两件事必须分开说**；可以尝试的是把**逐版复核的输入集合改成从 `reports/` 机械提取、不再人工维护清单**（`grep -ohaE '\b26[0-9]{2}\.[0-9]{5}\b' reports/*.md | sort -u`）——**这与 08-21 记录的 MaAI 失败是同一形状：不是通道失效，是通道的覆盖范围被无声地缩小了**）

方向：MoE 解码 — 扩表后浮出一篇直接对口却从未读过的论文（当前问题：项目解码 110ms/token，根因是 HF 的 MoE 实现在单 token 解码时每层多次 GPU→CPU 同步、48 层 × 17 次 ≈ **816 次同步/token**；**通道二扩表后浮出 2608.15018《S2-MoE: Enabling Efficient Self-Speculative Decoding for Mixture-of-Experts》**，v1 投稿 2026-08-15T04:08:04Z、**v2 修订于 2026-08-19T09:26:35Z，从未被逐版差分、也没有全文首读记录**，主题正对口项目的 MoE 解码；可以尝试的是先做 v1→v2 差分与全文首读，**且必须先把批大小四态判准再谈否决**——评测过批 1 且收益保持则该规则沉默、评测过批 1 但收益向 1 收缩才是否决、从未在 1 上评测、以及**批大小全文从未报告（记为未知，不等于前一种）**；**同时必检缓存命中陷阱**（grep `radix`/`prefix cach`/`warm`/`cold`），车机上电后的第一句话永远是冷的，**而"论文对缓存状态一字不提"本身就是发现**；本篇已委派细读，结论以单独提交并入）

方向：判定器构造效度（优先级仍为全部事项中最高） — 连续第四轮列为待办且连续第四轮未开工（当前问题：项目对外数字仍是**单次运行的严格工具名严格召回 80.49%**，而 2608.12880 已证只翻转与行为无关的元数据即改变 180 条中的 31 条判定，Thinkingbox 又给出 pass@20 **91.12%** 对 pass^20 **25.25%** 并明写"工具调用层面的信号不是端到端任务完成的清晰代理"；**本轮没有任何新论文证据，也没有任何进展**；可以尝试的仍是 08-21 定下的第一步——**照搬 StateMemBench Table 8 的四格转移表**：选一个与行为无关的拨杆、判定器固定不变、**报四个转移计数而不是那个差值**（其原始值为关→开由错变对 3、开→关由对变错 7、两臂都对 35、两臂都错 5，即 **10/50=20% 逐条翻转而聚合只动 8 个百分点**），**零 GPU、零成本，本项被推迟四轮的唯一原因是没人动手**；对座舱另有一层含义不变：**用户说"打开空调"没有第 20 次机会，pass^k 才是对齐业务语义的指标**）

方向：索引冻结的正确处理 — 窗口起点必须不动，否则会永久丢批（当前问题：本轮索引 26 小时零推进，**实际覆盖投稿数为 0**；若按"上一轮的时钟时刻"把下一轮窗口起点改成 2026-08-22T02:30Z，则 08-21 与 08-22 两批一旦后来被回填就**永久看不见了**——而回填是本项目已观测过的既成事实（08-17 一次回填 83 篇、08-19 一次回填四批 235 篇）；可以尝试的是把"**窗口起点绑定最后一篇实际覆盖到的投稿时刻，而不是上一轮的时钟时刻**"在冻结日显式复述一遍，本轮起点因此保持 **2026-08-20T18:00Z**；**另把"方向为零"与"索引为零"分开记**——前者是领域没产出、后续动作是继续等，后者是检索源没推进、后续动作是保持起点并预期回填，**本轮是后者，且是本项目第二次遇到（第一次是 08-15）**）

方向：零结果的读法 — 17 组全零不构成对既定规则的反例（当前问题：08-21 记过一条规则"篇数与查询组的空满程度都不是'有无重大新增'的代理"，本轮 17 组全零容易被当成该规则的反例；**但本轮的零不是语音方向没产出，而是索引一篇都没进**——不限类别的窗口查询 `(all:a) AND submittedDate:[...]` 同样是 **0**，**所以两者根本不在同一层**；可以尝试的是继续保留第 14 组 `(cat:cs.HC OR cat:cs.OS)` 查询——它连续两轮独家命中当轮最重要线索（08-20、08-21），**本轮为零只能算窗口为空、不能算该组表现下滑**；**并且要写清词干归并对零结果的方向**：归并只多召回不少召回，**因此此前所有记录为 totalResults=0 的零结果全部仍然有效，不得用本轮的发现去回溯质疑任何一条已记录的零结果**——AEC 十轮为零、空间听觉连续为零、流式 ASR 连续为零都不受影响，**受影响的只有精度**）

方向：本轮不新增任何工程候选，这一条要说清楚（当前问题：连续多轮都有 P0 会让"本轮无候选"显得像失职；**本轮的两条 P0/P1 都是方法学与流程发现，不是可以进候选台账的技术**——论文通道零、通道二窗口内零；**08-21 立项的候选 `backchannel-timing-sidecar-maai-offline-v1`（用 MaAI 的 MIT 中文应答权重 `vap_bc_ch` 做纯 CPU 旁路观测）本轮没有任何进展，仍阻塞在同一个前置条件上：项目缺带应答/打断标注的中文评测数据**；可以尝试的是把本轮的四条零成本查询修正（判停组、TTS 组、AEC 组、逐版复核输入集合）当作本轮的实际交付物，**它们改完即生效且已被 A/B 验证**，而不是勉强凑一个技术候选）

持续跟踪项（本轮无进展，逐条列出以免被"有 P0"掩盖）：**判定器处理不变性检查（2608.12880，优先级仍为最高，连续第四轮未开工）**；2608.19652 StateMemBench 的标注工具包（追踪表上价值最高的第三档条目，全文 23 页无任何 URL）；2608.19741 Thinkingbox 的 507 任务基准是否落地；2608.19662 ReCache 是否出现 LICENSE；2608.19880 EnvHarness 的 WebShop 桥接是否出现；2608.19901 MaliciousSkillBench 附录 G.6 缺失的四类制品；2608.19557 是否出现 LICENSE；`X-Square-Robot/X2Streaming-TTS` 是否出现（08-20 设的两周期限，**到期日 2026-09-02，本轮第三天**）；`X-Square-Robot/Qwen3TTS-Streaming` 是否出现提及论文的提交；`MaAI-Kyoto/MaAI` 的中文 `vad_ch`/`bc_det_ch` 是否移出 CC-BY-NC-ND-4.0、`readme/vap_bc.md` 的"仅支持日语"陈旧说明是否修正、**是否首次出现任何可核实的精度数字**；只读审阅 EnvHarness 的 `rules.py`（8,274 字节）；离线复现 X2-Turn 的 EasyTurn-zh 判停指标（非门控 Apache-2.0，连续第三轮未动）；测项目自身上游 token 速率与抖动；TensorRT/Triton 链路环境评估；`streaming-se-frontend-rtsemamba-kd1-v1` 的 PESQ→CER 阻塞（仍须自测中文 CER）；PACE（2608.07631）仍是最强全双工命中；AutoDesign 附录 A.5 人评数据（第七次待核）；GCM-Bench 的 PACE 中间件、SemBridge、Smart-Glass-Challenge、flashdrive、CASA、`duplexgen/personaplex-finetune` 六个占位仓库；DuplexGen-B 承诺的 180 段基准；FireRedTTS3 是否补出任何时延/流式基准；Real-World AVSE 挑战赛数据是否公开；**本轮新增：17 个从未做过逐版差分的非 v1 条目**（2608.15018、2608.06718、2607.06540、2608.07346、2608.09096、2604.00688、2608.12951、2602.06036、2608.02508、2608.03571、2608.08311、2608.09448、2608.14546、2603.22120、2604.26904、2608.14513、2608.16790，前三条已委派）。
