# 每日调研日报 · 2026-08-08

Action 恢复：Resume Contract（当前车控在 crash、重复审批或多进程 resume 后缺少“只执行一次”的外部证明；论文在 LangGraph/CrewAI 等持久化路径中测到重复副作用和静默复用旧审批值；准备以执行器外部 action ledger 回归 SIGKILL、冲突审批和 2/4/8 路并发，要求每个授权分支至多一次副作用）

最终车态：OneDayAgent verification-only（当前严格工具名和执行接受仍不能证明 CarSim 终态与口播一致；论文只加验证由0.771升至0.804、平均延迟仅由27.6增至29.7分钟，而完整方案需53.6分钟；准备只移植“原请求+参数+最终产物核验+定点修复”，不搬小时级任务分解）

KV 质量：RestoreKV（B版持久KV曾损失约22个百分点，但当前110ms/token主要来自MoE同步而非cache更新；论文以8个restore token和一次LoRA前向在相同KV预算下把Qwen3-4B的5% KVzip RULER由38.2提至73.2；准备先离线比较full cache/当前B/RestoreKV，明确它不替代无同步fast path）

长时音视频：StreamArena（当前语音回归缺少小时级因果流、主动提醒和历史证据追溯；其243个平均88.8分钟视频、3,646个开放任务同时覆盖感知/回忆/工具/主动触发；准备构造统一时钟的30/60分钟座舱回放，但其同骨干平均回答仍为27.5秒，不能当低延迟全双工后端）

多模态约束：C-SuiteBench 配对消融（光场屏和副驾屏上下文可能增强描述却削弱车控约束；论文主表中九模型加入全部视觉证据后受约束分配平均下降0.08，最佳单通道反而相对文本提升0.052；准备对同一车控请求逐个加入车态/主屏/副驾屏并检查参数和最终状态）

Harness 优化：HarnessOpt-Bench（当前Harness改动的评测昂贵且容易对可见样本过拟合；论文固定模型/环境/verifier、隐藏最终测试并保存候选版本，模型选择影响平均大于coding harness；准备先稳定安全verifier，再小规模开放prompt/context/retry/tool schema，禁止改测试集和成功定义）

模态路由：SmartMage（当前全模态固定注入会带来无关上下文和额外计算；论文按问题语义、text-modality对齐和模态质量选择辅助模态，再做模态感知专家路由；准备短期只借证据门控原则，暂不把非流式3D MoE接入当前同步受限热路径）

多人前端：REAL-2MIX/QIANGDA 去重（Radar中的REAL-2MIX是前一日QIANGDA论文的v2改名修订，不是新benchmark；继续用同一双人混音分别指定目标乘员的成对身份协议，等待官方代码、数据和许可证，不重复建项）

AEC/语义判停：持续跟踪（今日没有直接命中真实座舱双讲AEC、中文判停或打断的新开放结果；继续影子评估NAC endpointer，按ep50/ep90/cutoff、回声、双讲和最终车态分桶）

TTS/MoE：持续原实验（A/B首音频P50仍为2,918/3,491ms，单token仍约110ms；今日候选不能证明这两项已改善；继续本地CosyVoice首包P50/P95与 `index_select`+`bmm` 无同步fast path）
