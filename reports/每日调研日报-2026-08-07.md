# 每日调研日报

更新日期：2026-08-07

项目快照：今天没有新提交或新基准；A/B 的 Pareto 结论保持不变。新增候选均为待验证方案，
不代表已经改善 StreamingModelHarness。

方向：LedgerMind 证据账本（工具名正确仍不能证明参数、执行路径、最终车态和口播一致；为候选、typed call、执行回执、最终 CarSim 状态和口播声明建立版本、TTL、依赖和有限修复；先在现有 A/B 失败样本上验证参数+最终车态至少达到 84% 基线且闲聊副作用为 0）

方向：SpatialSTALE 读时记忆审计（旧车态和旧工具回执可能压过当前观察；在动作前按 source version、observation version 和 TTL 删除冲突证据；构造 no-memory、raw-memory、audited、oracle-audited 四组，要求过期证据导致的错误车控为 0）

方向：Sparse Event-KV donor-pair（删除源音频或工具 span 后，下游 KV 仍可能携带旧语义；通过服务内容完全相同、只翻转被删源值的成对实验测量因果影响；固定 token、位置、候选工具和 query，要求旧 donor 不再改变当前 typed call）

方向：OneDayAgent verification-only（聚合意图展开不全，而当前约 80% 严格工具准确率使额外调用扩大错误面；执行后用原始意图与最终 CarSim state 定点检查并只修复缺失子动作；比较 DIRECT 与 verification-only，要求聚合意图提升且单意图、闲聊和 P90 不退化）

方向：CloudEdgeVLA 异步 freshness contract（Harness C 的 RAG、state 或回执晚到时，阻塞会增延迟而直接使用会陈旧；慢路径结果携带 request ID、source version、age 和 TTL，本地执行器始终用当前车态复核；比较 blocking、latest-valid、drop 和 fallback，要求跨请求串用与过期执行均为 0）

方向：RestoreKV 条件性保留（未来若采用激进查询无关 KV 驱逐，可能需要在固定预算下恢复 full-cache 行为；完整 KV 丢弃前生成少量 restore token 并与具体 evictor 配套训练；仅在 KV>10000 且出现真实显存或 attention 瓶颈后测试，当前不接入）

方向：Qwen3-Omni MoE 无同步 fast path（110ms/token 仍主要来自约 816 次 GPU→CPU 同步而不是 KV=2500 的 0.2ms 复制；以 index_select+bmm 替换逐 expert Python 循环；继续在 5090/H200 与原实现做输出一致性和 ms/token A/B）

方向：本地 CosyVoice 首包（A/B 首音频 P50 仍为 2918/3491ms，主要瓶颈是 Edge TTS；用本地流式首包缩短 commit 到首音频；继续测 P50/P90/P95、中文忠实度和打断性，不用今天的 memory 论文替代该实验）

方向：独立语义端点器（主模型自主 probe_turn_end 已实测失败，静音阈值又会切碎自然停顿；让轻量模型只决定继续或提交，不让主模型重复接管终止控制；在 700/1000/1500ms 停顿、短确认、改口和取消集上要求误切下降且首动作不退化）
