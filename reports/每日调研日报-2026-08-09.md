# 每日调研日报｜2026-08-09

当前事实：baseline 414/452、召回 443/452；38 个失败为 25 候选内选错、4 no-call、
9 召回缺失。candidate-ID/CMTF 与未接入 runtime 的 action-safety 已证伪，禁止强制 ID 覆盖。

- 方向：条件式 Top-2 Tool Verifier（用 MulRobBench 的允许/禁止动作契约和 CalibForge 的
  solver 分歧思想，只处理低分差混淆；414 零退化且预计净修≥16才进 H20）
- 方向：守卫式规则记忆（Activity Frames 的确定性编译与 guard-miss 回退；理论上限 9，
  未达昂贵门，记录 proposed/rejected 而不烧 GPU）
- 方向：可恢复 handoff（Resume Contract 的 exactly-once/consume-once；Radar 发布 manifest、
  Harness 02:00 ack、实验 result manifest、下一班回写，任何环节超过24h明确报警）
- 方向：参数与最终态（MASS 权威 typed state 只作公开设计证据；参数化本体正在独立运行，
  本班不重复启动）
- 方向：判停与云监督（SoulX、云监督均消费现有独立实验结果，不创建重复 H20 作业）

今日 H20 决定：两个候选均先离线；当前没有新增候选获得“414 零退化 + 净修≥16”的实测证据，
因此本班不启动 H20，避免与 CMTF、SoulX、参数化本体、云监督争抢动态池。
