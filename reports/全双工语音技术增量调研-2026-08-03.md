# 全双工语音技术增量调研

更新日期：2026-08-03（按当日结束时的项目状态回溯更新）

面向项目：
- StreamingModelHarness
- Cockpit Agent Radar
- 视觉、音频、文本与车控融合的智能座舱助手

## StreamingModelHarness 当前进展与问题

截至8月3日结束，项目已经从架构讨论进入同口径实测和负面方案淘汰：

- clean口径扩展到400条A/B基准，并加入A、B预取、B纯自主三方比较。
- H20同批100条语音中，A严格工具成功率80.49%，B为79.27%；A的RAG Recall@24为98.78%。
- B纯自主模式约79%，持久KV累积损失约22个百分点，说明长会话上下文引力已是实测问题。
- 已修复B版编造工具名且失败后不重试的问题。
- `probe_turn_end`实验证伪，Qwen原生logits不能直接承担在线判停。
- 投机预取和工具清单压缩均出现负优化，已回退或默认关闭。

| 当前问题 | 项目证据 | 8月3日相关技术 | 可执行实验 | 成功门槛 |
|---|---|---|---|---|
| 长会话KV导致能力下降 | B版KV累积损失约22个百分点 | M3-DuplexBench、Voice Memory、AdaMM、ReflectWorld-MM | 固定完整历史下测逐轮准确率，再把规则、实体和事件迁出模型KV | 8轮后任务成功率下降≤3个百分点 |
| 轮替和打断缺少可靠判停 | `probe_turn_end`负面结果；现有脚本不能覆盖自然轮替 | M3-DuplexBench | 增加SHIFT/PAUSE/BACKCHANNEL/BARGE_IN回放和独立判停器 | 不牺牲漏响应率的前提下降低误判停与打断延迟 |
| 音频前端缺少真实双讲鲁棒性 | clean/20dB已有数据，但没有AEC+NS双讲闭环 | faster-enhancer.c | AEC/LMPAN后接因果NS，对干净、噪声、回声分别分桶 | ASR CER和车控成功率改善且前端新增P95≤30ms |
| 视觉尚未进入在线Harness | 当前在线链路以音频和车控为主 | Mage-VL | 用codec变化区域作为视觉事件门，仅将关键事件写入KV | 视觉token下降≥70%，音频deadline不退化 |
| 车控错误缺少可审计持续修复 | A/B严格成功率约80%，仅靠扩大候选不能解决选错 | Voice Memory、ARDena | 建立受保留集约束的ASR/近义工具规则与独立Policy层 | 修复目标错误且全量回归不下降 |

## 一、先说结论

### P0：直接命中当前工程短板

1. **M3-DuplexBench**：直接补多轮、打断和固定上下文公平评测，用于量化B版KV累积和轮替问题；它是基准，不是判停模型。
2. **faster-enhancer.c**：可作为AEC后的常开因果降噪候选，但不能消除助手扬声器回声。
3. **Voice Memory**：适合把重复ASR和近义工具错误沉淀为可审计规则，必须通过保留集门控，不能直接改模型行为。

### P1：适合形成下一阶段架构

1. **Mage-VL**：用codec事件稀疏化持续视频，适合作为座舱“视觉VAD”。
2. **AdaMM / ReflectWorld-MM**：把分析结果、实体和事件迁移到外部记忆，可能缓解无限KV，但需要座舱数据验证。
3. **ARDena**：将动态场景规则独立成Policy层，车控执行仍须经过typed call和确定性安全门。

### P2：长期模型与数据建设

1. **iFLYTEK-Embodied-Omni**：可借鉴“规划脑+动作小脑”分层，不应直接套用机器人动作模型。
2. **OmniVideo-100K**：适合借鉴实体锚定和跨段证据链来构造座舱音视频评测集。

## M3-DuplexBench

M3-DuplexBench从连续对话中提取SHIFT、PAUSE、BACKCHANNEL和BARGE_IN事件，
覆盖英语/日语、闲聊/多轮QA，并比较三种历史条件：

- 无上下文
- 只有用户历史
- 用户与系统完整teacher-forced历史

完整历史通常降低轮替延迟，但对领域和模型的效果并不一致。这说明全双工模型的
“什么时候说”不仅由当前音频决定，也会模仿历史对话节奏。

对StreamingModelHarness的落点：

- 将现有8轮测试扩展为固定完整上下文；
- 分别测跟随指令、指代、打断后恢复、KV裁剪和逐轮延迟；
- 为Qwen流式KV实现M3 adapter，而不是直接照搬Moshi输入格式。

## Mage-VL

Mage-VL不均匀抽取完整视频帧，而是复用H.264/HEVC等codec的信息：

- I帧保留完整patch；
- P帧只保留运动向量/残差表明真正变化的区域；
- System 1轻量事件闸门决定何时唤醒System 2的Qwen3-4B推理。

论文报告视觉token减少超过75%，最高获得3.5倍墙钟加速。

对座舱：

```text
摄像头持续流
  → codec动态区域/事件门
  → 只有乘员手势、视线、上下车、危险事件才写入视觉KV
```

这比固定每秒抽帧更适合长期在线助手。

## faster-enhancer.c

这是FastEnhancer-Medium的固定模型专用C/int8运行时：

- 48kHz，320样本一帧，6.67ms deadline；
- 无look-ahead，固定约14.67ms STFT对齐延迟；
- 启动后无堆内存分配；
- Apple M2单核RTF 0.069，Galaxy S23+ RTF 0.096；
- 相对fp32仅损失约0.006 PESQ。

建议链路：

```text
扬声器参考+麦克风 → AEC/LMPAN → faster-enhancer NS
→ RMS/FastTurn/SoulX → ASR/Qwen
```

它负责噪声增强，不负责消除助手扬声器回声，因此不能替代AEC。

## Voice Memory

Voice Memory不训练新权重，而是维护每个领域的可审计`memory.md`：

- Listener按记忆判断应纠正ASR还是保留1-best；
- Thinker异步提出有限规则修改；
- 只有保留集分数严格改善才接受修改。

结果：十个领域加权WER从8.36%降到7.52%，ATIS命令域从8.40%降至约3.4%，
同时显著减少过度纠错。

适合把车控反复错误沉淀为受门控规则：

- 副驾屏→副甲品
- 吸顶屏/顶屏同义
- 车型专有功能名
- 座位与部件简称

## AdaMM与ReflectWorld-MM

AdaMM认为相似度检索不适合回答“最近一周出现几次、趋势如何、谁最常做”。
它自动归纳重复属性并物化为表，同时保留topic→episode→event语义图。

ReflectWorld-MM围绕长期实体而非帧组织视频记忆：

- 有界短期感知；
- 多尺度情节记忆；
- 演化的实体语义记忆；
- 程序记忆；
- 持久数据库与证据回溯。

二者可共同解决座舱KV无限增长：

```text
短期语音/视觉 → 当前KV
重复偏好/车况 → AdaMM分析表
乘员/物品/事件 → ReflectWorld实体记忆
```

## iFLYTEK-Embodied-Omni

该模型把VLM和视频生成作为高层“脑”，把Action Generation Model作为“小脑”，
通过共享注意力协作。其机器人动作空间不能直接用于车控，但分层思想适用：

```text
Qwen/Omni：理解、规划、未来状态预测
确定性VAL/Executor：schema、权限、前置条件、幂等、状态提交
```

模型报告LIBERO-Plus零样本平均成功率89.6%，RoboTwin 2.0约93%。

## OmniVideo-100K

数据引擎先生成主实体、全局摘要和分段音视频脚本，再从跨段线索生成QA与证据链，
避免声音与画面分开caption后失去对应关系。

- 5,214个视频；
- 100K音视频QA；
- 505条人工核验测试；
- Qwen3-Omni-30B微调后OmniVideo-Test提升约13.86%。

可用于构建“乘员说话+手势+车外事件+车辆状态”的座舱联合数据。

## ARDena

ARDena用Prompt Construction Engine组合：

- 持久用户上下文；
- 当前场景约束；
- 交互状态；
- 当前允许工具。

适合定义驾驶、驻车、儿童在车、充电、休息等模式。需要强调：prompt控制不是安全
证明，真实车控仍需确定性Policy Gate。

## H20 A/B结果

同批100条语音：

| 指标 | main(A) | harness_b(B) |
|---|---:|---:|
| 车控严格成功 | 80.49% | 79.27% |
| RAG Recall@24 | 98.78% | 不适用 |
| ASR CER | 6.39% | 无ASR |
| 日常对话误车控 | 0% | 0% |
| 首文本P50 | 758ms | 131ms |
| 首动作P50 | 1,094ms | 700ms |
| 首音频P50 | 2,918ms | 3,491ms |

二者没有全面支配关系，均保留。Harness C建议并行运行音频KV和ASR/RAG：
B负责快速判断，A提供高覆盖候选，模型只能从候选中发typed call。

## 优先级

### P0

- A/B失败样本语义复核和参数/最终车态评分；
- 本地TTS替换Edge TTS；
- M3多轮评测适配；
- AEC+NS真实车内双讲数据。

### P1

- Harness C并行候选约束；
- Voice Memory车控ASR纠错；
- Mage-VL视觉事件门；
- 外部实体/分析记忆。

### P2

- OmniVideo座舱音视频数据构建；
- iFLYTEK式脑-执行器协作；
- ARDena场景策略。
