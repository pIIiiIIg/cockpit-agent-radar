# 全双工语音技术增量调研

更新日期：2026-08-21

对比基线：
- `reports/全双工语音技术增量调研-2026-08-20.md`

面向项目：
- StreamingModelHarness
- Cockpit Agent Radar
- 视觉、音频、文本与车控融合的智能座舱助手

## 本轮证据边界（先写清，后面所有数字都受它约束）

**项目侧无新增。** 项目快照仍是 2026-08-10 10:30 那一份，最新提交仍为 2026-08-04，最近一次 H20 A/B 仍是 2026-08-03。本轮没有新提交、没有新基准、没有新负面结果。

**Radar 连续第九轮未刷新。** `data/items.json` 的 `generated` 仍是 `2026-08-12T09:30:35+08:00`，条目仍为 499，新增 0 条。直接检索仍是唯一发现通道。

**索引继续按每天一批推进。** 08-20 报告记录当时全站最新投稿为 `2608.19197`（2026-08-19T17:58:56Z）。本轮同一查询（`all:a`，不限类别，按投稿时间倒序）返回 **`2608.20340`（2026-08-20T17:59:59Z）**，即只新增了 08-20 一个批次。截止时刻仍落在 17:5x 的 arXiv 每日 14:00 ET 截稿边界上，**这是连续第五次观测到索引按整批推进，从不逐篇推进**。

窗口按投稿时间取 **2026-08-19T18:00Z 至 2026-08-21T02:30Z**：起点定在 08-19 批次截稿边界之后，与 08-20 报告实际覆盖到的最后一篇（投稿时刻 2026-08-19T17:58:56Z）严格衔接且不重叠；实际可覆盖到的最后一篇为 2026-08-20T17:59:59Z。14 组查询去重后 **78 篇**。对照查询正常：`cat:cs.SD` totalResults **21,749**（08-20 为 21,740、08-19 为 21,728），`cat:eess.AS` **22,209**（08-20 为 22,202），`cat:cs.CL` 116,790，`cat:cs.MM` 9,947。**一切论文一律按投稿时间归属，不按发现时间归属。**

**下一轮窗口起点：2026-08-20T18:00Z。**

**本轮是"篇数中等、语音类目近乎枯竭、而最强线索来自非语音类目"的一轮。** 78 篇里语音相关的查询组几乎全线为零：`speech language model OR spoken dialogue OR speech-to-speech` **totalResults = 0**、`speech recognition AND (streaming OR online OR latency)` **0**、`spatial audio OR binaural OR ambisonic` **0**、`speech evaluation OR audio benchmark OR MOS prediction` **0**。**而本轮的 P0 来自 `cat:cs.HC` 一组，这是第 14 组查询连续第二轮独家命中当轮最重要线索。**

**本轮逐版复核为零新增，这是第一次。** 47 个追踪 ID 一次批量查询，3 个为非 v1（`2608.13416v2`、`2608.10878v2`、`2608.14097v2`），**三个全部是此前已经逐版差分过的**（StreamTTT 与 Ambisonics 于 08-19、X2-Turn 于 08-20）。没有任何追踪项在本窗口内产生新修订。**这一条必须明写，否则"版本通道无产出"会被误读成"没查版本通道"。**

**main 的恢复。** main 此前仍停在 2026-08-10 的报告（**连续第九轮**，其 HEAD 提交标题却是 `radar: 2026-08-12`）。本轮已先把 08-11 至 08-20 的 14 份报告与 8 份候选台账恢复到主线，再写今日报告。

制品复核时刻为 2026-08-21，晚于 02:30 的论文截止时刻。

## StreamingModelHarness 当前进展与问题

| 当前问题 | 项目证据 | 今日相关技术 | 可执行实验 | 成功门槛 |
|---|---|---|---|---|
| 首音频约 3 秒，瓶颈在外挂 Edge TTS | A/B 首音频 P50 2,918ms / 3,491ms，P90 3,453ms / 4,171ms，而首文本最快 131ms | **本轮无新增。** TTS 一组 3 篇中 2 篇是假阳性（量子张量网络去噪、粒子物理），唯一相关的 2608.19959 是合成语音欺骗性研究，不含合成质量或时延 | 沿用 08-20 的 X2Streaming-TTS 只读审阅计划 | 首音频 P90≤1,000ms；中文 CER 不劣于 Edge |
| 判停/打断"该不该发生"缺判据来源（**本轮最大进展**） | `probe_turn_end` 已实测主模型自判停不可行（负面结果） | **MaAI（`MaAI-Kyoto/MaAI`）**：实时连续的判停 / 应答（backchannel）/ 点头预测软件，**MIT 代码、支持中文、可纯 CPU 运行**，2026-08-09 起连续新增 VAD 与 backchannel 检测模型；配套论文 **2608.19527** 给出该机制的人因效果与一条**倒 U 型**约束 | 用 MaAI 的中文 `vap_bc_ch`（MIT 权重）在项目 452 条基准音频上跑离线应答时机预测，与现有端点判定并列记录；**先只做旁路观测，不接入播报路径** | 判停信号完全走音频通路之外的旁路；每个可插入点有可核验依据；**应答频次必须设上限而非越多越好** |
| 判定器处理不变性未验证（**优先级仍最高，且本轮第一次拿到完整可抄的量具**） | 2608.12880：只翻转与行为无关的元数据即改变 180 条中的 31 条判定 | **Thinkingbox（2608.19741）**：GPT-5.4 的 pass@20 **91.12%** 而 pass^20 只有 **25.25%**，且明写"工具调用层面的信号不是端到端任务完成的清晰代理"；**StateMemBench（2608.19652）Table 8 直接给出可照搬的样板**——判定器固定不变、只拨一个与行为无关的开关，**20% 的条目翻转而聚合只动 8 个百分点**，并报四个转移计数而非那个差值 | **第一步照搬 Table 8 的四格转移表**（选一个与行为无关的拨杆、判定器固定、报四个计数）；第二步把 452 条改造成封闭选项池并公布确定性份额；第三步补两个泄漏扫描器 | 聚合通过率旁必须并列逐条翻转率；**判定器最多只能索取二元判定，失效类别必须由构造固定**（mode-κ 仅 0.00–0.09）；**不得再用单次运行的 80.49% 严格工具名召回作为唯一头条** |
| 工具/动作通道缺"播报前动作门"的**可实现形状** | 项目此前只有 SemaPLC 给出的三条门不变量，没有可读的实现 | **EnvHarness（2608.19880）的 `Rules.filter_action`**：Apache-2.0 可读源码，逐步纯函数钩子，**是一个运行时而不是 LLM 审阅者**；**MaliciousSkillBench（2608.19901）的 `ABSTAIN_ERROR` 第三状态**：门自身出错绝不退化成放行，覆盖率与准确率分开报 | 只读审阅 `rules.py`（8,274 字节），把四条规则与 `ABSTAIN_ERROR` 写进动作门设计文档 | 拒绝必须返回实时车态加理由；**门永不得触碰评分器**；门错误永不等于放行；门覆盖率单列 |
| 回声消除与扬声器泄漏零一手依据 | 项目从未获得任何 AEC 正文证据 | **连续第九轮为零。** 本窗口该组 totalResults = **1**，且该 1 篇是无人机 IRS-ISAC 保密速率（非声学）。**但 MaAI 的双通道 VAD 第一次给了"泄漏的下游后果"一个可用制品**——它不消除回声，而是在串音下判断谁在说话 | 不写任何 AEC 结论；**MaAI VAD 只能记为"串音鲁棒的归属判定"，不得记为 AEC 证据** | 需要一篇因果、可流式、带下游指标、且含自身播放参考抽头的一手论文 |
| 多乘员插话、"谁在说话"无任何一手依据 | 项目评测集为单说话人合成音频 | 论文侧仍为零（`target speaker` 一组 1 篇且是 EEG 听觉注意解码 ASIC）；**制品侧首次有进展**：MaAI `vad` 双通道跨通道注意力，训练含噪声与 RIR 增强 | 沿用架构约束（身份判定走旁路）；**注意 MaAI 的双通道是"用户 vs 系统"而不是"乘员 A vs 乘员 B"，不可直接当多乘员方案** | 判定信号与音频通路解耦；多乘员需另找证据 |
| 工具/动作通道缺"播报前动作门" | 工具名严格召回 80.49%，无泄漏扫描器、无重复运行翻转率 | **Thinkingbox** 的判定用**确定性哈希断言**比对终态数据库与黄金态，LLM 只用于失败归因、不进入判定 | 照抄其"判定确定性、归因才允许用模型"的分层；并照抄其试次隔离要求 | 判定链路零 LLM；两次试次不得共享任何状态 |
| MoE 解码 110ms/token 与首包 prefill | 每 token 约 816 次 GPU→CPU 同步 | **ReCache（2608.19662）**：工具 schema 级 KV 复用，TTFT 26.319→7.200ms（3.655×）、KV 显存 −92.43% | **不进入候选。** 其 TTFT 定义为"缓存命中下"且明确排除离线构建成本 | 任何 prefill 收益必须在冷首次请求下复测 |

## 一、先说结论

### P0：MaAI —— 追踪表上第一个"中文可用 + 代码 MIT + 可纯 CPU 运行"的实时判停/应答制品

**这一条的形状与以往都不同：核心价值在制品，论文只是发现通道。** 本轮通过 `cat:cs.HC` 查询命中论文 **2608.19527**《Does Listening Matter? Backchanneling and Nodding in AI Clone》（京都大学 Inoue/Kato/Kawahara + Sony CSL Kasahara，2026-08-20T00:53:25Z 投稿，v1，5 页，cs.HC，ICMI 2026 Late-Breaking Results track）。论文本身是一份小规模人因研究，但它使用的预测组件指向一个此前从未进入过本项目追踪表的开源软件：**`github.com/MaAI-Kyoto/MaAI`**。

**一手核实结果（全部通过 GitHub / Hugging Face API 直接核对，非转述论文）：**

- 仓库存在，**MIT**（LICENSE 文件 1,078 字节，逐字读为标准 MIT，版权方 "MaAI Development Team"，2025 年），created 2025-07-04T07:12:01Z，**pushed 2026-08-19T08:02:06Z**，224 个 blob 合计 **88,247,753 字节**，120 stars，最新 tag `v0.2.15`。
- 功能覆盖四类：**判停（Turn-Taking，VAP）**、**语音活动检测（VAD）**、**应答预测与应答检测（Backchannel prediction / detection）**、**点头预测（Nodding）**。
- **语言覆盖含中文。** 主 README 自述 "Currently available for English, Chinese, and Japanese languages"。
- **可纯 CPU 运行。** 主 README 自述 "Thanks to its lightweight design, MaAI operates efficiently, even exclusively on CPU hardware"。
- **帧率是用户可设的参数**，因而算法块长可直接换算：判停与应答模型支持 **5 / 10 / 20 Hz**（CPC 编码器，`model_type="normal"`）或 **12.5 Hz**（Mimi 编码器，`normal-ver2`）；VAD 支持 **10 / 20 / 50 Hz**（CPC）或 12.5 Hz（Mimi）。
- **`MaaiMultiple` 让多个模型共享同一个音频编码器**，官方说明为"（相对昂贵的）编码器每音频帧只运行一次"。

**编辑判断：这正是项目 `probe_turn_end` 负面结果所指向的那种结构，而且是第一个不需要 GPU 的。** 项目已实测"主模型自己判停"不可行；X2-Turn（08-20 的并列 P0）给出的替代路线是"判停头与 ASR 头并行、不回灌解码"，但那是一个 4B 模型、要占 GPU。MaAI 给出的是**更彻底的解耦**：它完全不做 ASR、不做语义，只从声学流连续预测非语言行为，因此可以整个搬到 CPU 上，与被 MoE 主模型占满的 GPU 互不争抢。**对一个 GPU 已经是瓶颈的车机，这个属性的价值可能高于精度本身。**

**但开放状态是本轮最需要小心的一条，因为它是"代码一个许可证、权重三个许可证"的拼盘。** 详见第 1 节。一句话结论：**中文应答预测权重 `vap_bc_ch` 是 MIT，可商用；而中文判停主力权重 `vap_ch` 是 CC-BY-NC-ND-4.0，不可商用且不可衍生**；其 MIT 替代品 `vap_ch_kyoto` 存在但**能力严格更少**（只有 5Hz 与 10Hz 两个文件，无 Mimi 12.5Hz、无 20Hz、无短上下文变体）。**"MaAI 是 MIT 的"这句话是错的，必须逐模型看。**

### P0（同一条脉络）：2608.19527 的真正价值是一条倒 U 型约束，不是那三个显著性

**论文事实。** N=35 名日语母语者（本科/研究生），被试内设计，AB/BA 配衡，单尾配对 t 检验。加入应答与点头后显著改善的是三项：**注意感 5.29 对 4.80（p=.018）**、**真实感 4.77 对 4.29（p=.006）**、**共在感 4.60 对 3.94（p=.002）**；亲近感（p=.081）与参与感（p=.089）为边缘显著；其余四项无显著优势。九项量表内部一致性 Cronbach's α 为 .77 / .81。顺序效应检验九项全部 p>.21。

**论文事实（本条最重要）：注意感与应答次数呈显著的倒 U 型关系，二次项 p=.009，在中等数量处达到峰值。** 论文原话是 "simply providing more was not necessarily better"。点头与其余各项均未出现显著曲线关系。系统实际产生的量为：每段对话应答 **27.29 次（SD 7.55）**、每条用户话语 **2.25 次（SD 0.92）**；点头 **21.83 次（SD 6.10）**、每条话语 **1.76 次（SD 0.62）**。

**同等重要的负面结果，论文自己报了：用户的行为一点没变。** 用户话语条数 12.89 对 13.11（p=.516）、平均话语长度 7.30 对 6.93 秒（p=.195）、对话时长 4.90 对 4.83 分钟（p=.300），全部 p>.19。论文自己的解释是"主观评价的改善反映的是用户对交互的**感知**变化，而不是他们自身外显对话行为的变化"。

**编辑判断：这两条合起来，给出了一条项目可以直接执行的设计约束和一条不可用的论证。** 可用的是：**应答频次必须设上限，且"更多"会反向伤害注意感**——这是目前唯一一条把"应答不是越多越好"变成显著性检验结果的一手证据，项目若要做座舱应答，这条应当在第一版就写进参数表，而不是上线后靠投诉发现。不可用的是：**不能用它论证应答能提升任务效率或缩短交互**，因为论文自己的行为数据在这一点上是零效应。

### P1：Thinkingbox —— 它挑战的正是项目现在的头条指标

**论文事实。** 2608.19741《One Success Isn't Reliability: Thinkingbox, a Sandbox and Benchmark for Agents in Stateful Business Workflows》，微软，2026-08-20T07:37:57Z 投稿，v1，45 页，cs.CL，论文许可证 CC-BY-4.0。507 个任务、每任务独立 **20 次**尝试、12 个模型，即每模型 **10,140** 次试次。**GPT-5.4 的 pass@20 达到 91.12%，而 pass^20（20 次全中）只有 25.25%。**

**编辑判断：这一条直接指向项目的 80.49% 严格工具名召回。** 论文的核心主张是 "tool-call-level signals are not clear proxies for end-to-end task completion"（工具调用层面的信号不是端到端任务完成的清晰代理）。项目当前的头条数字恰恰是一个工具调用层面的单次运行指标。**加上 08-20 从 ComponentBench 记下的"两次运行聚合完全相同（52.5%）却有 17.3% 的逐条翻转"，现在有两条独立证据说明单次运行的聚合数字会系统性高估可靠性。** 本轮的建议因此是具体的：**把 452 条自建判定改报 pass@k 与 pass^k 一对数字。**

**同等重要的正面记录，而且是 08-20 那批的干净反例。** 08-20 记录的三篇（SemaPLC、ComponentBench、2608.18351）判定器或真值都由作者或 LLM 造，人工一致性统计全部为零。**Thinkingbox 反过来：它的主判定是确定性的哈希断言**，比对终态数据库与任务特定的黄金态，LLM 只用于给失败轨迹打归因标签。论文自己把这个取舍写明了：

> "We accept this asymmetry to keep the primary metric deterministic; extending LLM-judged rubrics to every task would reintroduce judge variance into the verdict."（我们接受这种不对称，以保持主指标的确定性；把 LLM 评判的评分细则扩展到每个任务，会把评判者方差重新引入判定。）

**这正是项目"判定链路不得引入 LLM 判定器"这条既定规则的第一份外部同向证据**，此前记录的都是反面教训。

### P1（并列）：ReCache 命中了项目的成本结构，但它的 TTFT 定义正是 08-20 记下的那个陷阱

**论文事实。** 2608.19662《ReCache: Efficient KV Cache Reuse and Compression for Tool-Augmented LLM Agents》，2026-08-20T05:57:24Z 投稿，v1，17 页。摘要给出：资源级注意力在 Inv-F1 上 **82.3% 对 Dense 的 82.4%**（差 0.1 个百分点）而 **TTFT 加速 3.655×**（26.319ms → 7.200ms）；完整框架把已分配 KV 张量显存降低 **92.43%**、注意力加速 **1.423×**。规模扩展上：Dense 的 TTFT 随资源上下文增长"从几十毫秒涨到 5,000 毫秒以上"，而其 Ω_full 维持在 50ms 以下，ReCache 近乎平坦、略高于 **5ms**；TPOT 方面 Dense 与 Ω_full 约 11ms 而 ReCache 略高于 6ms；注意力时延 Dense 与 Ω_full 约 5ms 而 ReCache 低于 **0.2ms**；显存 ReCache 封顶在 **0.03 GiB** 而 Dense 与 Ω_full 逼近 **8 GiB**。

**为什么它值得记：项目已经有一条相关的账。** 08-19 记录 2608.14036 的结论是技能"每任务多花 **93.8K** 输入 token"。ReCache 处理的正是这笔开销的结构——把工具/资源 schema 的编码与实际资源访问分开，schema 部分预先编码、跨请求复用。

**但两条边界让它不能进入候选，第一条尤其重要：**

1. **它的 TTFT 明确定义在"缓存命中"之下。** 原文："We use time-to-first-token (TTFT) to quantify online prefill latency **under a cache hit**, including request-local KV materialization but **excluding offline cache construction**."（在缓存命中下量化在线 prefill 时延，包含请求局部的 KV 物化，但**排除离线缓存构建**。）**这与 08-20 从 `Qwen3TTS-Streaming` README 记下的 `prefix-cache 命中` 是同一个陷阱，而且这次是论文在指标定义里自己写明的。车机的第一句话永远是冷的，因此这组数字对项目最关心的那一次请求不成立。**
2. **代码仓库存在但没有许可证。** 详见第 4 节。

### P1（并列）：本轮的方法学收获——制品通道找到了论文通道结构上看不见的东西

**MaAI 不是新东西，它建于 2025-07-04，比本项目的追踪表还早。它之所以到今天才被发现，是因为它十三个月里没有产出过一篇会被本项目的 14 组关键词命中的论文。** 而它在最近十二天里连续新增了实质功能：2026-08-09 加入 VAD 模型、2026-08-12 加入中文 `vad_ch`、2026-08-17 加入应答检测模型、2026-08-19 加入单通道模式。**这四次更新全部落在本项目最近几轮的报告窗口内，全部被漏掉，原因是它们没有论文。**

**编辑判断：这是第三条发现通道，必须与前两条并列固化。** 第一条是 `submittedDate` 窗口查询（看不见修订版）；第二条是追踪表逐版复核（只能看见已在表内的条目）；**第三条是制品通道——按机构/作者反查 GitHub 与 Hugging Face，它能看见"根本没有论文的活跃工程"。** 08-20 已经记过一次它的价值（一次 `/users/X-Square-Robot/repos` 调用同时查清三件事），本轮是第二次，且这次它找到的是一个**完全不在追踪表里**的项目。

### P2：本轮的零结果覆盖面比正面结果更大

见第 6 节。**AEC 连续第九轮零一手依据**；语音语言模型、流式 ASR、空间听觉、音频评测四组查询本窗口 totalResults 直接为 **0**；`full-duplex OR turn-taking OR barge-in OR interruption` 一组返回 3 篇而 **3 篇全是假阳性**，且**本轮真正的判停进展再一次不是被这组命中的**。另发现一处新的查询写法缺陷（见 6.7）。

## 1. MaAI（`MaAI-Kyoto/MaAI`）

- 仓库：`https://github.com/MaAI-Kyoto/MaAI`（源码 **MIT**）
- 模型：`https://huggingface.co/maai-kyoto`（**34 个模型，许可证不统一，见下**）
- API 文档：`https://maai-kyoto.github.io/MaAI/`
- 关联论文（本轮发现通道）：`https://arxiv.org/abs/2608.19527`
- 上游依赖：VAP 原始实现 `https://github.com/ErikEkstedt/VoiceActivityProjection`；Mimi 编码器 `https://huggingface.co/kyutai/mimi`（CC BY 4.0）；CPC 预训练模型来自 `https://github.com/facebookresearch/CPC_audio`

### 它是什么

一个**实时、连续、轻量**的非语言行为预测软件包，定位是给口语对话系统与交互机器人做"什么时候该接话、该应答、该点头"的判断。它的方法基座是 **Voice Activity Projection（VAP）** 及其扩展。名称取自日语"間 / 間合い"，指人在交互中用各种模态调节的细微时机与间距。

**必须先说清它不是什么：它完全不做 ASR、不做语义理解、不生成文本。** 它只从声学流输出概率。这一点决定了它在项目里的位置——**它是一个旁路信号源，不是一个对话模型**，这正是项目 `probe_turn_end` 负面结果之后应当走的方向。

### 核心方法（或核心架构）

**四类模型，逐条记录官方说明：**

- **判停（Turn-Taking）**：直接使用原始 VAP，预测"下一刻哪一位参与者会说话"。分三个变体：基础 VAP、**噪声鲁棒 VAP（官方标注为"Recommended"）**、单通道 VAP。
- **语音活动检测（VAD）**：官方说明写明它与判停模型的区别是"**不预测未来**：它检测每位说话人**此刻**是否在说话"。关键机制是**跨通道注意力联合处理两个通道**，官方原话："even when one speaker's voice leaks into the other speaker's microphone (crosstalk), the model can still decide who is actually talking."（即便一位说话人的声音泄漏进另一位的麦克风即串音，模型仍能判断实际是谁在说话。）训练时加入了噪声与**混响（RIR）增强**，官方因此说明"噪声鲁棒配方就是默认配方……**`vad` 本身就是多条件模型**"，故没有单独的 `vad_mc` 模式。输出为每通道 [0.0, 1.0] 的概率；二值化默认阈值 **0.5**，官方另注 **0.54 在训练所用开发集上取得最佳 F1**。
- **应答（Backchannel）**：分两类。**预测**模型输入 2 通道 16kHz 音频（ch1 用户、ch2 系统），输出应答后验概率 `p_bc`；另有**两类型**版本。**检测**模型则判断"当前正在说的这句话本身是不是一个应答"。
- **点头（Nodding）**：三类点头的预测，另有带运动学参数的版本。

**一条直接可借的工程设计：`MaaiMultiple` 让多个模型共享同一个音频编码器**，官方说明为"当多个 Maai 模型需要跑在同一条音频流上时，`MaaiMultiple` 类在它们之间共享单个音频编码器，因此（相对昂贵的）编码器每音频帧只运行一次"。源码侧可佐证这个"相对昂贵"的说法：`src/maai/encoder.py` 为 **71,539 字节**，是全包最大的单个源文件之一。

**帧率与上下文是显式参数，因此算法块长可以直接换算：**

| 模式 | `model_type` | 可选 `frame_rate` |
|---|---|---|
| 判停 / 应答预测 | `normal`（CPC 编码器） | 5, 10, 20 Hz |
| 判停 / 应答预测 | `normal-ver2`（Mimi 编码器） | 12.5 Hz |
| VAD | `normal`（CPC 编码器） | 10, 20, 50 Hz |
| VAD | `normal-ver2`（Mimi 编码器） | 12.5 Hz |

**编辑判断：这张表是本条最实用的部分。** 10 Hz 意味着 100ms 一个块，50 Hz 意味着 20ms 一个块，**而项目此前记录的判停时延口径（X2-Turn 的 288ms 相对词尾）根本无法与之直接比较**。MaAI 把"算法块长"这一项变成了可选参数，项目可以据此在"块长 / 精度 / CPU 占用"三者间自己做取舍——**这是追踪表上第一个把这个取舍交给使用方的判停制品**。

**注意 12.5 Hz 又一次出现。** 08-20 记录 Qwen3-TTS 生态存在 12 Hz 与 12.5 Hz 的命名/实际帧率脱节，两处独立出现。**本处不是同类问题**：MaAI 明确写出 12.5 Hz 对应 Mimi 编码器、10/20/50 Hz 对应 CPC 编码器，两者是不同模型而非同一模型的两种说法。**记录在案是为了避免日后把三处混为一谈。**

### 论文结果

**必须写死的一条：本轮没有从一手来源核实到 MaAI 任何一项精度指标。**

- 论文 2608.19527 **不报告任何应答预测的精度、召回或时机误差**，也不报告任何时延。它是一份主观感知研究。
- 仓库 README **不给出任何数字**，它把精度指标全部指向三篇外部论文（IWSDS 2024 判停、LREC-COLING 2024 多语判停、NAACL 2025 应答预测、以及一篇噪声鲁棒 VAP 的现场实验）。
- **这四篇本轮均未核实。** 因此**本报告不给出 MaAI 的任何精度数字**，只给出已一手核实的工程属性（许可证、文件、帧率、语言、CPU 可运行性、更新活跃度）。

**2608.19527 自身的结果**（该系统在其中的角色是被集成方，评的是整套 AI clone 而非 MaAI 本身）：

| 项目 | 有反馈 | 无反馈 | p（单尾配对 t） |
|---|---|---|---|
| Q3 注意感 | **5.29** | 4.80 | **.018** |
| Q6 真实感 | **4.77** | 4.29 | **.006** |
| Q8 共在感 | **4.60** | 3.94 | **.002** |
| Q4 亲近感 | — | — | .081（边缘） |
| Q5 参与感 | — | — | .089（边缘） |
| 用户话语条数 | 12.89 (2.52) | 13.11 (2.13) | .516 |
| 平均话语长度（秒） | 7.30 (2.91) | 6.93 (3.15) | .195 |
| 对话时长（分钟） | 4.90 (0.25) | 4.83 (0.19) | .300 |

**倒 U 型（本条最重要）：注意感对应答次数的二次回归项 p = .009，在中等数量处达峰。** 点头与其余各项均无显著曲线关系。

### 对 StreamingModelHarness 的帮助

**第一，它是追踪表上第一个可以不占 GPU 的判停制品。** 项目的 GPU 已被 110ms/token 的 MoE 主模型占满，而 X2-Turn 那条路线要再加一个 4B 模型。MaAI 自述可纯 CPU 运行，且提供 5–50 Hz 的帧率档位让使用方自行权衡 CPU 占用。**这使得"判停旁路"第一次成为一件不需要先解决显存与调度冲突就能试的事。**

**第二，中文应答预测权重是 MIT，可以直接用在商业车机上。** `maai-kyoto/vap_bc_ch`（created 2025-11-19T08:08:42Z，非门控、非私有，**license:mit**）含四个权重文件，覆盖 5 / 10 / 20 Hz 的 CPC 版本与 12.5 Hz 的 Mimi 版本，上下文均为 20,000 毫秒。**这是本轮唯一一个"中文 + 判停/应答方向 + MIT + 非门控"的权重。**

**第三，双通道 VAD 给了"扬声器泄漏"这个长期空白一个可用的下游制品，但必须严格限定它解决的是哪一半。** 项目的 AEC 空白已连续九轮，而 AEC 要解决的是"把自己播放的声音从麦克风信号里减掉"。**MaAI 的 VAD 不做这件事。** 它做的是在串音已经存在的前提下，判断此刻说话的是用户还是系统。**沿用既定判别式：ANC = 副扬声器 + 误差麦克风；AEC = 自身播放信号的参考/回环抽头；而 MaAI VAD 属于第三类——串音条件下的归属判定。** 这一条对项目仍然有用（打断判定需要知道"现在这段声音是不是用户在插话，还是我自己的播报漏回来了"），但**任何把它写成"AEC 有进展了"的表述都是错的**。

**第四，`MaaiMultiple` 的共享编码器设计可以直接照搬到项目的多头旁路上。** 项目若同时需要判停、应答时机与串音归属三个信号，天真做法是跑三个模型三次编码。官方给出的做法是编码器每帧只跑一次、多个轻量头共享。**这与项目在 MoE dispatch 上"每层固定代价"的推理是同一类工程思路，且这里有可读源码。**

**第五，2608.19527 的倒 U 型给了一条可以立刻写进参数表的约束。** 应答频次要设上限。论文的实测量级是每条用户话语 2.25 次应答、每段 5 分钟对话 27.29 次，**但项目不能直接抄这个数**——它是日语、实验室、平板、非驾驶场景。可抄的是**形状**：单调递增假设是错的，参数表里应当有上限而不是只有下限。

### 局限与开放状态

**开放状态必须分四栏记，而本条的权重一栏是目前追踪表上最碎的一次。**

- **论文**：2608.19527 公开（arXiv v1），论文许可证 `creativecommons.org/licenses/by-nc-nd/4.0/`（**这是论文本身的许可证，与代码、权重无关**）。
- **代码**：**公开且为 MIT**，LICENSE 文件 1,078 字节，逐字读为未修改的标准 MIT 文本。仓库活跃：最近提交依次为 2026-08-19（单通道模式处理）、2026-08-17（新增应答检测模型）、2026-08-13（VAD 文档更新、`MaaiMultiple` 修复）、2026-08-12（新增中文 `vad_ch`）、2026-08-09（新增 VAD 模型）。
- **权重**：**公开、全部非门控非私有，但许可证是四个家族的拼盘**。34 个模型中：

| 许可证 | 代表模型 | 对商业车机 |
|---|---|---|
| **MIT** | `vap_bc_{jp,en,ch,tri}`、`vap_*_kyoto` 系列、`vap_nod_jp`、`vap_nod_para_jp`、`vap_prompt_jp` | **可用** |
| **CC-BY-NC-ND-4.0** | `vap_{ch,en,jp}`、`vap_mc_*`、`vap_ca`、`vap_fr`、`vad_jp`、**`vad_ch`**、`bc_det_jp`、**`bc_det_ch`** | **不可用**（非商业 **且** 禁止衍生） |
| **CC-BY-NC-SA-4.0** | `vap_tri`、`vap_mc_tri`、`test` | **不可用**（非商业） |
| **CC-BY-4.0** | `continuous-mimi-onnx` | 可用 |
| **无许可证标签** | `vap_bc_2type_jp`、`bc_det_en` | **未知**，按不可用处理 |

- **这个拼盘里最关键的一条：中文判停主力权重不可商用，而它的 MIT 替代品能力严格更少。** `maai-kyoto/vap_ch`（CC-BY-NC-ND-4.0）含 **9 个权重文件**，覆盖 5/10/20 Hz 与 2,500 / 3,000 / 5,000 / 20,000 毫秒多种上下文长度，外加 Mimi 12.5 Hz；而 MIT 的 `maai-kyoto/vap_ch_kyoto` 只有 **2 个文件**（`vap_state_dict_ch_kyoto_5hz_20000msec.pt` 与 `..._10hz_20000msec.pt`）——**没有 20 Hz、没有 Mimi 12.5 Hz、没有任何短上下文变体**。项目若要商用，判停侧只能用这两个文件。
- **同样关键：最近十天新增的三个中文/日文模型全部是 CC-BY-NC-ND-4.0。** `vad_ch`（created 2026-08-12T07:32:54Z）、`bc_det_ch` 与 `bc_det_jp`（均 2026-08-17）——**即"双通道串音归属判定"这个对项目最有吸引力的能力，其中文权重恰恰是不可商用的那一档。**
- **数据**：**全部未发布。** 应答模型的官方文档只写训练自 "Online Conversation Dataset"（附一个 arXiv 链接），VAD 模型的文档不给出任何数据集名称。
- **依赖侧的许可证不传递，且官方自己写明了。** 仓库 README 的许可证段原话："The source code in this repository is licensed under the MIT License. **For the trained models, please follow the license described in the README of each model or on Hugging Face repository.**" 另注 Mimi 为 CC BY 4.0、CPC 预训练模型"请遵循其自身的特定许可证"。**这是"代码许可证从不传递到权重"的又一个例证，而且这次是上游自己主动声明的。**

**一处必须原样保留的官方文档自相矛盾，不替作者选择。** `readme/vap_bc.md` 的"Supported Languages"小节第一句写 **"Currently, only Japanese is supported."**，但紧接着的同一份文档里就列出了 English（`lang=en`）、Chinese（`lang=ch`）与 Multilingual（`lang=tri`）三个小节，其参数表同时列出 jp/en/ch/tri 四个取值；主 README 的更新日志写 "Backchannel prediction model supporting three languages (English, Chinese, and Japanese) is now available (November 19th, 2025)"；Hugging Face 上 `vap_bc_ch` 的 createdAt 正是 2025-11-19T08:08:42Z。**四处证据指向"中文已支持"，一处句子指向"只有日语"。本报告的判断是那句话是陈旧残留，但按规矩两者并存记录，且项目在实测前不得把中文支持当作已确认。**

**论文 2608.19527 自身的局限，逐条列出：**

1. **基础管线是按键说话（push-to-talk），不是全双工。** 论文原文："The system adopts a push-to-talk interface: the user presses and holds a button at the bottom of the screen, and the recorded speech is processed through the ASR→LLM→TTS pipeline upon release." **因此这篇论文完全没有提供任何关于打断、抢话或重叠语音的证据**，它证明的是"在半双工主通道之外并联一条连续的倾听反馈通道"有感知收益。**这个区分对项目很重要：它是旁路通道的证据，不是全双工的证据。**
2. **全文零时延数字、零硬件规格。** 只写明系统经浏览器跑在一台 Android 平板上。**没有 CPU 型号、没有推理耗时、没有端到端反应时延。** 因此本篇不能为任何时延预算提供依据。
3. **主管线全部是云服务**：ASR 为 Deepgram nova-2、LLM 为 GPT-4.1（`gpt-4.1-2025-04-14`）、TTS 为 Cartesia sonic-3。**与项目的本地部署前提完全不同。**
4. **应答音频是预生成的固定片段随机抽取**（日语 "un"、"un-un" 等），不是实时合成；点头是把头像图片做垂直动画，单次点头 70% 概率、双次 30%。**这是原型级实现，不是产品级。**
5. **N=35，全部为日语母语的本科/研究生，报酬 500 日元图书卡。** 单一人群、单一语言、单一被克隆对象（第一作者本人）。论文自己列为待办的正是这三条，外加"应答与点头是一起开启的，未来实验应当拆开两个模态以厘清各自贡献"。
6. **主观量表九项中六项无显著优势**，其中包括理解度、兴趣、交谈意愿与**节奏感（Q9）**。**节奏感无显著改善这一条对项目尤其值得注意**——加了实时应答之后，用户并没有觉得"交互节奏更顺畅"。
7. **倒 U 型是探索性分析**，论文自述为 "As an exploratory analysis"，且只在三项显著项上拟合、只有注意感一项的二次项显著。**单一研究、N=35、事后拟合，方向可信度高于数值可信度。**

**综合判断：进入"制品离线评估"，不进入候选序列。** 理由有三：本轮未从一手来源核实到任何精度指标；中文判停主力权重与中文 VAD 权重均为不可商用许可证；以及项目尚未测过它在车内噪声下的表现。**但它是本轮唯一一项可以立刻动手、不占 GPU、且中文应答权重许可证干净的实验。**

## 2. Thinkingbox（2608.19741）

- 论文：`https://arxiv.org/abs/2608.19741`（v1，2026-08-20T07:37:57Z 投稿，45 页，cs.CL）
- 代码：`https://github.com/microsoft/thinkingbox`（**MIT**）
- 论文许可证（arXiv 元数据 `/License`）：`creativecommons.org/licenses/by/4.0/`
- 作者：Zhuochun Li、Youngmin Ko、Ali Keramati（三人共同一作）、Nicola Ferri、Susana Palmaz Lopez Pelaez、Liang-Chun Tsai、Calvin Wang、Mirco Milletari、Tuhin Kundu、Vadim Smolyakov、Kjartan Olafsson、Tommy Guy。论文注明"共同一作；工作完成于微软实习期间"。

### 它是什么

一个**有状态业务工作流**下的 agent 沙箱与基准。它跑的是"模拟用户 ↔ LLM agent ↔ 隔离的 MCP 兼容工具"三方多轮交互，然后**用终态、副作用与对话三类可执行检查**来判定，而不是判定一个最终答案。五个业务域：零售（Retail）、预订（Booking）、保险（Insurance）、新型银行（Neobank）、咨询（Consulting）。

它的出发点用论文自己的话说是："Thinkingbox is motivated by a limitation of many function-call-oriented tool-use..."，而结论句更直白：**"tool-call-level signals are not clear proxies for end-to-end task completion."**

### 核心方法（或核心架构）

**（一）确定性判定，LLM 只用于归因。** 主评估把最终数据库状态与黄金期望状态用**确定性的、基于哈希的断言**比对。原文："The agent succeeds only when it produces the exact required side effects without introducing incorrect additional effects."（agent 只有在产生了确切要求的副作用、且没有引入错误的额外副作用时才算成功。）解码失败、测试执行失败、API 错误与其它系统错误一律计为失败试次。**LLM 评判者只用于给失败轨迹打归因标签，不进入判定。**

**（二）试次隔离是硬要求。** 原文："Two attempts of the same task must not share database rows, cached tool state, or side effects, otherwise pass@k and training rewards become unreliable."（同一任务的两次尝试不得共享数据库行、缓存的工具状态或副作用，否则 pass@k 与训练奖励会变得不可靠。）

**（三）两个指标必须成对报。** 论文的定义：**pass@k 是"k 次中至少一次成功"的概率**，**pass^k 是"k 次全部成功"的概率**。原文的区分句："pass@k measures whether retries can *discover* a successful trajectory, whereas pass^k measures dependable repeated execution."（pass@k 度量重试能否**发现**一条成功轨迹，而 pass^k 度量可靠的重复执行。）另报 95% 的**任务簇自助置信区间**（重采样 507 个任务、保留每个被采样任务内的全部 20 个结果）。

**（四）任务筛选规则值得单独抄。** 剔除的类别包括：工具损坏、初始状态不一致、目标含糊、结果不可核验、**检查依赖于工具调用轨迹**、以及在 LLM agent 下反复无法终止的交互。原文要求"被接受的任务必须至少存在一条有效的完成策略，同时保有清晰的正确性判据"。**"剔除依赖工具调用轨迹的检查"这一条与它的头条主张是自洽的**：既然主张工具调用层面的信号不是完成度的代理，判定就不能建立在轨迹上。

**（五）失败归因是确定性优先级，且论文自己给它降级。** 论文用固定的优先级顺序给每条失败轨迹指派唯一的主导特征，并自述这些特征"是可观测的诊断，而不是唯一的因果解释"。

### 论文结果

**头条（Table 11）：GPT-5.4 的 pass@20 为 91.12%，pass^20 仅为 25.25%。** 论文的概括是 "Repeated inference can often find one successful trajectory without making the agent dependable."（重复推理常常能找到一条成功轨迹，却并不能让 agent 变得可靠。）

**同表中已核实的另外几行：**

| 模型 | pass@1（95% CI） | pass^20 | pass@20 | 0/20 的任务数 | 20/20 的任务数 |
|---|---|---|---|---|---|
| MiniMax-M2.5 | 0.19% [0.00%, 0.53%] | 0.00% | 0.59% | 504 | **0** |
| Qwen3-8B | 0.13% [0.01%, 0.34%] | 0.00% | 0.99% | 502 | **0** |

**另有一行的模型名在 PDF 抽取中与相邻列粘连、无法唯一确定，其数值为 pass@1 CI [3.66%, 5.75%]、pass^20 0.00%、pass@20 25.44%、378 个任务 0/20、0 个任务 20/20。该行的模型归属记为待核，不作猜测。**

**规模：** M = 507 个任务，每任务 n = 20 次独立评估，即每模型 **10,140** 次试次，共 12 个专有与开放权重模型。论文强调 "These attempts are independent stochastic samples rather than deterministic reruns."（这些尝试是独立的随机样本，而不是确定性重跑。）

**模拟用户的规格与它的诚实自述。** 固定使用 GPT-5.4-mini 部署，温度 **0.3**、无推理努力、**4,096** token 补全上限，最多贡献 **10** 个追问轮次。因为采样非确定，"20 次试次在用户措辞上也各不相同，因此可靠性结果度量的是对一族用户交互分布的鲁棒性，而不是对单一固定对话的重复"。论文随后逐条列出模拟用户不具备的真实用户属性："it does not misremember or misstate facts, does not change its goal mid-conversation, does not become uncooperative under repeated failure, and its brevity is a fixed instruction rather than a behavioral trait that varies by user."

### 对 StreamingModelHarness 的帮助

**第一，它给项目的头条指标提出了一个必须回答的质疑。** 项目当前对外的数字是**单次运行的严格工具名召回 80.49%**，这既是工具调用层面的信号，又是单次运行的聚合值——**恰好同时踩中论文批评的两点**。加上 08-20 从 ComponentBench 记下的"聚合完全相同却有 17.3% 逐条翻转"，两条独立证据同向。**本轮的具体建议：把 452 条自建判定改报 pass@k 与 pass^k 一对数字，并在聚合值旁并列逐条翻转率。**

**第二，pass@k / pass^k 的取舍对座舱有一层特殊含义。** 论文场景是业务工作流，重试是允许的，所以 pass@20 有意义。**车控场景没有这个奢侈：用户说"打开空调"，系统没有第 20 次机会。** 因此对项目而言，**pass^k 才是那个对齐业务语义的指标，pass@k 只用于诊断"能力存在但不稳定"与"能力不存在"的区别**。这一条项目应当在采纳时明确写下，否则容易照抄论文的呈现顺序而误导。

**第三，判定架构的分层可以整体照搬。** 判定确定性、归因才允许用模型、且归因结论明确降级为"可观测诊断而非因果解释"。**这是项目"判定链路不得引入 LLM 判定器"这条规则的第一个正面外部先例。**

**第四，试次隔离这条硬要求项目目前不满足也无从验证。** 论文的表述已经到了可执行的粒度（不得共享数据库行、缓存的工具状态或副作用）。项目的 452 条评测若要改报 pass^k，**第一步不是跑 20 遍，而是先证明两次运行之间没有共享状态**，否则 pass^k 会被系统性高估。

### 局限与开放状态

- **论文**：公开（arXiv v1），CC-BY-4.0。
- **代码**：**部分兑现，这是本轮的一个新形态，必须单独记。** 论文原文写 "We release both THINKINGBOX and THINKINGBOX-BENCH: https://github.com/microsoft/thinkingbox"（我们同时发布 Thinkingbox 与 Thinkingbox-Bench）。该 URL **确实解析**，仓库真实且实质：`microsoft/thinkingbox`，**MIT**，created 2026-04-22T19:01:10Z，pushed **2026-08-20T17:19:40Z**（与论文投稿同日），142 个 blob 合计 **1,001,955 字节**，6 stars。**但仓库里只有沙箱框架，没有那 507 个任务。** 全树 `dataset/` 目录下只有三个与场景有关的文件：`dataset/scenario/cloud_drive.yaml`（396 字节）、`dataset/test_case/cloud_drive.meta.yaml`（807 字节）、`dataset/test_case/cloud_drive.py`（6,177 字节）。**`cloud_drive` 不属于论文的五个业务域中的任何一个。**
- **因此开放状态应记为第七种形态："URL 有效、仓库真实，但承诺的两件制品只兑现了一件。"** 它既不是第五档（URL 失效），也不是第三档（无 URL 的 unverifiable）——**它是"承诺 A 与 B，交付了 A"**。此前的五档加上 08-20 新增的"沉默式未发布"，本轮再加这一档。
- **并且这是 08-20 记下的"发布的是产品不是实验"这个形态的第二个独立实例。** 该仓库 README 长 20,489 字节，其中 `arxiv`、`thinkingbox-bench`、`507`、`pass@`、`retail`、`insurance`、`neobank` 的出现次数**全部为 0**。08-20 的第一例是 `midea-ai/SemaPLC`（README 全文不提论文、不提 arXiv、不提任何一个数字）。**两例形态完全一致：机构把工程框架开源了，把论文实验留在内部。**
- **权重**：不适用（评测的是 12 个第三方模型）。
- **数据**：**507 个任务与其黄金终态未发布**（见上）。
- **硬件与成本**：Qwen 系列经本地 vLLM 的 OpenAI 兼容端点提供服务，其余经 Azure API。**GPU 型号从未给出**，因此它加入 CoRun、AATC、SemaPLC、DuplexGen-B、2608.17972 的无硬件名单。论文报告了每轮 token 用量的相对趋势，并自述"更大的每轮上下文或更多轮次并不能可靠地转化为正确的状态转移"。
- **主判定不覆盖用户可见的沟通质量。** 论文自述："Reported pass rates therefore measure correct, policy-compliant backend outcomes rather than the fidelity of user-facing communication."（因此报告的通过率度量的是正确且合规的后端结果，而不是面向用户的沟通的保真度。）**对座舱而言这是一处实质缺口**——播报内容对不对，恰恰是项目要管的事。
- **与项目的领域距离**：无语音、无中文、无每轮时延、无音频。**只借协议与指标，不借任何技术。**

## 3. ReCache（2608.19662）

- 论文：`https://arxiv.org/abs/2608.19662`（v1，2026-08-20T05:57:24Z 投稿，17 页）
- 代码：`https://github.com/EIT-NLP/ReCache`（**存在，但全仓库没有任何 LICENSE 文件**）
- 论文许可证（arXiv 元数据 `/License`）：`arxiv.org/licenses/nonexclusive-distrib/1.0/`
- 作者：Yichu Fang、Sitong Wei、Haozhe Hu、Xiaoyu Shen（通信邮箱分别为 `sjtu.edu.cn` 与 `eitech.edu.cn`）

### 它是什么

一个面向**工具增强型 LLM agent** 的资源级 KV 复用与压缩框架。核心主张写在摘要里：**把可复用的 schema 编码与选择性的资源访问分开**，从而"大幅降低 agent 推理成本，且有效性损失有限"。

### 核心方法（或核心架构）

三层，逐层叠加：

1. **资源级注意力（Ω_full）**：让资源表示以共享前缀预先编码、跨请求复用，配合**资源局部的位置重索引**。这一层单独就拿到了 3.655× 的 TTFT 加速。
2. **结构剪枝（Ω_{20,3}）**：按层与头组的贡献做 Top-K 选择（论文的敏感性分析用了 Top-K 层 KL 与 Top-K 头组 KG 两个维度）。
3. **语义剪枝（SMP）**：进一步削减语义字段的 token。

### 论文结果

**Table 2（资源级注意力消融）：**

| 方法 | Inv-F1 | ID-F1 | Halluc. | TTFT (ms) |
|---|---|---|---|---|
| Dense | 82.4 | 96.0 | 0.0 | 26.319 |
| Block | 82.2 | 95.8 | 0.1 | — |
| **Ω_full** | **82.3** | **96.0** | 0.2 | **7.200** |

论文对这张表的概括是 Ω_full 与 Dense"在各项有效性指标上相差至多 0.2%"，逐格核对成立。

**完整框架：** 已分配 KV 张量显存降低 **92.43%**、注意力加速 **1.423×**，同时"在域内保留了 Dense 的 97.5% Inv-F1"；分域看 Inv-F1 为 T_IND **80.3%**、T_OOD **60.8%**。

**规模扩展（Figure 4，四档资源长度 0–1K / 1K–5K / 5K–10K / ≥10K）：** Dense 的 TTFT "从几十毫秒涨到 5,000 毫秒以上"，Ω_full 维持在 **50ms 以下**，ReCache 近乎平坦、**略高于 5ms**。TPOT：Dense 与 Ω_full 约 **11ms**，ReCache 略高于 **6ms**。注意力时延：Dense 与 Ω_full 约 **5ms**，ReCache 低于 **0.2ms**；在 XL 档，Ω_full+SMP 把注意力时延压到 **0.5ms 以下**，而只做结构剪枝的 Ω_{20,3} 仍在 **2ms 以上**。显存：ReCache 封顶 **0.03 GiB**，Dense 与 Ω_full 逼近 **8 GiB**。

**硬件：** 主模型 Qwen3-4B 在一张 **NVIDIA A800** 上训练，初步实验的 Qwen3-1.7B 在一张 **NVIDIA RTX 5000** 上。评测覆盖七个工具/技能使用数据集。

### 对 StreamingModelHarness 的帮助

**它命中的是项目一笔已经记过账的开销。** 08-19 从 2608.14036 记录的是技能"每任务多花 93.8K 输入 token"，而 ReCache 处理的正是这笔 token 的结构——工具 schema 是跨请求高度重复的内容，理应只编码一次。**若项目的座舱工具 schema 规模可观，这条思路在方向上成立。**

**但本轮的结论是"记录方向，不采纳数字"**，理由见下。

### 局限与开放状态

**第一，也是决定性的一条：它的 TTFT 定义在缓存命中之下，且明确排除离线构建成本。** 原文："We use time-to-first-token (TTFT) to quantify online prefill latency **under a cache hit**, including request-local KV materialization but **excluding offline cache construction**." **这与 08-20 从 `Qwen3TTS-Streaming` README 里挖出的 `prefix-cache 命中` 条件是同一个陷阱，只是这次它出现在论文自己的指标定义里而不是藏在附注里。** 车机上电后的第一句话必然是冷的，**因此 26.319ms → 7.200ms 这个对比对项目最关心的那一次请求不成立**。论文没有给出任何冷启动或缓存未命中的数字，也没有给出离线构建一次要多久。

**第二，开放状态是一个此前未记录过的形态：代码可读但无许可证。** `EIT-NLP/ReCache` 确实存在（created 2026-08-19T03:12:43Z，pushed 2026-08-19T05:42:28Z，size 1,881 KB，1 star），论文摘要写 "The code is available at https://github.com/EIT-NLP/ReCache."。**但 GitHub API 的 `license` 字段为 `null`，且全树递归列举确认没有任何 `LICENSE` / `LICENSE.txt` / `COPYING` 文件。** 按 GitHub 默认条款，未附许可证的公开仓库保留全部权利。**"公开可读"不等于"开源可用"，这一条应当加入开放状态判据：许可证缺失是一种独立状态，不能与"已发布可核"合并。**

**第三，仓库结构显示它包含相当规模的训练基础设施，与"纯推理期优化"的读法不符。** 全树含 `capsule/main/train.py`、FSDP 与 DeepSpeed 的 stage2/stage3 配置、以及 `capsule/data/config/slimpajama.json`。论文侧也写明主模型是"训练"出来的（"We train the primary Qwen3-4B model on an NVIDIA A800 GPU"）。**因此不能把它归入"叠加在现成模型上的推理期改动"这一类**，它至少需要一次训练才能得到论文报告的效果。

**第四，批大小从未在正文中给出。** 本报告不据此触发"最小可评估批大小"这条否决规则——**规则要求写出到底是"从未在 1 上评测"还是"收益随批变小而缩水"，而本篇是"批大小根本没有报告"，这是第三种情况，按未知记录。**

**综合判断：REJECT 为候选，TRACK 为方向。** 决定性理由是缓存命中口径，次要理由是无许可证。**可带走的一条是它的指标拆分方式：把 TTFT、TPOT 与"注意力时延"分开报**，论文自己解释了为什么——注意力上的收益"会被 TPOT 里未变化的投影与前馈计算所掩盖"。**这条拆分纪律对项目诊断 110ms/token 同样适用。**

## 4. 术语与同名/同源辨析（本轮新增）

- **`MaAI`（软件包）与 `VAP`（方法）不是同一层的东西，也不要与本项目已记录的 `PersonaPlex` 混淆。** MaAI 是京都大学维护的实现，VAP 是其方法基座（原始实现在 `ErikEkstedt/VoiceActivityProjection`）。另注 2608.19527 的引文里出现了 **PersonaPlex（arXiv:2602.06053）**，与本项目 08-11 记录的"PersonaPlex 是一个独立的 S2S 模型、不是 DuplexGen 的一部分"一致，**本轮不改变该判定**。
- **`12.5 Hz` 在本轮第三次出现，但这一次不是矛盾。** 08-19（VoiceChat-TTS）与 08-20（Qwen3-TTS 生态）记录的是同一模型的两种说法互相打架；**MaAI 这里 12.5 Hz 明确对应 Mimi 编码器、5/10/20/50 Hz 明确对应 CPC 编码器，是两类不同模型**。记录在案是为了防止日后把三处并列成"同一个生态问题"。
- **`endpoint` 是本轮新增的关键词陷阱，且量级最大。** 见 6.7。
- **`Interrupting the Loop`（2608.19893）里的 "interrupting" 与语音打断无关**，它讲的是在基座语言模型里周期性更换话题对"判定惊讶度与连接感"的影响，属 cs.CL 文本方向。**"interrupt" 词根的第三个假阳性来源**（前两个是并行机排程与统计学的 "Interrupted Time Series"）。

## 5. 今日建议路线

### 第一阶段（零 GPU，可立即做）

1. **把 MaAI 的许可证拼盘做成一张项目内部表，并按"可商用 / 不可商用 / 未知"三分。** 这一步必须在任何技术评估之前完成，否则可能在不可商用的 `vap_ch` 与 `vad_ch` 上投入工作。**结论已经清楚：中文应答预测（`vap_bc_ch`，MIT）可用；中文判停只能用能力更少的 `vap_ch_kyoto`（MIT）；中文 VAD 与中文应答检测目前不可商用。**
2. **把"应答频次设上限"写进座舱交互参数表。** 依据是 2608.19527 的倒 U 型（注意感对应答次数的二次项 p=.009）。**抄形状不抄数值**——论文的 2.25 次/话语来自日语实验室平板场景。
3. **把 Thinkingbox 的 pass@k / pass^k 一对指标引入项目 452 条自建判定的报告口径**，并在聚合值旁并列逐条翻转率（后者的依据是 08-20 的 ComponentBench）。**先只改报告口径与最小子集试算，不急于跑满 20 遍。**
4. **在跑任何重复试次之前，先验证项目评测的试次隔离。** 依据是 Thinkingbox 的硬要求：两次尝试不得共享数据库行、缓存的工具状态或副作用。**隔离不成立时 pass^k 会被系统性高估，这一步不能跳。**
5. **判定器处理不变性检查**（优先级仍为最高，来自 2608.12880）。**本轮第一次有了完整可抄的量具：照搬 StateMemBench Table 8 的四格转移表**——选一个与行为无关的拨杆、判定器固定不变、**报四个转移计数而不是那个差值**。**这是本轮全部内容里工作量最小、收益最大的一项。**
6. **把 452 条判定改造成封闭选项池，并公布确定性份额。** 构造时同时算出金标准、一个已知坏基线会给出的具体错误答案、以及 2–3 个中性干扰项，提问时不展示池；随后报告"纯字符串匹配能定下多少条、判定器在该子集上的一致率是多少"（对照值 **28% 与 94.1%**）。**同时写几个可执行的座舱"坏读者"基线**（永远取最新槽位、取提及最频繁的槽位、不重算派生值、过早丢弃约束），**只保留至少一个坏基线与确定性重放不一致的条目**——这一步同时完成选样与失效标注，**并使注入故障召回率按构造成立**。另加一类"反陷阱"样本，用来抓住奖励过度作废的判定器。
7. **补两个泄漏扫描器**：条目侧（断言每条承重事实出现在其指定会话中，且逐条禁用短语不泄漏到渲染面，失败则重渲染）与特征侧（审计高权重特征里是否出现来源或批次标记）。
8. **只读审阅 `envharness/harnesses/rules.py`（8,274 字节，Apache-2.0），把四条动作门规则与 `ABSTAIN_ERROR` 第三状态写进动作门设计文档。** 不运行该仓库任何代码。
9. **给 452 条判定各打上"标注强度 × 证据类型"两条正交轴的标签，并写死"弱档证据永不用于设定阈值"**，同时预先声明一个 `llm` 证据档，使 LLM 标注在表里可见而不被悄悄提升。

### 第二阶段（单卡或纯 CPU 可做）

10. **用 `vap_bc_ch`（MIT）在项目 452 条基准音频上做离线应答时机预测，只做旁路观测。** 记录三组数字：不同 `frame_rate`（5 / 10 / 20 Hz）下的 CPU 占用、每帧耗时、以及应答触发点相对项目已有标注的分布。**不接入播报路径，不改生产链路。**
11. **离线复现 X2-Turn 的 EasyTurn-zh 判停指标**（08-20 的既定项，本轮未动，权重与代码仍是开放非门控的 Apache-2.0）。**做完第 10 项后，两条判停路线第一次可以在项目自己的音频上直接对比：一条要 4B + GPU，一条可纯 CPU。**
12. **测项目自身的上游 token 速率与抖动**（08-20 的既定项，X2Streaming-TTS 的 ρ 论证在项目条件下未经验证）。

### 第三阶段（需要环境或授权评估）

13. **若中文 VAD 的串音归属能力被判定为必需，需要就 `vad_ch` 的 CC-BY-NC-ND-4.0 与作者接洽授权**（README 写明"系统开发或使用 MaAI 的合作研究请联系京都大学 Koji Inoue"）。**在授权落定之前不得把 `vad_ch` 写进任何架构图。**
14. **评估 TensorRT/Triton 链路是否可接受**（08-20 的既定项，未动）。
15. **跟踪 `X-Square-Robot/X2Streaming-TTS` 是否出现**（08-20 设的两周期限，本轮为第二天，未复核）。

## 6. 本轮负面结果与零结果（与正面结果同等重要）

**这些是"查了、没有"，不是"没查"。每一条都记录了查询与 totalResults。**

### 6.1 回声消除：连续第九轮零一手依据

查询 `abs:"speech enhancement" OR abs:"noise suppression" OR abs:"echo cancellation" OR abs:"beamforming"` 在本窗口 totalResults = **1**，该 1 篇为 `2608.20278`（无人机挂载六维可动 IRS 辅助 ISAC 系统的保密速率最大化，eess.SP），**非声学**。**"beamforming" 连续第二轮 100% 落在非声学领域。** 查询继续保留。

**本轮的新情况必须与这条零结果分开写：MaAI 的双通道 VAD 是第一个触及"扬声器泄漏"这个问题域的可用制品，但它不是 AEC。** 沿用判别式：**ANC = 副扬声器 + 误差麦克风；AEC = 自身播放信号的参考/回环抽头；MaAI VAD = 串音条件下判断谁在说话。** 第三类既不消除也不抵消泄漏，它接受泄漏存在并做归属判定。**AEC 的计数保持为九轮零。**

### 6.2 四组查询本窗口 totalResults 直接为 0

- **语音语言模型 / 语音到语音**：`abs:"speech language model" OR abs:"spoken dialogue" OR abs:"speech-to-speech"` = **0**。
- **流式 ASR**：`abs:"speech recognition" AND (abs:"streaming" OR abs:"online" OR abs:"latency")` = **0**（连续第二轮为 0）。
- **空间听觉**：`abs:"spatial audio" OR abs:"ambisonic" OR abs:"binaural" OR abs:"sound source localization"` = **0**（连续第二轮为 0）。
- **音频评测**：`abs:"speech evaluation" OR abs:"audio benchmark" OR abs:"MOS prediction"` = **0**。

**这四组同时为零，而本轮仍然出了一条 P0，本身就是对"用查询组的空满程度判断产出"这种做法的又一次证伪。**

### 6.3 全双工组 3 篇，3 篇全是假阳性，且真正的进展再次不在其中

`abs:"full duplex" OR abs:"turn-taking" OR abs:"barge-in" OR abs:"interruption"` totalResults = **3**：`2608.19893`（基座语言模型里周期性话题更换对判定惊讶度与连接感的影响，cs.CL）、`2608.19887`（时变时滞诱导的间歇性集群与分形集体秩序，nlin.CD）、`2608.19622`（数据中心灵活性价值取决于所在电网，physics.soc-ph）。

**本轮真正的判停/应答进展（2608.19527 与 MaAI）没有被这组命中，它来自 `cat:cs.HC`。这是连续第二轮出现同一形态**——08-20 的两条 P0 也都不是被这组命中的。**结论强化：这一组关键词对"全双工"这个主题存在系统性盲区，其空满程度不得单独用于任何方向性结论。**

### 6.4 第 14 组查询连续第二轮独家命中当轮最重要线索

`(cat:cs.HC OR cat:cs.OS) AND (abs:"speech" OR abs:"voice" OR abs:"audio" OR abs:"latency")` 返回 **4 篇**：`2608.19527`（本轮 P0）、`2608.19826`（机器人语音音高对儿童压力水平的影响）、`2608.19827`（低成本声学装置感知与解读动作）、`2608.19588`（中国心理健康研究的本地化生态瞬时评估）。**保留这组查询的决定第二次得到验证。**

### 6.5 语音类目本身近乎枯竭

`cat:cs.SD` 与 `cat:eess.AS` 的控制总数分别只增加 **9** 与 **7**（21,740→21,749、22,202→22,209）。**这是本项目记录以来单批次增量最小的一次之一**，与 08-20 的 50 篇窗口相比，语音类目的实际产出更低。

### 6.6 TTS 组 3 篇中 2 篇是跨领域假阳性

`abs:"text-to-speech" OR abs:"speech synthesis" OR abs:"TTS"` totalResults = **3**：`2608.19959`（合成语音如何欺骗人类的趋势追踪，cs.CR）、`2608.19789`（条件 GAN 中的量子启发张量网络去噪，cs.AI）、`2608.19651`（顶夸克 philic 第三类双希格斯二重态模型中的电弱重子生成，**hep-ph**）。**后两篇与语音合成毫无关系**，粒子物理那一篇是本项目记录到的跨度最大的一次关键词假阳性。

### 6.7 新发现的查询写法缺陷：`abs:"endpoint"` 过于宽泛

`abs:"voice activity detection" OR abs:"endpoint" OR abs:"endpointing"` 一组返回 **22 篇，22 篇全部与语音无关**，覆盖泛函分析（"endpoint estimates" 是该领域固定术语）、Tor 网站指纹防御、地球观测数据架构、心脏形状补全、全息对偶、几何相位等。**根因是裸词 `endpoint` 在数学与安全领域都是高频固定术语**，与 08-20 记录的车内组布尔优先级缺陷属同类问题（查询写法缺陷，不等于该方向无产出），但成因不同：**上一个是布尔结合律，这一个是词义歧义。**

**建议下一轮改写为 `abs:"voice activity detection" OR abs:"endpointing" OR abs:"end-of-turn" OR abs:"turn-end detection"`，即删去裸词 `endpoint`，并记录改写前后的 totalResults 对照。** 与上一轮同理：**必须记录对照，以免把口径变化误读成产出变化。**

**上一轮的车内组缺陷本轮已修复并做了对照。** 旧写法 `abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver" AND abs:"speech"` 在本窗口 totalResults = **15**；新写法 `(abs:"in-vehicle" OR abs:"cockpit" OR abs:"driver") AND (abs:"speech" OR abs:"voice" OR abs:"dialogue" OR abs:"assistant")` = **3**。**修复后剩下的 3 篇仍然全部与座舱语音无关**（`2608.20278` UAV IRS-ISAC、`2608.19752` IoT 车载应用自动化测试流水线、`2608.19661` 近海风场 AUV/ASV 导航规划）。**因此结论是双重确认的：车内语音方向本轮确实为零，而不是查询写法造成的假象。**

### 6.8 逐版复核零新增（本项目首次）

47 个追踪 ID 一次批量查询全部返回，3 个非 v1 全部是此前已完成差分的条目。**没有任何追踪项在本窗口产生新修订。** 这是逐版通道第一次完全为空，**必须与"没查"区分开来记录**。

## 7. 委托细读的六篇：全部否决，但带回若干可用原语

本节六篇全部完成全文核实与制品一手核查，**六篇没有一篇进入候选**。但其中三条对项目最高优先级事项有直接影响，另有两条修正了此前的判据，因此不是纯粹的否决记录。

### 7.1 FlashPrefill V2（2608.19758）—— REJECT，但它的基准硬件恰好就是项目的 H20

**先说为什么值得单独写：这是追踪表上第一篇基准硬件明确写成 H20 的论文。** 2608.19758v1，中科院自动化所 + 国科大 + 微信/腾讯，2026-08-20T08:02:55Z 投稿，20 页，cs.CL。原文："All methods are evaluated under identical hyperparameter settings on NVIDIA H20 GPUs"；算子级为**单张 H20**，端到端为 **TP=4、四张 H20**。

**论文结果。** 128K 上下文下相对 FlashAttention-2 为 27.19×（BF16）/ 47.26×（FP8）；相对 FA3/4 对齐的稠密算子为 17.5× / 30.5×（**后者才是诚实的基线**）。RULER 平均 87.79 对满注意力 88.82（Llama-3.1-8B）。密度随长度下降是全部收益的来源：4K→128K 为 76.0% → 4.6%。**端到端 TTFT，Qwen3-30B-A3B（MoE）、批大小 1：4K 为 0.10 s（FA3/4）对 0.09 s（本文 BF16，1.02×）；128K 为 13.78 s 对 4.17 s（3.30×）。**

**否决理由，按决定性排序，且必须写准以免日后按错误理由重新评估：**

1. **它按构造只管 prefill。** 论文原话："The two-stage pipeline runs in the extend (prefill) phase, while **decode falls back to dense attention, since a single query token per step leaves no room for block-level sparsity**."
2. **项目的上下文整个落在它评测区间之下。** 项目 KV 约 2,500 token，而论文最短的一列是 4K；在 4K 处 BF16 只有 1.01–1.09×，密度仍有 70–76%。
3. **其官方仓库的出厂默认值直接把项目排除在外。** `flashprefill_min_sparse_q_len` 默认 **4096**，文档写明低于该值整批走 FA3。**这是一手核实的仓库默认值，不是论文陈述。**
4. **FP8 路径在 RTX 5090 上不存在。** README 要求 "NVIDIA Hopper GPU (H20 / H100, sm_90a)"；451 个 kernel 实例化文件中 sm90 有 310 个、sm80 有 140 个、sm100 仅 1 个（且只有 BF16）、**sm120 为 0**，全部 80 个 FP8/e4m3 实例化都是 sm90 专属。

**明确不以批大小否决。** 该论文**真的评测了批 1**（1/4/8/16），且收益在批 1 处基本不衰减（128K 上 MoE 从批 16 的 3.40× 到批 1 的 3.30×，仅缩水 3%）。**按错误理由否决会导致日后重新 litigate。** 但需记一条口径：其头条的 47.26×/27.19× 是**批 4** 的算子数字（Fig. 2 图注明写 "All results are measured with a batch size of 4"），只有端到端 TTFT 表下探到批 1。

**开放状态：第二档，已发布可核，且许可证逐字读过。** `github.com/qhfan/FlashPrefillv2`，created 2026-08-19T09:56:38Z，**3,595 个 blob 合计 52,782,993 字节**，Apache-2.0。LICENSE 文件 **11,357 字节**，与 apache.org 标准文本逐字比对**完全一致**、版权行仍是占位符。**注意 GitHub API 的 `size: 7968` 与 blob 字节和 52,782,993 是两个不同的量，本报告两个都写出来。**

**但它出现了一处新的不一致形态，且是本轮最严重的一处：论文与它自己的官方仓库在头条方法学声明上互相矛盾。** 论文 §4.1 写："every model shares a single configuration **without per-model tuning** ... α = 0.1"。而仓库 `flashprefill_ops/flashprefill/prefill.py` 的注释写："**Paper-calibrated values** (block size 128): Llama-3.1-8B 0.18, Qwen2.5-7B 0.08, Qwen3-30B 0.12."，`server_args.py` 的帮助字符串重复了同样说法。**这是逐模型调校的三个值、被仓库明确标为"论文校准"，而其中 Qwen2.5-7B 这个模型在论文里根本没出现过。** 本报告不替作者选择，两边原样并存。**同时仓库还暴露了一个论文全文从未提及的开关** `--flashprefill-full-attention-layers`（README 示例传 4，代码默认 0），因此论文 Table 4/5/6 用的是哪个取值**无法从论文得知**。

**这也第三次印证了缓存口径这条筛查项。** 全文 20 页对 `radix`、`prefix cach`、`cache hit`、`warm`、`cold` 的命中数**全部为 0**，而它的头条指标是在 SGLang（默认开启 radix/prefix 缓存）中对同一批提示重复 30 次测得的 TTFT。**测量条件缺失本身就是发现，必须记。**

**值得带走的五条原语，全部与项目的 CUDA Graph 与无同步 MoE dispatch 直接相关：**

1. **靠"各自重算"消除分歧，而不是靠"互相通信"。** 原文："Producer and consumer evaluate the same sequence {n(t)} independently, so their control flows are **identical by construction** and the pipeline advances in lockstep **without any synchronization** for block skipping."**这正是项目在 MoE dispatch 上想要的形状。**
2. **用掩码跳过，不用分支跳过。** `todo = valid & ¬selected`，两侧 identically 跳过，**固定形状、无主机分支，可被图捕获**。
3. **把不规则工作折叠成一次固定形状的额外迭代**，"reusing the same MMA pipeline and online-softmax rescaling **without any additional kernel launch**"。
4. **项目自身根因的第二条外部佐证，且这次来自一套跑得起来的代码。** 原文："the index stage reuses a **per-stream workspace cache whose capacity-matched reuse avoids any host synchronization or allocator round trip in steady state**." 第一条是 08-20 记录的 FreeToken。
5. **按工作量而不是按位置切分负载**："the CSR list is partitioned by selected-block count rather than by position range"。

**一条负面结果值得单独留：分块 prefill 会侵蚀稀疏收益。** 实测 Qwen3-30B-A3B 上 P50 TTFT 加速从不分块的 2.2× 掉到 8K 分块的 **1.2×**，原因是"the index selection is re-run on every chunk and the mandatory tail blocks raise the effective density of short chunks"。**推广：任何按块重建索引的机制都有一个由其强制块决定的收益下限。**

### 7.2 CacheRoute（2608.19677，Meta）—— REJECT，但它的失败模型比它的成功结果更有用

2608.19677v1，Meta，**单作者**，2026-08-20T06:12:47Z 投稿，10 页，cs.DC。它是一个**请求路由器**，靠让同一前缀回到同一台机器来复用 KV 缓存。

**三条既定规则同时触发，逐条点名：多机前提**（头条需要 **60 张 H100 / 30 个目标**）；**最小可评估批大小**——**批大小根本没有被评测过**，全文 `batch` 只出现两次且都是行文用词，自变量是 QPS，最低一档 30 QPS；**以及缓存命中口径**——它的头条改进**就是** KV 命中率 64.1% → 93.2%，而座舱的第一句话永远是冷的，可回收量恰好为零。论文自己也提供了反证：在 32B 聚合负载上亲和性只把命中率从 1.1% 抬到 11.8%，而容量**掉到**朴素均衡器的 0.50–0.67×。

**开放状态为第六档"沉默式未发布"，且是最纯粹的一例：全文 PDF 的 `https?://` 命中数为 0，连自链接都没有。** 数据按构造不可发布（Meta 生产遥测导出的半合成负载）。**因此其主结果对 Meta 之外的任何人都不可复现。**

**值得带走的三条，其中第一条对项目的 KV 账本直接有用：**

1. **它自己的解析模型失败了，而这个失败是全文最有价值的结果。** 在通过了七项隔离检验、且精确测得每个目标 40,071 个 KV 块的前提下，单特征时间的占用模型仍然"missed served hit rate by **14.3 percentage points at the median and 44.7 points at p90**"。**推广：不要用容量与速率去预测缓存驻留，去测。** 这条直接适用于项目为自己约 2,500 token 的 KV 建任何预算模型。
2. **影子回放作为上线门，一条可以逐字采纳的规则。** 原文："A cache-hit increase is not enough; the plan is enabled only when **p99 or capacity improves**."**即中间指标的改善永远不构成上线理由，必须由终端时延指标放行。** 这与项目"前端改动必须以自己的 CER 验收"是同一条纪律的另一种表述。
3. **只有局部性没有均衡是没有价值的。** Sticky 路由拿到第二高的命中率（87.3%）和**最差的 p99（8.5 s）**；论文原话："locality alone does not help once a queue dominates the tail."

**另附一条冷缓存的标度律，日后有人提议"多机缓存"时可直接引用：** "For a business with rate λ_b sprayed uniformly across R destinations, the mean time between visits to one destination scales as R/λ_b. Increasing the fleet can thus **make a prefix colder even though total cache capacity grows**."

### 7.3 AV 边缘调度（2608.19557）—— REJECT，但它把"模型调用不能进快路径"证成了不可能性论证

2608.19557v1，Westcliff University，单作者，2026-08-20T01:55:40Z 投稿，8 页，cs.DC，CC-BY-4.0。

**否决理由三条同时成立：仿真器结果**（原文自述 "Results are from a simulator with deterministic channels and a single primary workload size"）、**全文零硬件声明**（`gpu|cpu|nvidia|hardware` 命中数为 0，而 `GPU` 在其代码里只是一个带 `speed` 标量的类）、**多机前提**（4–6 台异构服务器）。

**但最锋利的否决理由来自它自己的消融：全部优势都归于一个 40 个待处理任务的批处理视野，而座舱在任一时刻只有一个人在说话。** 其 Table III：TC-first + EFT 在 W=200 时 0.942、W=40 时 0.896、**W=1 时 0.795**——**而 0.795 与朴素 EDF 完全相同**。论文自己写："Ordering does nothing at W=1, since a window of one task cannot be reordered." **注意这里正确的表述是"收益随视野缩小而消失"，不是"未在批 1 上评测"——它评测了。**

**值得带走的三条：**

1. **"模型调用不能放在每任务路径上"被写成了不可能性论证，措辞可以直接引用。** 原文："A model call takes seconds, whereas an admission decision must be taken in the time it takes a PU to free; **placing a language model on the per-task path is therefore not a tuning choice but an impossibility**."**这是项目 PACE 式"慢推理通道绝不可门控快音频通道"设计的一条独立外部佐证。**
2. **必须有陈旧策略回退，且理由是推导出来的而不是断言的。** 原文："Because control-plane latency is not bounded by the window duration, a deployment cannot assume a policy update will arrive before the window it was requested for. **The data plane therefore always holds a committed Θ and proceeds with it if no reply has arrived.**"**适用于项目任何异步计算的每轮策略**（路由提示、工具预选、说话人归属旁路）。
3. **一个"LLM 策略调用要花几秒"的实测锚点：均值 3.0 s、中位 2.8 s、p95 4.3 s**（小型托管模型 + 受约束输出 schema）。**日后有人提议把模型调用放到 1,000ms 首音频门附近时，这是可以直接引用的数字。**

**开放状态出现一个新组合：代码已发布但没有许可证。** `github.com/rezarz98/multi-agents-llm-edge-scheduling` 存在（46 条目、2 次提交，且 created 2026-08-16 **早于论文投稿 4 天**，又一次制品先于论文），但 API `license: null`、`/license` 返回 404、全树无任何 LICENSE 文件。**这与本轮 ReCache 是同一形态的第二例，说明"公开可读 ≠ 开源可用"不是孤例。**

**另有一条值得升格为常规检查项：它发布的代码里含有一个比论文所报更公平的基线。** 仓库有 `lib/baselines_windowed.py` 与 `run_windowed_baselines.py`，文档写明是"windowed PSO, GA, DDQN at W=40"，而**论文全文没有报告任何 W=40 的 GA/PSO/DDQN 数字**。既然论文自己的消融证明 W 是主导变量，那么最该做的对照（本文 W=40 对元启发式 W=40）**存在于代码中却缺席于论文表格**。**操作规则：把"比对仓库的基线文件与论文的基线表"列为常规核实项。**

### 7.4 机器人语音音高（2608.19826）—— REJECT，但带回一条必须写进任何语音 A/B 的前置条件

2608.19826v1，埃因霍温理工 + 乌得勒支大学，RO-MAN 2026 已接收，2026-08-20T09:27:56Z 投稿，9 页，cs.HC。

**否决理由一句话：它的因变量是听者情绪，而项目按既定规则把这一项排除在外；且它在该因变量上本身就是一个零结果。** 固定 ±35 Hz 的基频偏移把自报压力推向了**与假设相反**的方向，且不显著：**F(1, 25) = 1.70, p = .20，只解释 6.4% 的方差**。N=27 名荷兰儿童，零时延、零 CER、零中文材料。

**但它带回的这条对项目直接可用，且是本节最实用的一条：给合成语音做变调会悄悄改变响度。** 论文原文："Reexamination of the stimuli revealed an **unintended intensity variation, possibly introduced by speech manipulations**. A second survey using **intensity-normalized** stimuli (70dB) was implemented to improve reliability."**他们是在第一轮问卷已经跑完并得出零结果之后才发现的。** **操作规则：项目未来任何音色或韵律 A/B，必须先做响度归一化（他们用 70 dB），否则对比被混淆、零结果不可解释。**

**第二条：一条反对"静态韵律预设"作为产品功能的负面证据。** 固定的非自适应基频偏移没有产生可测的情绪收益，作者自己的诊断是"stable pitch manipulations ... may not reflect how pitch modulation is naturally used throughout a session... Moreover, pitch rarely operates in isolation"。**日后若有人提议做"舒缓语音模式"预设，这是反对把它做成静态偏移的引用。**

**一条必须原样保留的不一致：摘要断言了主分析并不支持的操纵检验。** 摘要写压力在游戏阶段上升"**confirming** the stress-inducing nature of the task"，而论文明确限定只用 Item 1 分析，Item 1 的检验是 **t(26) = 1.74, p = .09，论文自称 a nonsignificant trend**；p = .04 来自脚注里的 Item 2。**另有一处算术上不可能的统计量**：脚注 2 印 "R² = 0.7, F(1, 25) = 1.83"，而该自由度下 F=1.83 只对应 R² ≈ 0.068。按原样记录。

### 7.5 EEG 听觉注意解码 ASIC（2608.20198）—— REJECT，理由是 1 Hz

2608.20198v1，德累斯顿工大 + 埃尔朗根-纽伦堡大学，BioCAS 2026 已接收，2026-08-20T15:54:35Z 投稿，5 页，cs.AR。GF22FDX 22nm，已流片。

**它不是关键词假阳性——它真的是脑电驱动的目标说话人选择。** 但**否决理由是决定性且单一的：注意力判定每秒只更新一次。** 原文："only eight correlation coefficients are maintained in a circular buffer and **updated once per second** using a moving-average scheme."**一个每秒刷新一次的控制信号无法参与打断仲裁，因为打断的全部预算比一个更新间隔还小。**

**必须防一个误读：7.34 ms 不是判定时延。** 那是每次推理的算力耗时，被设计成塞进 125 Hz 脑电流的 8 ms 采样周期内。**把它读成"7 毫秒完成脑电注意力解码"是错的。** 另注该论文**从未声明自己的判定窗长度**（"decision window" 全文只出现一次，且在一条参考文献的标题里）；8 个系数的缓冲区若是 8 次连续的 1 秒更新则积分时间约 8 秒，若另有结构则更短，**论文没有给出判别方式，此处记为待核**。但最有利的读法也已经是 1 Hz，**两种读法都不通过**。

第二个独立的否决理由是传感前提：31 通道头皮电极、面向人工耳蜗使用者、且**权重需按人在 listening sessions 之间经串行 SRAM 接口重载**——即每位乘员都要戴电极并做个体标定。另注**全文没有给出任何 AAD 准确率**，所有相关表述都是定性或转引其合作论文。

**但它带回两条价值很高的佐证：**

1. **"身份/注意力判定必须是音频通路之外的控制信号"这条架构约束拿到了第二篇独立佐证，且来自完全不同的子领域。** 原文："The decoded attention information can **provide a control signal** for a hearing-assistance system to select or enhance the attended speech stream."第一篇是 08-19 的 2608.17972。**两篇互不相关的论文在不同子领域收敛到同一条约束，其价值高于任何单篇。**
2. **"空间编码不是多乘员的正确路线"拿到第三条独立佐证，而且论据类型是新的。** 原文："many of these approaches rely on **spatial cues** or predefined speaker configurations, which may **limit their robustness** in practical acoustic environments. In contrast, stimulus reconstruction-based AAD remains **independent of speaker locations**."**前两条佐证讲的是渲染成本（DiffM2A 的 2.66 s/4 s、Meta/Aalto 自述"适合离线"），这一条讲的是线索本身的鲁棒性——一个做实硅的团队出于鲁棒性主动放弃了空间线索。**

**另有一条可迁移的工程技巧：把皮尔逊相关从需要缓存全部历史的中心化形式改写为原始矩形式**，从而"enabling on-the-fly accumulation, eliminating the need to buffer complete signal histories"。**项目任何想在流上计算的在线指标**（预测与实际 token 速率的滑动相关、首音频时延的漂移监控、流式 CER 代理）**都可以用同一替换把状态从增长缓冲降为 O(1)**。

**一处必须并存的不一致：同一个术语在 5 页论文里指两个不同的数。** 摘要写 "a total silicon area of **2.09 mm²** (1264µm x 1654µm)"，正文写 "The **total silicon area is 76048µm²**"（= 0.076 mm²）。两个数各自算术自洽（1264×1654 = 2,090,656 µm²），但**约 2.01 mm² 的差额全文从未交代**。两个都记，不合并。

### 7.6 合成语音欺骗性趋势（2608.19959）—— REJECT，但它是本轮唯一一篇完全干净的论文，且它的听测协议直接可用

2608.19959v1，布尔诺理工 Security@FIT，SPSC 2026 已接收，2026-08-20T12:27:37Z 投稿，10 页，cs.CR，论文许可证 **CC-BY-NC-ND-4.0**。

**它与合成质量、时延完全无关**——没有 MOS、没有 CER/WER、没有 RTF、没有 TTFT。全文 `latency` 只出现一次，且是说明该研究**没能**测量它。**这是 TTS 关键词扫描的一个主题级假阳性**：RTVC / YourTTS / ElevenLabs 在此纯粹是攻击工具。

**并且标题本身构成一种新的不一致形态，值得单列：标题断言了正文明确拒绝主张的东西。** 标题是 "Tracking the Trend"，而正文**三处**否认可以据此得出趋势，例如："Because each release period is represented by one system ... our results **do not establish a general temporal trend in speech synthesis**." 作者还指出顺序本身就非单调。**因此这里没有任何可用的 TTS 自然度纵向数据**，任何把它当趋势线引用的做法都在引用论文拒绝主张的内容。**新形态：标题断言 vs 正文自我否认，与已记录的"摘要最高级只对聚合成立"不同。**

**它是本轮唯一一篇逐项交叉核对全部通过的论文，这一点必须明说，因为它校准了其余各篇。** 表 2 的条目数 108+108+36 = 252 = 63×4 = 9×7×4 全部自洽；判定数 2569+2514+2309 = 7,392 与 2519+2491+2419 = 7,429 精确相加；摘要的 77% = 100 − 22.65 与表 3 一致；人口统计两次独立加总均为 82。

**它最大的价值是那套听测协议，而且它恰好修好了 08-20 记录的 X2Streaming-TTS 那 120 名听众评测的每一处缺陷。项目若要做任何主观评测，应当逐条照搬：**

1. **报告评分者间一致性，并且在系数很低时去解释它而不是藏起来。** 他们用 Krippendorff's α（理由是"it accommodates nominal categories and incomplete response matrices"），然后把准确率约 90% 下 α 近零解释为**基率悖论**而非分歧："when the marginals are highly skewed ... the expected (chance) agreement approaches the observed agreement, so α collapses even though raw percent agreement exceeds 90%. **Krippendorff's α therefore measures consistency, not correctness, and must be read jointly with the accuracy metrics.**" 并把它与边缘分布均衡时的真歧义情形区分开（ElevenLabs α = 0.155："here low agreement **does** reflect genuine perceptual ambiguity"）。**这条同时能防止项目在自己偏斜的评测集上把低 α 误读成评分者不可靠。**
2. **把准确率拆成敏感度与偏倚，因为聚合准确率把两者混在一起。** 报 **d′** 与判据 c：本文 d′ 为 2.75 / 2.88 / **0.73**，而**在真实音频上的虚警率是 17.5%**——**这正是原始准确率会掩盖掉的量。**
3. **用配对的、逐被试的非参数检验，并用按被试重采样的 cluster bootstrap 置信区间**，因为每位听者贡献了许多相关的判断。**项目 452 条评测集正好是这种聚簇结构。**
4. **在解释任何指标之前先算出它的随机水平。** 他们算出 All-OK 的随机水平是 **6.25%**、Over-50-Percent 约 31%、逐句 50%，然后发现 33% 的得分"sits essentially at its chance level"——**朴素读法会把它当成正面结果。** **项目报告的每个指标旁边都应当写上它的随机水平。**
5. **用一族严格度递进的指标，并说明何时某一个失效。** 其中 **All-OK（全部片段都对）是最值得抄的一个**：正是它暴露了整段 F1 掩盖掉的定位失败，包括检测器的 0.00。
6. **在结果之前先声明自己的设计引入了哪些人为效应**（本文声明了三条：合成条目的高先验比例、剔除"不确定"回答、以及可能的非随机流失）。

**另有一条负面结果值得带走，而且它与项目最高优先级事项同源：六个源基准 EER 只有 0.57–5.34% 的成熟检测器，在没见过的现代商用合成上崩到 F1 0.09——比未受训练的人类（0.48）还差**，且在部分伪造上的严格定位率是 **0.00**。**推广的教训不是那些数字，而是：一个检测器在自家基准上的 EER 几乎不能预测它在未见生成器上的行为。** **这与 2608.12880 的构造效度失效是同一个模式，却来自完全不同的子领域**，并且适用于项目任何打算凭已发表基准分数就直接接入链路的预训练分类器——VAD、打断检测、说话人归属都在内。

## 8. 判定器专题：三篇同时命中项目最高优先级事项，脚手架敏感性从三条佐证变成六条

**这一节是本轮除 P0 之外最重要的内容，且它直指项目排序第一的未决事项（判定器处理不变性）。** 三篇论文按技术候选一律否决——经关键词逐项核对，三篇对 `speech`、`audio`、`voice`、`TTS`、`duplex`、`Mandarin`、`latency`、`real-time`、`barge-in`、`cockpit`、`automotive`、`vehicle` 的命中数**全部为零**（唯一的 "Chinese" 是香港中文大学这个机构名）。**但作为评测协议来源，它们分别是 P0 / P1 / P1。**

### 8.0 先说本节最重要的一条：脚手架敏感性已经从假说变成既成事实

**此前三条独立佐证是 DuplexWorld（2608.10716）、SemaPLC（2608.18565）、ComponentBench（2608.18307）。本轮一次性加三条，且三条的操纵变量各不相同：**

4. **EnvHarness Table 12——六条里最强的一条。** 同一策略、同一批 100 个 ALFWorld 任务、**同一个验证器**，只换掉包在外面的动作/转移/观测封装层：逐任务成功率落在 [0.4, 0.6] 区间内的比例从 **6.0% 涨到 80.0%**，而**平均成功率从 0.74 移到 0.48**。论文原话："A single interface therefore suffices to calibrate an environment against an explicit, measurable objective, **without any access to its internals**."**即一层只碰接口的封装可以把实测通过率放到它想要的几乎任何位置。**
5. **MaliciousSkillBench Table 42——同一来源、同一检测器，只改划分协议。** SRC011 的良性误报率从 **0.113 涨到 0.644**，SRC012 从 **0.000 涨到 0.522**；总体 Macro-F1 从 0.932 掉到 0.665。**作者自己写明了这不是配对因果估计**（"The evaluated units differ and Random exposes each source during training"），按原样记录。
6. **StateMemBench Table 8——只拨一个与行为无关的开关，判定器保持不变，20% 的条目翻转而聚合只动了 8 个百分点。** 详见 8.2。

**编辑判断：从本轮起，"脚手架可以把指标放到几乎任何位置"应当按既成事实处理，而不再按待验证假说处理；并且"聚合稳定而逐条大幅翻转"这一现象已经被两个互不相关的团队在两个完全不同的领域各自复现过一次（ComponentBench 17.3%、StateMemBench 20%），同样应当按既成现象处理。**

### 8.1 项目应当第一个动手做的东西，本轮第一次有了完整可抄的样板

**StateMemBench 的 Table 8 就是项目一直想做却没有做的那个实验，而且它的呈现方式可以逐字照搬。** 设计是：取 LongMemEval 多会话 oracle 的 50 条配对条目，**提示完全相同、全部历史都在上下文里因此没有检索混淆**，唯一的操纵是把 DeepSeek-V4-Flash 的原生推理轨迹开关打开或关掉，**判定器在两臂之间保持不变**。

| 臂 | 正确率 |
|---|---|
| thinking off | 42/50 = **84.0%** |
| thinking on | 38/50 = **76.0%** |
| 差 | **−8.0 个百分点** |

| 转移 | 条数 |
|---|---|
| 关→开 由错变对 | **3** |
| 开→关 由对变错 | **7** |
| 两臂都对 | 35 |
| 两臂都错 | 5 |

**10/50 即 20% 的条目发生了翻转，而聚合只移动了 8 个百分点。** 论文自己的读法很克制："the −8.0pp point estimate rests on **3 rescues against 7 new failures**"。

**编辑判断：项目应当把这张表的格式原样搬过来作为自己的重复运行翻转率量具**——选一个与行为无关的拨杆（该论文选的是推理轨迹开关，与 2608.12880 的元数据标志位同构）、判定器固定不变、**报告四个转移计数而不是那个差值**。**这是本轮全部内容里工作量最小、收益最大的一项，且零 GPU。**

### 8.2 StateMemBench（2608.19652）——协议 P0，但开放状态是四栏全部第三档

- 论文：`https://arxiv.org/abs/2608.19652`（v1，2026-08-20T05:41:23Z 投稿，23 页，cs.AI）
- 作者：Xinyi Fan\*、Miri Liu\*（共同一作）、Ruozhen Yang、Siru Ouyang、Jiawei Han，**全部为伊利诺伊大学厄巴纳-香槟分校**
- 论文许可证（arXiv 元数据 `/License`）：`arxiv.org/licenses/nonexclusive-distrib/1.0/`

**它的构造方式是本节最值得抄的部分。** 每个场景是一段**符号化事件程序**（规则声明、取值更新、带作用域的例外、承诺、撤回），正确答案由**确定性求解器重放该程序算出**，而不是人工标注。然后是关键一步——**可执行的"坏读者"基线**：论文实现了一族 lazy reader 策略，每一个都对应记忆系统的一种可能失效（只信最近一次提及、只信出现最频繁的取值、直接返回已陈述值而不重算、过早丢弃已锚定的决定），**而陷阱场景恰好取自"至少一个策略与完整重放不一致"的那些**，那些不一致的策略随即构成该场景的失效模式签名。**这等于让每一条评测样本自带一个已知的错误答案，即注入故障召回率按构造成立。**

**封闭选项池评分：** 在生成时就构造好一组可信答案（含金标准、被针对策略算出的**漂移答案**、以及 3–4 个中性干扰项），**但在提问时不展示**。因此判定器唯一的工作是把自由文本映射到池上——一个严格更简单且可审计的任务，并且能把"跟踪错了"与"完全答错"分开。论文还写明"Gold and drift are computed **before rendering**, so the renderer cannot alter trap semantics."

**它还有一个项目目前完全没有的东西——泄漏扫描器：** "we programmatically verify that the load-bearing facts appear, and appear in the correct assigned session, as well as that **no banned phrases appear** in the final scenario. Any scenarios that fail are **re-rendered until they pass**."

**它是本项目筛过的第一篇把 LLM 判定器的验证工作真正做完的论文，而它给出的结论对项目至关重要。** 论文把确定性份额直接量化了："a deterministic word-boundary matcher decides the **28%** of answers that name exactly one pool option verbatim and **agrees with the judge on 94.1%** of those; the judge maps the rest"。判定器间一致性（Table 7）则给出一条必须写死的规矩：

> "Mode κ (which drift sub-mechanism) is **near zero on every benchmark (0.00–0.09)** while binary κ is fair-to-substantial: **judges agree on *whether* a failure is drift but not on *why* the wrong fact won.** This is the empirical basis for … **mechanism must be fixed by construction rather than inferred post hoc.**"

**编辑判断：这条应当直接成为项目的硬规矩——判定器最多只能向 LLM 索取一个二元判定；失效类别必须由构造固定，LLM 事后归因的机制标签在统计上等于噪声（κ = 0.00–0.09）。** 这同时给 08-20 记录的三篇（SemaPLC / ComponentBench / 2608.18351 的 LLM 归因）提供了一个量化的否定依据：**它们的归因标签之所以不可信，不是因为没做人工核对，而是因为这类标签本身就没有可复现的信号。**

**它的人工锚定同样值得抄，因为设计上防住了范畴被表格强加。** Track A（结构化，n=12）让标注者回答固定子问题、**由回答推导出类别**，"so the taxonomy cannot be imposed by the form"；Track B（盲测，n=6）让标注者在看到分类体系**之前**先用自由文本描述失效。结果：二元漂移判定 83.3% 原始一致、κ=0.43；**完整类别标签只有 50.0% 原始一致**；盲测的表层观察标签 κ=0.80，且 12 条盲测描述全部落进已有类别、无需新增。**而最该记住的一个数字是人机对比：两名标注者与判定器的二元一致率只有 58.3% 与 41.7%，且两人给出的漂移都比判定器少（8.3% 与 25.0% 对 50.0%）——因此论文自称其漂移率是"judge-labeled upper bounds"。**

**开放状态是本轮最遗憾的一条：论文写"We release StateMemBench"、并写"The annotation kit (forms, keys, and the analyzer) is released with the benchmark"，而 23 页全文没有给出任何 URL。** 超链接注解只有一条 arXiv 自链接；全部五条裸 URL 均为第三方引用（`microsoft/STATE-Bench` 是被引用的他人基准、OpenAI 与微软的博客、Qwen 博客、Instacart 数据源）。**代码、数据均为第三档 unverifiable，许可证未知。** **编辑判断：那套标注工具包（表单、答案键、分析器）是本批次里对项目最直接可用的制品，而它现在拿不到——列入追踪，是追踪表上价值最高的第三档条目。**

**两处摘要级不一致，按原样记录。** 其一，摘要的"1.6×"只有在把 GraphRAG（0.224）归类为"graph-RAG 系统"而非"记忆系统"时才成立，**而正文自己的检验说两者持平**："McNemar over the 322 shared probes: 38 vs 35 discordant, **p=0.82**"。其二，**摘要没有提一个消融比头条方法更好**：DeepSeek 上"w/o dep.-propagation"为 **0.373**，高于完整 StateMem 的 **0.363**；正文如实披露了，但表注把消融排除在加粗竞争之外。**另注全文未声明任何硬件、无墙钟、无 token 计数，且主表每个配置只跑一次，因此 Table 8 是全文唯一的翻转率测量。**

### 8.3 EnvHarness（2608.19880，Google Cloud AI Research）——协议 P1，且带回项目一直缺的那个动作门形状

- 论文：`https://arxiv.org/abs/2608.19880`（v1，2026-08-20T10:42:06Z 投稿，41 页，cs.AI，**CC-BY-4.0**）
- 代码：`https://github.com/google-research/envharness`（**Apache-2.0**，LICENSE 11,357 字节，逐字为未修改 Apache 文本；242 条目，9 stars）
- 项目页：`https://www.envharness.com`

**它对项目的价值集中在一个源码文件上，而不是论文。** `envharness/harnesses/rules.py`（8,274 字节，Apache-2.0，已一手读过）提供三个纯函数式的逐步钩子：`filter_action(action, env_state)`、`modify_transition(action, response, env_state)`、`filter_observation(obs, env_state)`。**这是本项目找到的第一个"播报前确定性动作门"的真实形状，而且它是一个运行时而不是一个 LLM 审阅者——正是 08-20 从 SemaPLC 得出的"唯一可能塞进时延预算的形状"。** 四条可直接搬：

1. **拒绝必须返回实时状态加理由，绝不能是一个光秃秃的拒绝。** 拦截返回一个带类型的 `Blocked(reason=...)`，且拦截路径保持环境不变、重新观测、把当前状态连同理由一起交回。源码注释解释了为什么："A bare `[blocked]` Observation would strip env-critical `obs.data` … and **strand or crash format-specific policies on their next `act()`**." **座舱对应物：动作门拒绝一次工具调用时，必须把当前车态与拒绝理由一起返回，否则模型的下一轮无法恢复。**
2. **层次信任分离由结构强制，而不是靠约定。** `Rules` 在结构上**碰不到**初始状态（那是独立的 `Setup` 层）也碰不到奖励/判定（那是验证器）。论文侧的表述是："Because all interventions remain external, **the ground-truth evaluation logic is preserved**, ensuring the original verifier still can score the episode." **编辑判断：把这条作为项目 452 条判定的硬不变量——动作门可以拒绝或改写一个动作，但永远不得触碰评分器。**
3. **生成出来的门代码在隔离子进程里运行**，"so a bad mutation becomes a **recorded trace** instead of a dead run"。
4. **跨进程边界传递的必须是数据，不能是句柄**（`get_env_state()`）。**若项目为保住时延预算而把动作门放到进程外，这条直接适用。**

**论文本体的结果与项目无关（ALFWorld / WebArena / SWE-bench / SpreadsheetBench），但它的 Table 12 是 8.0 节列的第四条佐证。** 另需记一条方法学正面记录：**它的 LLM 只做提议者、不做评分者**——设计智能体诊断弱点并写出真正的 Python 组件，而原基准的验证器原封不动地裁决。**这与 Thinkingbox 的"判定确定性、归因才允许用模型"是同一条纪律的第二个正面先例。**

**开放状态：第二档，但有一处经文件级核实的缺口。** 论文 Table 4 报告了 **ALFWorld 与 WebShop** 两个 RL 结果，而 242 条路径中 **`webshop` 出现 0 次**，README 也从未提及 WebShop。**即 Table 4 的整个 WebShop 列（Score 79.2 对 75.6、SR 67.4 对 66.0）没有任何已发布的桥接或实验。** 这是"仓库存在 ≠ 论文声称的制品存在"这条规则的又一次生效。**另有两处不一致按原样记录：** OfficeQA 的 F1 增量论文写 **+1.96** 而 README 写 **+1.97**（57.73 − 55.77 = 1.96，README 是错的那个）；摘要说"为强化学习提供了更优的优化信号"，而 Table 4 四项里**有一项退化**（ALFWorld OOD 88.8 对 89.6）**且该表全表没有任何标准差**——正文诚实地写了这条退化，属摘要层面的省略。

**另记一条对项目开放问题的否定回答：它没有回答"缩小可达工具集能否提升准确率"。** EnvHarness 是在**训练期**限制动作集，然后在**标准的、不受限的**实例上评测；**因此它证明的是"训练期限制能改善不受限的评测"，而不是"推理期限制"。** 08-19 从 2608.14036 推出的那条假设**在第三次尝试后仍然开放**。

### 8.4 MaliciousSkillBench（2608.19901）——协议 P1，且是本轮第二篇完全干净的论文

- 论文：`https://arxiv.org/abs/2608.19901`（v1，2026-08-20T11:13:00Z 投稿，**49 页**，cs.CR）
- 代码：`https://github.com/protectskills/MaliciousSkillBench`（**Apache-2.0**，LICENSE 11,358 字节）
- 数据：`https://huggingface.co/datasets/ProtectSkills/MaliciousSkillBench`（**CC-BY-4.0，非门控非私有**，`primary.parquet` 28,600,360 字节；模型卡 `arxiv:` 标签为 `arxiv:2608.19901`，**指向正确**）

**它的 `skill` 用的正是 08-19 记录 2608.14036 时的那个义项——可安装的指令包。** 规模：13 个公开来源、8,414 条原始恶意记录 → 7,562 条精确去重 → 7,539 条归一化去重 → 4,588 个结构族；最终 **9,740 条 = 7,505 恶意 + 2,235 良性**，四套评测协议在同一份冻结条目集上跑。

**四条可直接搬的纪律，其中两条直接作用于动作门：**

1. **证据分档门控——目前可见的最便宜的判定器升级。** 该基准把"标注强度"与"证据类型"做成两条**正交**的轴，并明确"evidence type must **not** be interpreted as an ordinal confidence score"；然后设一条硬门：**仅有扫描器证据的记录永不进入主要真值**，据此把 182,699 条注册表记录中的 **169,676 条**仅凭证据类型就排除在外。它甚至预先声明了自己没有用到的档位（`human`、`llm`、`unknown`）。**编辑判断：项目应当给 452 条判定各打上这两条轴的标签，并写死"弱档证据永不用于设定阈值"；同时预先声明一个 `llm` 档，使得凡是 LLM 标注的东西在表里是可见的，而不是被悄悄提升。**
2. **`ABSTAIN_ERROR` 作为第三种状态——这一条应当立刻写进播报前动作门。** 原文："Technical failures remain a third state, ABSTAIN ERROR, and are **never silently converted to benign predictions**. Primary binary metrics therefore use successful scans while **reporting coverage separately**."**座舱对应物：动作门自身出错时绝不可退化成放行；且门的覆盖率必须与门的准确率分开报告。** 这是一行不变量，但安全后果是实质性的。
3. **一个作用在模型特征侧的泄漏扫描器**，与 StateMemBench 那个作用在条目侧的互补：审计导出的高权重线性特征里是否出现来源 ID、基准 ID 或数据集名标记，**记录到零命中**，且来源标识按构造被排除在检测器输入之外。**同时它对自己的结论作了正确的限定**："This reduces the likelihood of a trivial metadata leak but **does not establish source invariance**."
4. **它明确拒绝给单条误报/漏报打语义"错误成因"标签**，原文："Automated keyword summaries are used only as inspection aids and have **no human-verified causal-annotation status** … We therefore report frozen counts and source concentration without … promoting those keywords to ground truth."**这与 StateMemBench 的 mode-κ 结论指向同一处，且是一条可以在日后引用的先例——作者对自己的工作守住了这条线。**

**它是本项目筛过的论文里少有的把"少数类指标必须给召回"当成论文主张本身的一篇**：每一个工作点都同时给出召回与良性误报率——95.6% / 62.4%、2.5% / 1.1%、0.0% / 0.55%、25.3% / 49.9%。**并且它给出了一条对项目动作门选型直接有用的结论：在跨来源的分布漂移下，一个学出来的文本门只能用 62.4% 的良性误报率去买 95.6% 的召回**，作者的总结是"**no detector occupies the desired high-recall, low-FPR regime on held-out sources**"。**编辑判断：这是"播报前动作门必须是确定性的 schema/车态校验，而不是学出来的分类器"这一选择的最强外部论据。**

**另有一条存在性证明值得记：确定性方法的重复运行翻转率可以真正等于零。** 种子 42/43/44、划分冻结，"**standard deviation 0 in the frozen result table**"——因为 TF-IDF 加线性模型本身就是确定性的。**即"重复运行不稳定"不是评测的固有属性，而是方法选择的后果。**

**它是本轮第二篇完全干净的论文，且它主动交代了七处上游不一致而不是抹平。** 摘要的 0.932/0.916/0.665 与 Table 2、README 三处逐位一致；95.6%/62.4% 与 340/545 = 62.39%、802/839 = 95.59% 精确对得上；9,740 = 7,505 + 2,235 成立。附录 A.4 逐条列出上游来源的自相矛盾（例如 SRC005 的 README 报 350 行而固定修订版只有 347 行，"The three missing malicious records are **not reconstructed**"），并给出一条规矩：**"'Paper-reported size' 与 'artifact-level samples available for a frozen evaluation snapshot' 是两个不同的量"**。**它对硬件的处理同样是本项目见过最好的**："The frozen run package does not record an exact host Python version, operating-system build, CPU model, RAM size, or GPU model, **so we do not invent those fields here**."

**但许可证有一处被聚合标签掩盖的实质风险，必须点名。** 仓库层面是 Apache-2.0（代码）+ CC-BY-4.0（数据），**但 `THIRD_PARTY_NOTICE.md` 写明："Third-party Skill artifacts included or referenced by this project are **not** relicensed under Apache-2.0 or CC BY 4.0. Those artifacts **retain their respective upstream terms**."** 而其 Table 6 列出的上游许可证里有三个对商业车机是问题：**SRC001 为 academic-research-only、SRC006 为 terms not provided、SRC010 为 CC-BY-NC-SA-4.0**。**编辑判断：聚合的 CC-BY-4.0 只清理了基准自身的元数据层，对这三个来源什么都没清理。** 另有一处经文件级核实的缺口：附录 G.6 声称冻结包保留五类制品，而 GitHub 56 条目与 HF 18 文件里对 `joblib|seed|estimator|minilm|embedding` 的命中数**全部为零**——**五类里有四类（逐种子指标表、seed-42 序列化估计器、MiniLM 嵌入矩阵、嵌入方法学清单）两处都不存在。**

**一处该论文没有说明的来源独立性问题，值得单独记：** 其局限一节写 SRC002/SRC004 使用"author-provided historical artifact snapshots"，而 **SRC002 就是 `ProtectSkills` 这个组织自己的 HF 数据集，与本论文的发布方是同一个组织**。**即 8,414 条原始恶意记录里有 236 条依赖于非公开快照，且其中一部分的上游就是发布方自己，而"author-provided"这个措辞没有披露这层关系。**

### 8.5 术语：`harness` 现在有五个义项，且有一篇论文同时使用其中两个

在既有三义（训练脚手架 2608.03571 / 执行脚手架 Evo-Bench 2608.09096 / 普通动词 2608.19013）之外，本轮新增两个：**（四）环境脚手架**——EnvHarness 自定义为 "a programmable layer that wraps an existing, static environment"，并与执行脚手架并列成 "Agent = Model + Harness" 对 "Customized Env = Static Env + EnvHarness"；**（五）可执行测试运行器制品**——MaliciousSkillBench 只在这个窄义上使用。**EnvHarness 一篇之内同时使用义项（二）与（四），且专设一张对照表说明，属有意为之而非混用。** StateMemBench 全文从不使用这个词。

## 风险提醒

1. **不要说"MaAI 是 MIT 的"。** 代码是 MIT，权重是四个许可证家族的拼盘，其中**中文判停主力 `vap_ch`、中文 VAD `vad_ch`、中文应答检测 `bc_det_ch` 全部是 CC-BY-NC-ND-4.0，既非商业又禁止衍生**；两个模型（`vap_bc_2type_jp`、`bc_det_en`）**完全没有许可证标签**，按未知处理。可商用的中文权重目前只有 `vap_bc_ch`（应答预测）与能力更少的 `vap_ch_kyoto`（判停）。
2. **不要把 MaAI 的双通道 VAD 记成 AEC 的进展。** 它不消除自身播放的回声，它在串音已存在时判断谁在说话。**AEC 的计数是连续第九轮为零。**
3. **本轮没有从一手来源核实到 MaAI 的任何精度数字。** 论文 2608.19527 只有主观量表，仓库 README 把精度全部指向四篇未核实的外部论文。**在自测之前，项目文档里不得出现 MaAI 的任何准确率。**
4. **2608.19527 的基础管线是按键说话，不是全双工。** 它证明的是"半双工主通道 + 并联的连续倾听旁路"有感知收益，**不提供任何关于打断、抢话或重叠语音的证据**。
5. **不要用 2608.19527 论证应答能提升效率。** 它自己的行为数据（话语条数、话语长度、对话时长）全部无显著差异，p 值分别为 .516 / .195 / .300。
6. **应答不是越多越好，这已经是显著性检验结果而不是直觉。** 注意感对应答次数的二次项 p=.009 且呈倒 U 型。但**该分析是探索性的、N=35、单一语言场景**，可信的是方向不是数值。
7. **不要把 ReCache 的 7.200ms 当成项目可获得的 prefill。** 它明确定义在**缓存命中**下且**排除离线缓存构建成本**，而车机上电后的第一句话必然是冷的。**这是 08-20 记录的 prefix-cache 陷阱的第二个独立实例。**
8. **ReCache 的仓库存在但没有任何许可证文件**，按 GitHub 默认条款保留全部权利。**"公开可读"不等于"开源可用"。**
9. **Thinkingbox 承诺发布两件制品，只交付了一件。** 507 个任务与黄金终态不在仓库里，仓库里只有沙箱框架与一个 `cloud_drive` 演示场景。**且其 README 中 `arxiv` / `507` / `pass@` / 五个业务域名称的出现次数全部为 0**——这是 08-20 记录的"发布产品不发布实验"形态的第二个独立实例。
10. **对座舱而言 pass^k 才是对齐业务语义的指标。** 用户说"打开空调"没有第 20 次机会。照抄论文以 pass@k 为主的呈现顺序会误导。
11. **在验证试次隔离之前不要报 pass^k。** 两次运行若共享任何状态，pass^k 会被系统性高估。
12. **不要按错误理由否决 FlashPrefill V2。** 它**确实评测了批 1** 且收益在批 1 处几乎不衰减，因此"最小可评估批大小"这条规则在它身上**不触发**。正确的否决理由是：按构造只管 prefill（解码回退到稠密注意力）、项目上下文低于其评测区间下限、其仓库默认 `flashprefill_min_sparse_q_len = 4096` 让它对项目直接失效、以及其 FP8 kernel 只有 sm90 实例化、**RTX 5090（sm120）为 0**。
13. **不要引用 FlashPrefill V2 的 47.26×/27.19× 作为批 1 数字。** 那是**批 4** 的算子数字，只有端到端 TTFT 表下探到批 1；而在项目所处的 4K 以下区间，它自己的表给出的是 1.01–1.09×。
14. **缓存命中口径本轮出现第三例，且第三例是"完全没提"。** ReCache 写在指标定义里、`Qwen3TTS-Streaming` 写在 README 条件里、**FlashPrefill V2 则是 20 页全文对 `radix`/`prefix cach`/`warm`/`cold` 零命中**，而其头条 TTFT 是在默认开启前缀缓存的 SGLang 中对同一批提示重复 30 次测得的。**测量条件的缺失本身就是发现。**
15. **任何音色或韵律 A/B 必须先做响度归一化。** 2608.19826 的作者是在第一轮问卷跑完并得到零结果之后，才发现变调操作意外改变了响度。**不归一化的对比是被混淆的，其零结果不可解释。**
16. **不要把 EEG 听觉注意解码的 7.34 ms 当成判定时延。** 那是每次推理的算力耗时；**该系统的注意力判定每秒只更新一次**，最不利读法下积分时间约 8 秒。
17. **不要把 2608.19959 当成 TTS 自然度的纵向趋势数据。** 它的标题写"Tracking the Trend"，而正文**三处**明确否认可以据此得出趋势（每个发布期只有一个系统，且唯一的商用系统与"最新"完全混淆）。**这是"标题断言正文自我否认"这一新形态的第一例。**
18. **不要凭已发表基准分数直接接入任何预训练分类器。** 六个源基准 EER 仅 0.57–5.34% 的语音深伪检测器，在未见过的现代商用合成上崩到 F1 **0.09**，比未受训练的人类（0.48）还差，部分伪造的严格定位率为 **0.00**。**这条同样适用于 VAD、打断检测与说话人归属。**
19. **判定器最多只能向 LLM 索取一个二元判定。** StateMemBench 的判定器间一致性显示：二元漂移判定的 κ 可以达到 fair-to-substantial，而**"是哪一种失效机制"的 mode-κ 在每个基准上都是 0.00–0.09**。**因此 LLM 事后归因的机制标签在统计上等于噪声，失效类别必须由构造固定。** 这条同时为 08-20 记录的三篇（SemaPLC / ComponentBench / 2608.18351）的 LLM 归因提供了量化的否定依据。
20. **不要把判定器的漂移率当成真实值，它是上界。** 该论文的人工锚定显示两名标注者与判定器的二元一致率只有 **58.3% 与 41.7%**，且两人给出的漂移都**比判定器少**（8.3% 与 25.0% 对 50.0%）。**项目自己的 LLM 辅助判定同理，一律按上界记。**
21. **动作门自身出错时绝不可退化成放行。** MaliciousSkillBench 的 `ABSTAIN_ERROR` 第三状态："Technical failures … are **never silently converted to benign predictions**"，且**门的覆盖率必须与门的准确率分开报告**。这是一行不变量，但安全后果是实质性的。
22. **播报前动作门不能是学出来的分类器。** 在跨来源分布漂移下，一个学出来的文本门只能用 **62.4% 的良性误报率**去买 95.6% 的召回，作者结论是"**no detector occupies the desired high-recall, low-FPR regime on held-out sources**"。**动作门必须是确定性的 schema 与车态校验。**
23. **MaliciousSkillBench 的聚合 CC-BY-4.0 不能当成整份数据可商用。** 其 `THIRD_PARTY_NOTICE.md` 写明第三方 Skill 制品**不被重新许可**、保留各自上游条款，而 Table 6 里 **SRC001 为 academic-research-only、SRC006 为 terms not provided、SRC010 为 CC-BY-NC-SA-4.0**。**这是本轮第二个"聚合许可证掩盖了成分许可证"的例子，第一个是 MaAI 的逐模型拼盘。**
24. **StateMemBench 声称发布了基准与标注工具包，而全文 23 页没有任何 URL。** 那套标注工具包（表单、答案键、分析器）是本批次对项目最直接可用的制品，**现在拿不到**，属第三档 unverifiable。
25. **EnvHarness 的 Table 4 里整个 WebShop 列没有对应的已发布实验。** 242 条仓库路径中 `webshop` 出现 0 次，README 也从不提及。**仓库存在不等于论文声称的制品存在。**
26. **MaAI 建于 2025-07-04，比本项目追踪表还早，却到今天才被发现，原因是它十三个月没有产出会被 14 组关键词命中的论文。** 它最近十二天连续新增的四项功能（VAD、中文 VAD、应答检测、单通道模式）全部落在本项目最近几轮的窗口内且全部被漏掉。**这说明制品通道是与关键词查询、逐版复核并列的第三条发现通道，而不是可选补充。**
