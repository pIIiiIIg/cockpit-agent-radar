# 每日调研日报｜2026-08-11

证据日期：项目快照仍是 2026-08-10 10:30 那一份（最新提交 2026-08-04、最近 H20 A/B
2026-08-03），本日项目侧无新增；Radar 生成于 2026-08-11 09:29，候选含 2026-08-10
14:23/19:21 两批。本日报未使用 2026-08-11 之后证据。

方向：语义判停与打断 — DuplexGen（当前 `probe_turn_end` 已证明主模型自判停不可行，
且缺少“该不该打断”的判据来源；它不让主模型判停，而是在每个可插入点收五人分布式标注，
用 KL 软标签把小模型拉向人类分布，4B 上平均 KL 0.335 优于纯提示 0.458；可以尝试只搬这套
标注协议做中文车控改口/追加约束/多乘员插话的分场景试标，不搬它的英文语料和 PersonaPlex）

方向：打断评测的自动裁判 — 反事实副语言审计（当前多轮打断评测最省事的做法是找音频大模型
打分，风险极高；该文固定转写只改情感与时序，测出 Qwen-2.5-Omni-7B 为 50.0/47.1、
Phi-4-Multimodal 与 Voxtral 同样贴随机线，Gemini 只在两选项同时可见时才好；可以尝试
把“先过反事实审计”写成自动裁判准入条件，并在现有基准上只改语气不改文本，看工具、参数
和最终车态有没有不该发生的变化）

方向：聚合意图与长时记忆分层 — StreamMind（当前聚合意图展开不全，记忆一旦做起来极易被
塞进用户等待路径；它用非思考前台只做即答/派单/建监视器，后台异步写分层事件记忆并做召回，
同骨干下平均查询到答案时延从 81.4s 降到 27.5s、保留 89.7% 准确率；可以尝试只借这条执行
契约——后台记忆构建不得进入 commit 到首动作关键路径、记忆读取以查询时刻为界，绝对秒级
时延与 397B 骨干不可比）

方向：解码时延归因 — 驾驶 VLA 层裁剪（当前 110ms/token 的根因是 HF MoE 每 token 约
816 次 GPU→CPU 同步，不是深度冗余；该文测出导航命令在第 1 层后即有 97.7% 线性可解码性，
而与冻结规划器兼容性到最后一层才最好，裁 8/32 层可得 1.33× 解码器加速；可以尝试借它
“语义可读性与下游可执行性分开测”的思路诊断上下文引力，但先修同步再谈裁层）

方向：本地 TTS 首音频（当前首音频 P50 仍为 2,918/3,491ms，Edge TTS 是主因；今日无新的
TTS 正文证据，DuplexGen 只用 Chatterbox/ElevenLabs 渲染离线语料且未报告首包；沿用
2026-08-10 的 `omnivoice-local-tts-16step-v1` 离线单变量，不新增 TTS 候选）

无重大新增的方向：回声消除与降噪、空间听觉、多乘员定位与分离、KV cache、实时推理服务，
今日 Radar 均无可用一手正文，继续跟踪，不作任何推测性结论。

今日新增 handoff 候选：无。DuplexGen 与 StreamMind 目前只能形成评测规范与执行契约，
缺少项目侧独立失败桶基数、单变量和可信的预计净修数，按既定规则只留在报告，不进候选 JSON。

开放状态提醒：DuplexGen 代码 Apache-2.0 且可用，两个语料公开但按场景混合继承许可证；
但承载 PP-DG 结果的 `personaplex-finetune` 至今仍只有 README，官方声明不含检查点。
StreamArena 数据为 CC-BY-NC-4.0、代码仓库许可证 NOASSERTION。副语言审计与驾驶 VLA
两篇未发现任何官方代码/数据发布，记为未知。

镜像与重复来源：今日 45 条候选里，12 条是 Nemotron-3-Nano-Omni 的第三方 MLX 量化镜像
（另有 2026-08-02 GGUF、2026-08-04 NVFP4 同谱系），Stockmark JapanDocReader 的论文页
与模型页是同一发布；DuplexGen（2026-07-30 已收录）、StreamArena（2026-08-07 已收录）
为第二来源；AES/HDC（2608.03571）已在 2026-08-05 报告完整覆盖，今日不重复展开——注意
它说的 harness 是训练脚手架，与 StreamingModelHarness 不是一回事。
